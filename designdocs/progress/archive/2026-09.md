# September 2026 completed checkpoints

## 2026-09-11

- Task event: `PK-H3-UPSTREAM-V4-WIN` — DeepSeek's canonical model rename broke
  both live Knowledge scenarios. [Issue #3](https://github.com/yydspanda/obsidian-copilot/issues/3)
  retains old Flash compatibility, blocks Pro before I/O, and passes 2/2 live scenarios.

## 2026-09-15

- Task event: `OPS-UPSTREAM-SYNC` — Current-branch upstream synchronization.

Merged canonical `61619fe427f27c23fbb08b6a040d18ae73c3b219` into the current
development branch as `0c381e68c628ba401597c14a1dc12d11cb3d8ff8` and pushed it.
Twenty upstream commits, three test/documentation conflicts, no manual production
code edits; 85 ahead / 0 behind at the merge. Remote `master` was not synchronized,
as explicitly requested by the user.

The 48-suite / 1,238-test regression, 36 Node tests, production build, formatting,
lint and Obsidian review pass. The clean build is deployed to the Windows test
Vault; scoped files and Reviews are unchanged, and the queue remains user-paused.
Bounded gallery checks cover 36 states, with one isolated rerun. Full evidence and
limitations are in the [Windows checkpoint](../acceptance/2026-09-14-windows.md).
The broader `PK-H3-UPSTREAM-V4-WIN` acceptance task remains in progress.

## 2026-09-18

- [x] `PK-H3-UPSTREAM-V4-WIN` — Completed bounded merged-V4 Windows regression.

Five authorized materials complete compilation, explicit Review and Apply; old
notes/history and unrelated tasks remain intact. Two minimal repairs protect
legacy Project data when both writing and backup fail, and remove stack overflow
from six large-secret redaction patterns. Commits `b60bf2e6` and `4bf81d7b` are
pushed; clean artifact `05903630` is deployed and verified in the test Vault.

The final full sweep passes 722 suites / 10,472 tests, with zero failures and two
existing conditional skips. Build/typecheck, lint/format, syntax/mobile smoke,
progress governance and Obsidian review pass with retained warnings. Final
Windows checks pass settings compatibility, unload/reload, no duplicate work,
Studio/popout/close/reopen, Query, exact citation navigation and return. Extra
model requests are bounded at 9/20. The final 18/18 preservation audit proves
the post-deployment 74 files and entire Runtime unchanged; queue remains paused.

The [completion checkpoint](../acceptance/2026-09-18-unattended.md) retains the
first failed full sweep, the expected unload-time diagnostic-log refresh and
the Gallery shared-editor-style cleanup exception. It does not claim real
legacy Windows disk-fault migration or the separately registered future gates.
Canonical upstream is 11 commits ahead of the frozen baseline; its drift gate
fails and becomes the next `OPS-UPSTREAM-SYNC` task. No merge or remote-master
change is included in this completion.

- [x] `PK-H3-REVIEW11-FIX` — Repaired all four cumulative review findings.

Existing-file updates reject external real paths before `Vault.process`; Chat
retains case-distinct files; registered source reads enforce the shared 8 MiB
budget before loading and again before copying/hashing; the unused model policy
and its self-only test are deleted. Red/green regression evidence is retained in
the [review checkpoint](../acceptance/2026-09-18-review11.md).

All 10 directly relevant suites / 175 tests pass. Build/typecheck, format/lint,
syntax/mobile module-load smoke and Obsidian review pass. Production code is
net six lines smaller; the only upstream-owned production edit restores the
upstream Chat path comparison. No prompts or build policy changed.

The fresh full sweep is **not green**: 722 suites pass, one fails; 10,490 tests
pass, seven log-redaction tests fail and two are skipped. The unchanged redaction
module throws in its email-run regular expression on huge credentials; this
separate failure is not hidden or fixed by changing these review repairs. Source
hashes before and after the full run match. No commit, push, deployment, model
call, live Vault mutation or upstream merge was performed for this task.

- [x] `PK-LOG-REDACTION-WIN` — Closed the remaining log-redaction failures and
      delivered these repairs together with the four review fixes.

Commit `3806657e` is pushed to the current development branch and deployed as
`4.0.8+dev.3806657e.clean.3dbe2a0fe726` to `Obsidian-Copilot-Test` only.
The final full non-paid sweep passes 724 suites / 10,498 tests, with two existing
skips; format/lint/build/review and normal commit hooks pass. First-run local HTTP
and second-run gallery timeouts are retained in the evidence. The final run uses
the installed Jest's unmodified CLI entry and fresh cache, without relaxing any
assertion or timeout. A worker teardown warning remains visible.

Windows passes 12 isolated memory checks and two physical filesystem checks,
including actual junction rejection, plus the loaded Studio ready/paused check.
All 74 files are retained; only the expected unload diagnostic log changes.
Settings and all 66 job IDs/statuses/stages/attempts and paused control remain
unchanged. Startup revisions advance without new pending/processing work.
No real model request, live Wiki Apply, other-Vault action or upstream merge is
performed. [Delivery checkpoint](../acceptance/2026-09-18-redaction-delivery.md)
records hashes, the initial temporary-fixture correction, and evidence limits.
Upstream remains 11 commits behind, tracked separately by `OPS-UPSTREAM-SYNC`.

- Task event: `OPS-UPSTREAM-SYNC` — User-authorized current-branch synchronization
  through canonical `996a088c59a2ae123852d8e542f354b1eba72cee` (12 commits).

Exactly one text conflict in the session manager is reconciled with the existing
retired-model refusal intact. Two intentional regression failures precede the
minimal compatibility check; all 56 affected suites / 1,623 tests then pass.
An obsolete test enum reference is replaced with its concrete fixture value;
the already-repaired huge-log stress test remains enabled despite upstream's
skip. Build/typecheck, syntax/mobile load, format/lint, governance and full
Obsidian review pass with retained warnings. No dependency versions change.

The test Vault runtime is byte-identical to the prior checkpoint: 38 historic
failed attempts across six sources, all with newer completed/review outcomes.
No job retry, paid model call, Apply, deployment, Vault write or remote-master
update occurs. The prior full sweep and Windows results predate this merge;
only a short Windows smoke check is needed after a future deployment.
[Synchronization checkpoint](../acceptance/2026-09-18-upstream-sync.md) preserves
the baseline, conflict details, validation failures and remaining evidence limits.
