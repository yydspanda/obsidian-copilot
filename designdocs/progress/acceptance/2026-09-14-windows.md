# Windows acceptance checkpoint — 2026-09-14

Task: `PK-H3-UPSTREAM-V4-WIN`. Acceptance remains incomplete.

Initial frozen runtime build: `1420e2be-clean-e07399f5ce79`, based on upstream
`9e2594b468a345dc2f7718eeb3eb85e28e935308`. Artifact hashes and build inputs are
recorded in [the September experiment log](../experiments/2026-09.md).
Checks target the Windows `Obsidian-Copilot-Test` Vault through explicit CLI
targeting; observations below contain no source text, credentials or private paths.

## Verified surfaces

- Studio displays its actual Bundle identity, durable revision and Activity.
- Setup displays three `Configured locally` cards. Network verification is
  `not_tested`; this does not establish provider connectivity or successful analysis.
- `Open Project file` opens the uniquely configured Project; its identity and
  Bundle match Studio. Its one source root, Wiki root and rules file exist.
- `Open Knowledge rules` opens the exact referenced, nonempty Markdown file.
- Returning to Activity works. The user explicitly paused the Bundle; its gate
  reports `paused` with reason `user`, and `Resume bundle` is available.
- All four pending proposals remain visible. Proposal 1 proposes one file create,
  with `current` integrity, `blocks_allowed`, and three passing structural checks.
  These checks do not establish the factual correctness of the proposed content.

## Background activity remains an acceptance finding

The first post-reload observation was runtime revision 1194: 4 awaiting review,
32 failed and 9 completed. Five jobs have creation timestamps at reload time:
2 awaiting review and 3 `knowledge_compiler_analysis_rejected` failures.
Before the explicit pause, revision 1256 showed 4 awaiting review, 35 failed and
10 completed; the three additional failures have the same code. Their cause is
not yet established. No processing jobs, pending jobs, Apply claims, Apply commits
or active ordinary/forward transactions were observed at that pre-pause snapshot.

Startup can scan registered sources and process queued work without opening
Studio. No manually initiated model call is not proof of no background requests.
Do not resume or retry merely to make the counters change during read-only checks.

## Evidence navigation: file opens, exact selection fails

The user clicked Proposal 1's `Open exact source location` in Review. Navigation
settled without an error and opened a Markdown editor. The persisted proposal
digest matches the selected Review, and its single complete quote is present in
the editor. No proposal decisions or Wiki writes were requested in this check.

The quote spans the whole 3,785-character document, including YAML frontmatter.
Expected editor range is `{line: 0, ch: 0}` through `{line: 50, ch: 0}`. Actual
selection is `{line: 5, ch: 0}` through `{line: 50, ch: 0}`: 3,635 characters,
or CodeMirror offsets 150–3785. The YAML closing delimiter is on zero-based line 4.
The properties container is visible and the Markdown view is in Live Preview.

The resolver uses the complete quote, without removing frontmatter. The navigator
calls `setSelection`, `scrollIntoView`, and `focus`, then returns `opened` without
reading back the resulting range. The observed clipping aligns with the displayed
properties region, but this check does not isolate which editor operation adjusts
the selection. File opening passes; the exact-selection contract does not.

No runtime fix, editor-mode change or synthetic reselection was made.

## Review selection: state changes, interaction feedback fails

The user clicked `Use all proposed changes` and reported that success must be
checked at the bottom of the long proposal, while the clicked control feels inert.
The file/block controls now report `aria-pressed=true` and the enabled final button
reads `Validate and apply selection`. The durable Review remains `pending`, the
proposed create target is absent, and no Apply claim, commit or transaction exists.
The Bundle remains paused. Selection has succeeded without submitting a decision.

The bulk control keeps its original text and secondary styling, with no pressed
state or nearby selection confirmation. File/block choices carry `aria-pressed`
but retain fixed success/destructive variants with no Review-specific selected
styling. At the user's inspected scroll position, the bulk button is above the
viewport at y=-804 while the final button is at y=1308; the footer is not sticky.
There is no local status/live-region message confirming the selection.

Mark selection-only behavior as verified, but immediate feedback and action
placement as failed usability acceptance. No UI implementation changes were made.
Analysis failures, exact navigation, Review feedback, Apply/recovery, migration
and popout checks remain outstanding.

## Review feedback repair — implementation and isolated Windows rendering

The user authorized the bounded repair under
[issue #5](https://github.com/yydspanda/obsidian-copilot/issues/5). The only production
module changed is the fork-owned `KnowledgeReviewPanel.tsx`; the upstream shared
Button, submission protocol, prompts and build budget remain unchanged.

Bulk, file and block choices now show a selected checkmark, accent border and
pressed state. One top sticky action bar contains the choices, an immediate live
status and the single final submit button. Its status follows retained decisions,
including partial choices and refused controlled updates. `Selections alone do
not write files.` describes selection semantics; it does not claim that an earlier
submission could not have written files when the old snapshot remains mounted.
Selection does not auto-submit or request navigation to the bottom.

Verification against the unchanged upstream baseline above:

- Panel regression: 29/29 pass, after 13 intended initial failures and a separate
  failing-then-passing stale-post-submit wording regression.
- Studio root, controller and draft-store regression: 3 suites / 86 tests pass.
  A separate controller rerun passes its same 58 tests, not additional coverage.
- Gallery infrastructure: 3 suites / 30 tests pass. Six adjacent stories cover
  interactive, all-selected, mixed/partial/edited, busy, read-only/blocked and long proposals.
- Formatting, lint (0 errors / 3 existing warnings), TypeScript and the complete
  Obsidian review pass; dependency audit reports 0 vulnerabilities.
- Real Windows gallery: all six stories render at 300, 340, 400 and 600 pixels
  (24 cases), with no button overflow, exactly one submit, correct selected-icon
  visibility and disabled states, and no captured uncaught errors/rejections.
- At widths 300 and 600, selecting all eight virtual files while scrolled keeps
  the action bar at the same viewport y-coordinate. Browser scroll anchoring adjusts
  scrollTop by -40 / +16 pixels as text reflows; this is not a zero-scroll-offset
  guarantee. It does not navigate to the bottom. These are DOM/layout checks,
  not screenshot or full contrast audits.

The gallery uses inert virtual proposals and submits no real review. The test
Vault remains paused by the user with 4 awaiting review, 35 failed, 10 completed
and 3 cancelled jobs. Production deployment and the user's subjective feedback
remain pending at this checkpoint. The separate analysis and exact-evidence
findings above have not been repaired.

## Review feedback repair — production deployment

Source commit `7b0c9a8b` was committed and pushed to the fork. The clean production
build `7b0c9a8b-clean-9c310a31aac2` replaces the initial artifact; upstream remains
frozen at the same baseline. The deployment used the existing `npm run test:vault`
with `COPILOT_PERSONAL_MAX_BUNDLE_BYTES=10000000`, the explicit Windows test Vault
and an Obsidian CLI wrapper that passes its Vault name. No build scripts changed.

- `main.js`: 6,394,785 bytes; SHA-256
  `4dfed448edc1d30babca5df1ca7f3bc4c1ba0647a1af1ad37dc305316679d1ce`.
- `styles.css`: 77,051 bytes; SHA-256
  `ff7e9600fb4d33cb4992bdfb58f513ad1c20cf73c805a8949e44fb1caa8f275c`.
- Both copied artifact hashes match the local build. Production TypeScript,
  `node --check main.js`, simulated mobile module-load smoke and progress governance pass.
- The deployment command exits 0, but esbuild again prints its shutdown deadlock
  after artifact creation. This existing tooling finding is not claimed fixed.
- Automatic Windows CLI reload fails. Explicit plugin-manager disable, manifest
  refresh and enable replace the old instance successfully. Runtime reaches
  `workflow_read_ready`, reporting version `4.0.8+dev.7b0c9a8b.clean.9c310a31aac2`;
  captured startup uncaught errors and unhandled rejections are both zero.
- The temporary gallery plugin and its three copied artifacts were removed before
  production deployment; its stylesheet cannot mask the real plugin's rendering.
  Previous production artifacts were backed up outside the Vault for rollback.
- Real Studio reopens on Proposal 1. Its visible action bar has computed sticky
  positioning, reports `0 of 1 file decided`, and keeps final submission disabled
  until decisions are made. No synthetic selection or submission was made against
  this real proposal after reload.
- The Bundle remains `paused` with reason `user`; 4 awaiting review, 35 failed,
  10 completed and 3 cancelled remain unchanged. Two jobs are now queued after
  reload; no parsing, analysis, generation, validation, Apply or finalization job
  is running. Their cause was not investigated in this UI-only repair, and the
  pause was not lifted to process them.

This deploy does not establish user acceptance of the new interaction or completion
of Apply, analysis, exact-evidence, recovery, migration or popout checks.

## User confirmation — selection feedback passes

The user confirms that the updated selection feedback is clear. Read-only Windows
inspection of the same deployed build shows Proposal 1 with one proposed file
create, `All 1 file selected`, the bulk choice pressed and `Validate and apply
selection` enabled. Mark immediate selection feedback as passed by the user;
this does not establish that Apply has run or that the proposed content is correct.

The Bundle remains paused by the user. Counts remain 4 awaiting review, 2 queued,
35 failed, 10 completed and 3 cancelled, with no running processing or Apply job.
No synthetic click, submission, resume, reload or production-code change was made
during this read-only checkpoint. Apply acceptance is held for the paused-state
finding below; do not resume the two queued jobs simply to enable this check.

### Paused-state Apply mismatch — source inspection, not live reproduction

The live Review button is enabled while the Bundle is user-paused. Source
inspection shows that `KnowledgeProductionReviewedApplyCoordinator.submit()`
persists acceptance before calling `IngestQueue.beginReviewApply()`. The latter
rejects the current user pause (startup recovery has a separate exception); the coordinator retries the same
handoff, then returns `recovery_required` after an accepted review if it still fails.
Thus a valid accepted proposal can become durable before user pause prevents the
Apply handoff, without reaching the Wiki transaction. Review capability checks
do not currently exclude this user-paused state.

This risk was not triggered against the real proposal. It is a separate workflow
finding, not evidence that the selection-feedback repair failed. No fix, Resume
or Apply was authorized by this confirmation of visual feedback.

## Paused Apply repair — authorized implementation and verification

The user subsequently authorized the repair under
[issue #6](https://github.com/yydspanda/obsidian-copilot/issues/6). Ordinary Review
keeps local file/block/edit choices available but shows `Apply paused` and disables
write-bearing submission when Activity is not running. Whole-proposal rejection
remains available. A stale UI cannot bypass the controller check, and a pause
during asynchronous validation is checked again in the same atomic Runtime
transform that would persist a pending-to-accepted transition. The safe blocked
result propagates through the repository and coordinator without misreporting
proposal validation or creating Recovery solely from that pre-acceptance pause.

The repair preserves the existing startup-recovery exception, historical accepted
replay, and ordinary rejection. A pause or interruption after acceptance is durable
still uses the established recovery contract; this change does not merge acceptance
and the Apply claim into a new protocol or eliminate all crash windows. There is
no automatic Resume, schema migration, model request or real Review submission.
All 14 changed source/test/story files are fork-owned; none exists in the frozen
upstream tree. Shared upstream components and prompts are unchanged.

Verification:

- Backend: 7 complete suites / 462 tests pass. New regressions first observed
  6 intended failures, then all 12 targeted cases passed. Coverage includes user
  and rate-limit pause, pause inside the atomic transform, explicit Resume in the
  in-memory harness, rejection, startup recovery and post-acceptance interruption.
- Controller: 65 tests pass, after 6 intended failures and 1 already-correct case
  in the new 7-case group. Blocked submission retains the local draft.
- Panel: 35 tests pass, including 6 new pause cases; 4 initially failed as expected.
  Two capability cases additionally failed under a temporary mutation, then passed
  after restoration. Root: 26 tests pass, including ordinary-state mapping and
  Forward isolation. Forward behavior is not changed by this ordinary-Review fix.
- Four additional integration suites / 34 tests pass: KnowledgeCommitPipeline,
  ReviewQueueHandoff, KnowledgeProductionRecoveryActionCoordinator and
  KnowledgeStudioRuntimeCommandAdapter. Total: 14 suites / 622 tests, without
  counting targeted reruns again.
- Formatting, lint (0 errors / 3 existing warnings), complete Obsidian review and
  dependency audit pass. The test-fixture unsafe-return lint error was corrected
  without suppression. Independent final review found no blocking defect.
- Windows gallery: 7 stories at 300/340/400/600 pixels render 28 cases with sticky
  actions and zero button overflow. Paused choices show the pause explanation and
  disabled `Apply paused`; 8 virtual bulk-choice interactions show all-selected
  still blocked and all-skipped allowing `Reject proposal`, without clicking either
  final submit. Read-only state remains unavailable, not mislabeled as resumable.
  Captured uncaught errors/rejections are zero. These are DOM/layout checks, not
  screenshots or a full contrast audit. The first diagnostic's final-button label
  whitelist omitted `Review actions unavailable`; a direct follow-up verified the
  real read-only control is present and disabled, rather than changing production
  code to match that incomplete probe.

Production deployment and the user's confirmation of the new paused state are
pending at this checkpoint. The real Vault remains user-paused with 4 awaiting
review and 2 queued jobs; no real Apply or Resume was performed.

## Paused Apply repair — production artifact

Source commit `73a26b72` is committed and pushed. The canonical `npm run test:vault`
with the same explicit personal 10,000,000-byte budget produces clean build
`73a26b72-clean-47fdb9b2fe41`, on the unchanged upstream baseline recorded above.
No production build policy or deployment script changed.

- `main.js`: 6,396,142 bytes; SHA-256
  `e91b937a85ee18c49b0eb8ef928c87e84a9c8a04976b2daf14f6e4218f038e6a`.
- `styles.css`: 77,051 bytes; SHA-256
  `ff7e9600fb4d33cb4992bdfb58f513ad1c20cf73c805a8949e44fb1caa8f275c`.
- Both test-Vault artifacts match the local build. Production TypeScript,
  `node --check main.js` and simulated mobile module load pass. The deploy command
  exits 0; this run captures no esbuild shutdown deadlock, which does not establish
  that the previously intermittent tooling finding is fixed.
- The temporary gallery and its three artifacts were removed before deployment.
  The previous production build is backed up outside the Vault. Automatic Windows
  CLI reload still fails, so the explicit plugin-manager fallback is required.
- Plugin-manager disable, manifest refresh and enable replaced the old instance;
  version `4.0.8+dev.73a26b72.clean.47fdb9b2fe41` reached `workflow_read_ready`.
  Captured startup uncaught errors and unhandled rejections are both zero.
- Real Studio reopened on Proposal 1 with the pause explanation visible and
  `Apply paused` disabled. Bulk choice buttons remain enabled and the status is
  `0 of 1 file decided` after reload. No synthetic choice, Apply or Reject was
  invoked against the real proposal; the proposed create target remains absent.
- User pause is retained with its original timestamp. Counts are unchanged:
  4 awaiting review, 2 queued, 35 failed, 10 completed and 3 cancelled, with no
  processing, Apply or finalization job running. The temporary gallery is absent.

The deployed paused-state control passes read-only runtime inspection. The next
user check is selection feedback while Apply stays disabled, not a Resume or
write-bearing submission. Normal Apply, analysis, exact-evidence, recovery,
migration and popout acceptance remain incomplete.

## User checkpoint — selecting while paused

The user clicked `Use all proposed changes` on Proposal 1 in build
`73a26b72-clean-47fdb9b2fe41`. Read-only inspection confirms `All 1 file selected`,
the bulk button pressed, the pause explanation visible, and `Apply paused` still
disabled. The target remains absent. All 4 reviews remain awaiting review, with
2 queued jobs and unchanged failed/completed/cancelled counts; the original user
pause and timestamp remain intact, with no processing or Apply underway.

Mark the live selection-while-paused checkpoint as passed. No automated submit,
rejection, Resume, reload or code change was made. The next single navigation step
is Back to Activity; it does not authorize releasing the queued work or applying
the proposal.

The user then clicked Back. Read-only inspection confirms the active Activity
tab, the same user pause, 4 awaiting reviews, 2 queued jobs, no Recovery items and
no active command or processing job. `Resume bundle` is visible and enabled.
Continuing normal Apply requires an explicit decision about releasing queued
work, which may call models; no Resume was performed in this checkpoint.

The user subsequently authorized resuming the two queued jobs after being told
that background model calls may incur costs. The pre-click inspection still shows
the same deployed build, user pause, 2 queued jobs, 4 pending proposals and no
Recovery items; one visible `Resume bundle` button is enabled. The next manual
step is a single Resume click. This authorization is not an instruction to submit
any proposal, retry failed jobs, reload the plugin or change code.

## User checkpoint — Resume completes, processing is only partially successful

The user reports clicking Resume. On the same deployed build, Activity shows
`running`, `Knowledge activity resumed.`, no active command and zero queued or
processing jobs. The two jobs that were queued before Resume have finished this
processing attempt: one awaits Review and one failed during validation with
`knowledge_compiler_analysis_rejected`. Counts are 5 awaiting review, 36 failed,
10 completed and 3 cancelled; Recovery remains empty and no Apply is running.

Mark the Resume control as passed, not both processing jobs as successful. The
additional analysis rejection remains an unresolved finding; no Retry was made.
The current selected Review points to index 0 and has no retained local choices,
so do not assume the old selection or display number survived refresh. The
currently selected plan proposes one create with current integrity and passing
structural checks; its target is absent. These checks do not authorize Apply or
establish content correctness. The next single manual step is opening the Review
tab to inspect the five proposals, with no submission or further queue action.

The user opened Review. The selected Proposal 1 is
`changeset-ac17cde50a6a00823e588fdaa5c01ff74c6a5d82f6217c3dc4b6aaca54e04e57`;
record this opaque identity rather than relying on display order in later checks.
It contains one current, block-reviewable create whose target is absent. All
three structural checks pass, but no local decision is retained: the status is
`0 of 1 file decided` and `Choose all decisions` is disabled. Bundle control is
running, with no queued, processing, Apply or Recovery work and the same five
pending reviews. The next manual step is selection only, not final submission.

The user selected all changes on that same opaque proposal. The retained decision
is `accept_exact`, the status reads `All 1 file selected`, and `Validate and apply
selection` is enabled. The Bundle is running, with no queued, processing, Apply
or Recovery work; the create target is still absent. Capture the selected final
text's SHA-256 before the manual Apply so later verification does not depend on a
proposal still being displayed after submission:
`6a86e36de3884527c8487b4a38874e07ede45fbeaff40d3b6a66ddcd3e56371b`.
The exact target identity and expected hash are retained only in a temporary
renderer diagnostic for the follow-up check; no note body is emitted or written.
The next manual action is one explicit `Validate and apply selection` click,
which can create the proposed Wiki file only after its submission checks pass.

## User checkpoint — Apply fails before durable acceptance

The user clicked `Validate and apply selection` once. The UI reports the generic
error `The Knowledge Studio action could not be completed. Durable state was
reloaded.` The same proposal remains visible with `accept_exact` selected. A
separate Runtime read confirms outcome `pending`, record revision 0 and the
matching Queue job still `awaiting_review`, with no Apply claim. The target is
absent, its parent exists as a directory, there is no active ordinary/Forward
transaction, and no Apply commit matches this ChangeSet. Counts remain 5 awaiting
review, 36 failed, 10 completed and 3 cancelled, with no processing or Recovery.
Mark this Apply attempt failed, not applied or partially accepted.

Read-only freshness checks identify one concrete mismatch. The exact source is
still registered and its current SHA-256 equals the proposal's source hash:
`6a86e36de3884527c8487b4a38874e07ede45fbeaff40d3b6a66ddcd3e56371b`.
The proposal and its Queue anchor retain input revision 14 and pipeline fingerprint
`eb987cd6fe2ad95bb7772ead398b55eec8d82479276c72743b7b91a05b81a514`.
The source's retained rerun and high-watermark instead record revision 32 and
fingerprint `ce9f1cef9da24274e4db8d259e6039e06fae8746792eda8c4cc6e009fca46790`,
with the same source bytes. The job has `rerunRequested=true`.

The execution plan's read-only preparation rejects a job whose fingerprint
differs from the current watched source with `job_not_authorized`. That class
is not mapped to a stale/blocked Review result. This makes configuration drift
a supported failure hypothesis, but the current execution plan's fingerprint
was not directly observed and the original exception was not retained: the
controller catches it without logging name/code/stage and shows only the generic
error. Do not claim the exact thrown exception or sole root cause is proven, or
attribute it to the separate resumed job's analysis failure.

No retry, rejection, Resume, reload, model request or production-code change was
made during diagnosis. The temporary diagnostics are removed after recording
these summaries. Further implementation or a new submission requires direction;
normal Apply acceptance remains blocked on this finding.

## Outdated Review repair — issue #7

The user authorized repairing old proposal compatibility. The bounded change under
[issue #7](https://github.com/yydspanda/obsidian-copilot/issues/7) compares each ordinary
pending proposal with the current generation's registered source/pipeline identity.
It retains old proposals and target-integrity information, adds an explicit outdated
Apply gate, and leaves compatible proposals and the separate Forward workflow alone.
Command-time verification still owns acceptance. Known configuration/source-authority
proof failures return a sanitized blocked result before durable acceptance; arbitrary
artifact read failures are not relabelled as proven configuration changes. Literal
whole-proposal rejection uses the existing atomic identity proof without requiring
old source preparation. No persisted schema, fingerprint, prompt or upstream baseline
is rewritten, and no proposal is automatically rejected, regenerated or applied.

Deterministic verification: 9 suites / 254 tests pass (Review Panel 40, Studio Root
31, Controller 73, read/command adapters 49, production composition 22, reviewed
Apply/Review handoff/recovery coordinators 39). Red phases observed 28 intended
failures across the read adapter, composition, backend and UI before implementation;
five UI isolation/capability mutations also failed as intended and were restored.
The mixed current/old-proposal fixture confirms that only the incompatible proposal
is marked, without changing shared accept/reject capabilities or durable records.
Formatting, lint (0 errors / 3 existing warnings), progress governance, whitespace
checks and the full Obsidian review pass; dependency audit reports 0 vulnerabilities.
The review gate's intentionally invalid manifest fixtures are rejected as expected.
All six changed production TS/TSX files are fork-owned Knowledge modules; none exists
at the frozen `origin/master` baseline. No upstream source or build-budget policy changed.

Windows virtual gallery: all 9 Review stories rendered at widths 300, 340, 400 and
600 (36 cases), with no horizontal host overflow and a sticky action area throughout.
Both outdated states display the notice. The interactive outdated story blocks Apply,
enables explicit whole rejection after Skip all, and blocks Apply again after reselection;
the read-only story keeps actions unavailable. Current, paused, busy and unfinished
states retain their expected labels and gates. The temporary error counters report
0 uncaught errors / 0 unhandled rejections. These are virtual stories, not real-proposal
submissions or a full visual/contrast audit.

Before deployment, the real Vault remains running and idle: 5 pending reviews,
36 failed, 10 completed, 3 cancelled. The 11 durable Review identities/outcomes and
5 distinct target file hashes/absence markers were captured without emitting note
contents. The current three plugin artifacts were backed up to
`/tmp/copilot-stale-review-rollback.3UMqQz`; settings and notes were not copied.

Deployment evidence will follow. This repair does not establish the
exact discarded exception from the earlier failed Apply, nor does it resolve the
separate analysis rejection or source-navigation selection finding.

## September 15 — outdated Review deployment and real-state check

Code commit: `fbc413a05aca8c7ab09cffb3554892ba8c2ff8df`. Canonical production deployment
completed at `20260915-001050` with the explicit personal 10,000,000-byte budget;
the default 5,000,000-byte policy is unchanged. TypeScript, artifact syntax and the
simulated mobile module-load smoke pass. Actual deployed artifact hashes match local:

| Artifact     |     Bytes | SHA-256                                                            |
| ------------ | --------: | ------------------------------------------------------------------ |
| `main.js`    | 6,398,158 | `870260562f8ad190c6757f566307987aa5d511bceeb34e0fce1b4901c4917563` |
| `styles.css` |    77,051 | `ff7e9600fb4d33cb4992bdfb58f513ad1c20cf73c805a8949e44fb1caa8f275c` |

The canonical script's automatic Windows CLI reload failed as before. The verified
plugin-manager fallback disabled the old instance, refreshed manifests and enabled
the new instance. Readback proves instance replacement, version
`4.0.8+dev.fbc413a0.clean.1e0abdc3fa8a`, build `fbc413a0-clean-1e0abdc3fa8a`, and
`workflow_read_ready`. The temporary gallery was disabled and its three files removed
before deployment. Temporary error counters remained at 0 uncaught errors and
0 unhandled rejections through gallery verification, unload and startup.

Real Studio is open on the same Proposal 1. Its outdated notice is visible and
`Proposal out of date` is disabled; both bulk choice buttons remain available and
unpressed after reload. No real selection, Apply, Reject or Retry was automated.
The current generation classifies exactly the first two retained proposals as outdated:
`changeset-ac17cde50a6a00823e588fdaa5c01ff74c6a5d82f6217c3dc4b6aaca54e04e57`
and `changeset-7de0b9de94da573cbfd208dcdf4b1aae1f84e726aa2a567040aba3d2bc14b216`.
The other three are not marked by this configuration gate; that is not a guarantee
that their subsequent full acceptance validation will pass. All 11 durable Review
identities/digests/revisions/outcomes and all 5 target hashes/absence markers match
the pre-deployment baseline. All 5 proposals remain pending and Recovery is empty.

Startup did perform background processing in the already-running Bundle: the failed
count increased from 36 to 37, with the new job ending at validation under
`knowledge_compiler_analysis_rejected`, the same recorded code as the earlier separate
analysis finding. There are no queued or processing jobs at the final read; completed
10 and cancelled 3 are unchanged. This was not a controlled model experiment; exact
request count and model output were not captured, so do not claim zero model calls or
successful analysis from the deterministic test or reload results. No Retry or fix
for that separate failure was performed in this bounded repair.

The next single user checkpoint is confirming the visible outdated notice/button on
Proposal 1. Deciding whether to reject it and release a retained rerun is a later
explicit action; normal Apply and the other full-flow findings remain unverified.

All nine temporary acceptance globals and both error listeners were removed after
recording the summaries; the real Review page remains open and the gallery is absent.

## September 15 — explicit rejection and fresh Apply completed

The user explicitly rejected the outdated proposal
`changeset-ac17cde50a6a00823e588fdaa5c01ff74c6a5d82f6217c3dc4b6aaca54e04e57`.
Readback confirms outcome `rejected`, record revision 1, its job `cancelled`, no
pending Review anchor, and a matching rejection receipt. Its old create target
remained absent. Releasing the retained rerun produced a new pending proposal
`changeset-00c2a6c818dbf7f32156188043f0694dc34c325608c9951ae64143aeb6f5b26f`
for input revision 33, with the same source bytes and the newer pipeline.

The user opened that new proposal, selected its create and clicked Apply once.
The exact target `Wiki/Acceptance/Query Writebacks/H3 Reviewed Writeback Acceptance 20260806.md`
now exists and its SHA-256 matches the selected candidate:
`514a252678b8585ab58ff6d5c41bc1449337a260041b3ff24cd0981d0a6bbb9b`.
Review is `accepted`, record revision 1; the matching job is `completed`. The
stable Runtime revision 1403 sandwich proves matching accepted digest, source
identity, input revision, pipeline and Manifest intent across Review and the
Apply authority. The current and committed Manifest revisions are both 16 and
their digests agree.
No pending anchor, Apply claim, commit acknowledgement or active journal remains.
Recovery is empty. This establishes Apply success, not the success of unrelated
background analyses. The final Activity counts are 4 awaiting Review, 38 failed,
11 completed and 4 cancelled, with zero queued or processing jobs.

## September 15 — bounded analysis investigation, issue #8

On the unchanged deployed `fbc413a0-clean-1e0abdc3fa8a` build, the latest four
analysis rejections belong to the same source and retain the same source-content
hash and pipeline as its latest observation. Their input revisions are 74, 73,
72 and 69. Activity rows are not compiler input; old terminal jobs do not execute
merely because they remain visible. A new observation after generation replacement
can nevertheless enqueue unsuccessful material again. This is distinct from Retry
of an old `retryable:false` record.

The production model adapter already structurally validates analysis before the
compiler's semantic, resource and target checks. The compile-to-review handler
discards those detailed diagnostics and persists only the stage-specific message.
The old records therefore cannot identify the exact failing check or reproduce
the original model response.

A read-only check identifies a concrete current rule/authority conflict: the test
Schema instructs every regular ingest to target the same Wiki page, while Manifest
revision 16 assigns that generated page only to a different registered source.
The repeatedly failing source has no successful output and no authority over that
page. Following this rule necessarily conflicts with production target ownership.
This is a reproducible current blocker, not proof that every historical failure
had the same cause. No model call, rule edit, history deletion or Wiki write was
performed during these reads. Permission to change the test rule was requested;
the built-in prompts and write-authorization checks remain unchanged.

The issue #8 code repair is limited to the fork-owned compile-to-review Handler:
analysis failure messages retain at most five distinct, fixed allowlisted check
codes. Unknown codes, raw messages and fields are never projected. Classification,
retryability, rate limiting and non-analysis failure behavior are unchanged; the
maximum possible message is 266 characters, below the existing 1,000-character
Queue limit. No Runtime format change or history migration is involved.

Deterministic verification: the clean red phase has 41 intended missing-diagnostic
failures and 7 already-passing safety cases. The complete Handler suite then passes
64 tests. Production input passes 6 tests, including the new conflict reproduction:
following a Schema-directed target owned by another source yields exactly
`compiler_target_manifest_authorization_missing`, without target reads, generation,
candidate validation or Manifest mutation. The compiler, security and model-adapter
suites pass another 89 tests (5 suites / 159 tests total). Mocked compiler-result
cases establish the diagnostic projection contract, not 38 actual model failures.
Independent review finds no blocking leak or behavior change.

The new ownership regression was also checked against a temporary over-broad
authorization mutation: it fails on the promised empty authorization set. The
production input module was restored byte-for-byte to its original SHA-256
`a910aafa0b8c5c919655ef41fe864ec9c4f9038b072dbe5ccad8c969838c56a7`;
no authorization change is included in the repair.

The local production build passes under the explicit personal 10,000,000-byte
budget at 6,399,547 bytes; syntax and simulated mobile module-load checks pass.
Its `main.js` SHA-256 is
`6bf936b3bd4fe5fcfa1a0255fc470d2b41bbd6e10bba7b1ffa84a0961c12db00`.
The default upstream build limit is unchanged. This artifact is not deployed;
Windows still runs the prior frozen build. No new real model request was made,
and the proposed test-rule change remains unapproved. The temporary read-only
diagnostic variable was removed, with Activity counts unchanged at 57 total.

Formatting, lint (0 errors / 3 existing warnings), progress governance and the
complete Obsidian review pass. Dependency audit reports 0 vulnerabilities; the
review fixture's intentionally invalid manifests are rejected as expected.
No upstream-owned production file is changed. Changes remain local and uncommitted.

## September 15 — approved test Rules repair and diagnostic deployment

The user explicitly approved changing the test Rules and one targeted model attempt.
The real Bundle was paused before either operation: pause reason `user`, timestamp
`1789409681383`, initially zero runnable jobs. No terminal history was deleted.
The pre-change Runtime envelope was retained only in renderer memory for an isolated
test; its SHA-256 is
`e5259f04d6d972b6b4ffb827a973669e5700a0217119b53e07800dc34b8eb79c`.
The baseline also records hashes for 13 source/Wiki/Project files and the 12 durable
Review records. This clone must never be written back to the real Runtime.

`Knowledge/Schemas/acceptance.md` now restricts regular ingest to explicit writable
authorizations; otherwise it proposes at most one create-only, source-specific page
under the configured Wiki root. The application still verifies availability and
ownership. Generation uses only its supplied target IDs. Query writeback instructions
are unchanged. No built-in plugin prompt or authorization implementation was edited.
The rule was updated through Obsidian's compare-and-swap `vault.process` operation.
Old/new rule hashes are respectively
`d67f85de16c74e616f640a62d6fffbf8f58f409273aa66573b3544fa7cf3cd32` and
`96fcdde8a985119a1c58af4240b67b66b72fc34a2df08fd0afc02673855ca53a`.
The exact old rule and prior plugin artifacts are backed up locally under
`/tmp/copilot-analysis-rules-rollback.3LWuvY`; these temporary files are not a
versioned or durable backup service.

The issue #8 diagnostic build is now deployed: running manifest version
`4.0.8+dev.00522e4f.dirty.a1b135c7fe9d`, build timestamp `20260915-022218`.
The explicit personal-budget `npm run test:vault` build/copy completed successfully.
Automatic CLI reload still needed a plugin-manager disable/load-manifests/enable
fallback; replacement instance identity and `workflow_read_ready` confirm activation.
The copied main bundle is 6,399,547 bytes with the SHA-256 recorded above; styles are
77,051 bytes, SHA-256
`ff7e9600fb4d33cb4992bdfb58f513ad1c20cf73c805a8949e44fb1caa8f275c`.
Local and copied hashes match. The default upstream 5 MB ceiling is unchanged.

The new pipeline observation leaves 3 jobs queued while the real queue stays paused.
The other counts are unchanged: 4 awaiting Review, 38 failed, 11 completed and
4 cancelled (60 total). All processing/Apply/finalization and Recovery counts are zero.
All 4 pending proposals are now marked outdated because their Schema fingerprint
predates the approved rule; they are preserved, not rejected or automatically applied.

The authorized isolated model attempt is recorded as `EXP-20260915-001` in the
September experiment log. Preparation made zero requests and matched the current
real source hash and pipeline. One analysis request returned HTTP 200, but processing
had not completed when the 120-second diagnostic deadline expired (120,857 ms measured).
The isolated queue/route were closed; no generation, retry, fallback or Apply occurred.
This result is inconclusive for the live rule repair, not an acceptance pass and not
evidence that the historical ownership rejection recurred. No complete response or
token usage was available in the timeout summary.

Final verification matches all 13 scoped source/Wiki/Project file hashes and the
complete scoped file set, all 12 Review outcome/revision/digest proofs, and the real
queue counts/control baseline. The new source-specific Wiki target is absent.
Runtime revision 1425 was observed both during and after the attempt. Recovery remains
empty. The temporary diagnostic module was unloaded, its two files and directory were
removed, and its RAM-only Runtime snapshot and diagnostic globals were cleared.
No permanent companion plugin was installed. The diagnostic repair remains loaded
in Copilot, with the real queue paused and no further model call authorized by this run.
The two modified test modules pass again: 2 suites / 70 tests. Source changes are
still local and uncommitted; broader Windows acceptance remains unfinished.

## September 15 — timeout diagnosis without another model call

The 120-second deadline belongs to the temporary retest harness, not a timeout
reported by the production transport or provider. HTTP 200 only establishes that
response headers arrived; production still requires a bounded body read through EOF
and response validation before analysis can complete.

The previously inspected harness has a separate failure-masking path: after
`cloneQueue.runNext()` returns, it waits for `Promise.all(usageTasks)`. The usage
observer independently consumes `response.clone()` without its own cancellation or
deadline. For example, production can reject an HTTP 200 response with an invalid
Content-Type and cancel its response branch, while the clone continues waiting for
EOF. The harness then reports its overall timeout instead of the already-terminal
Queue failure. If `runNext()` has finished, its active controller has been removed,
so the later `queue.close()` is not sufficient to cancel that observer.

A zero-network Node diagnostic reproduces three cases: a complete valid response
settles both consumers; an unfinished valid body leaves both pending; an invalid
header with an unfinished body terminates the business substitute while the observer
and harness remain pending. The last two cases produce the same retained summary:
one analysis request, HTTP 200, no usage or duration. Command:
`node /tmp/copilot-retest-stream-diagnostic.mjs`. This is a synthetic reproduction
of the stream/observer mechanism, not execution of the production compiler or proof
of which branch the historical request took. The script forbids fetch and closes
all synthetic streams before exiting. Both its author and the primary agent observed
all three assertions pass.

The original temporary harness source/bundle are no longer present at their recorded
`/tmp` paths; the control-flow review uses the source read in the earlier session.
Their disappearance time/cause is unknown. The current Obsidian renderer's time
origin is after the retest, and it has no retained matching resource-timing entry.
The historical response header values, bytes/EOF timing and isolated Queue final
state therefore cannot be recovered from this read-only investigation.

Consequently, the earlier statement that analysis itself had not settled was too
strong: only the harness was observed not to settle. The live rule repair remains
unverified; neither slow model delivery nor a hung production compiler is established.
Before any separately approved live retest, the diagnostic should preserve the
business result independently of optional usage collection, record safe stage/body
progress, and explicitly cancel any observer it creates. This turn diagnoses that
repair only; it does not implement it or change the production plugin.

Read-only Windows verification still shows the same deployed build, pause timestamp
`1789409681383`, and counts: 3 queued, 4 awaiting Review, 38 failed, 11 completed and
4 cancelled. No additional model call, queue execution, Wiki write, rule edit, history
deletion or upstream change was performed during diagnosis.

The existing production transport mock suite was rerun without changes:
`npm test -- --runInBand --runTestsByPath src/knowledge/compiler/KnowledgeDeepSeekPrivateRoute.test.ts`
passes 1 suite / 37 tests, including pending-read cancellation and invalid response
handling. These tests use injected fetch doubles, not provider requests.

## September 15 — approved offline retest-tool repair

The user approved fixing the diagnostic tool, not another model invocation. The
reusable source is now `scripts/knowledge-analysis-retest.ts`, with adjacent Jest
regressions and `scripts/knowledge-analysis-retest.md` instructions. The independent
builder writes only a new temporary plugin directory; it neither installs nor runs
the artifact and never overwrites the production bundle.

The repair removes the entire optional usage-reading branch: no response clone,
secondary reader, usage parsing or statistics Promise remains. Production receives
the original native response directly. The diagnostic saves `runNext()`'s result and
the `queue_settled` phase before reading the remaining in-memory summaries, so a later
diagnostic failure cannot discard the already-returned business result. Token usage
and observed model identity are deliberately unavailable rather than guessed.

Each request now has a separately retained AbortController. Closing the attempt
aborts those controllers even when the Queue has already released its own execution
controller; linked listeners are removed as well. The deadline remains 120 seconds.
Preparation remains zero-network; duplicate/concurrent preparation and duplicate run
calls are fenced. Source selection is an explicit parameter, not an acceptance-fixture
identifier. The maximum remains two requests with no retry or fallback.

The copied Runtime must be valid, paused and without runnable jobs. Other retained
Review/history records are not pruned to satisfy that requirement. All mutation
capabilities still point to memory, real Vault capabilities remain read-only, and no
Apply entry point exists. The current live queue is not silently reshaped for testing;
planning a suitable snapshot and any paid retest remain separate actions.

The clean red phase passes 11 tests and fails exactly the independently retained
request's cancellation after Queue settlement. Adding unconditional cancellation
passes all 12 tests. A temporary reintroduction of the old post-business statistics
wait makes the HTTP-200/invalid-header regression return `timeout` instead of the
expected production failure. Restoring the exact source hash makes all 12 pass again.
After type/lint cleanup, the primary agent's final rerun also passes all 12 tests.
The existing Handler/input/transport regression batch passes another 107 tests
(4 suites / 119 tests across these final runs); every fetch is a test double.

The two-request success fixture ends in `no-change/completed`, not a new pending
Review. Its original Review collection is empty. These tests establish this repair's
failure/cancellation/isolation boundaries, not successful generation against the
real retained Review history or completion of Windows acceptance.

Full-project typecheck passes. The final independent artifact is 3,270,455 bytes,
SHA-256 `76f0d972f8ade8b95c83ee622fa609832dd008ff56fe3509ac51c380bd257101`;
entry source SHA-256 is
`10fd6386126288799f29961cecb43215fb5249919bbc44be85d1d529f7c66c5e`.
It was built with `node scripts/build-knowledge-analysis-retest.mjs` into
`/tmp/copilot-analysis-retest-8UFUSv` without loading or installing it. The production
main/style hashes remain unchanged from the earlier diagnostic deployment. No new
runtime dependencies, package/Jest configuration, built-in prompts or upstream-owned
files changed. Independent review found no blocking issue.

Formatting, full-project lint (zero errors, three existing warnings), progress
governance, diff whitespace checks and artifact syntax checks pass. The complete
Obsidian review passes with retained warnings and zero dependency vulnerabilities;
its intentionally invalid fixture manifests are rejected as expected.

No real model invocation, Windows deployment, queue operation or Vault mutation was
performed for this repair. The new tool and earlier issue #8 changes remain local
and uncommitted.

## September 15 — committed repair and renewed bounded retest approval

The issue #8 batch is committed as `b73bbbafb6b2469917ce0643761fd6b8d09bc390`
and pushed to `fork/knowledge-h3-personal-flow`; a remote branch lookup confirms
the same full hash. All 12 changed paths are fork-owned additions relative to the
frozen canonical baseline `9e2594b468a345dc2f7718eeb3eb85e28e935308`.
The commit hook preserved the verified source hashes. The focused four-suite rerun
passes all 119 tests; full formatting and lint pass with three existing warnings.

The user separately approved one new attempt for `Sources/Acceptance/Native Source.md`:
at most analysis plus generation (two HTTP requests), using an in-memory copy only,
without other-source execution, Wiki writes, automatic retries or real-queue resume.
This is a new authorization, not a reuse of the previous consumed attempt.

Read-only Windows preflight confirms the same deployed production build and the
user pause timestamp `1789409681383`. The current Runtime is revision 1446, not the
earlier checkpoint's 1425; that difference predates this new attempt and is not
claimed as unchanged. Counts remain 3 queued, 4 awaiting Review, 38 failed,
11 completed and 4 cancelled, with no executing work or Recovery. The three queued
jobs are ordinary unattempted jobs; the retained reruns belong to other sources
already awaiting Review. Real Runtime contents are retained only in memory.

The diagnostic now handles that complete snapshot through the existing public Queue
API: only in the clone, it cancels ordinary pending jobs with attempt zero, no retry
deadline and no reserved rerun. No Runtime JSON is pruned; cancelled history remains.
It rejects executing/interrupted work, retrying jobs, non-user pauses and a selected
source already awaiting Review. Other sources' Review records and reruns are retained.
The preparation summary reports the number of cancelled clone jobs.

The new nonempty-history regression first fails in the intended two cases (13 pass,
2 fail), then passes all 15 cases after the minimal tool-only change. Its real
in-memory Queue/Review fixture preserves a pending Review and another source's rerun;
all Runtime fields outside the Queue and total revision are identical after prepare.
The two-request fake-provider run executes only the selected source. The primary
agent independently reran all 15 tests; typecheck, targeted formatting/lint, syntax
and independent review pass. No production source or built-in prompt changed.

Fresh artifact: 3,270,833 bytes, SHA-256
`dfddc90b35ccdc76321bf1435353c857cfaff4a0ea3d8614f85026b32ddef433`;
entry source SHA-256
`b0659a6aa4bf9da46455aba4960388061b1cd8296dcedbdd032335b737d2ca86`.
The dedicated builder writes `/tmp/copilot-analysis-retest-4nC8km`; its two generated
files were temporarily copied to the test Vault's diagnostic plugin directory and
loaded manually. The deployed diagnostic hash matches, while Copilot's main/style
hashes remain those of the earlier frozen production build. Zero-request prepare
retains all 12 reviews and reports exactly 3 cancelled jobs in the clone.

That tool's first live invocation stops at `phase=enqueue` with
`single_job_enqueue_failed` after 903 ms and **zero HTTP requests**. Cancelling the
selected pending job left the same source/hash/pipeline high-watermark intact; the
Queue correctly deduplicated the next identical observation against cancelled
history. The earlier fake fixture used a different pipeline fingerprint, so it did
not cover this case. This is a pre-network harness refusal, not a model failure or
a consumed paid attempt. Afterwards, all 14 scoped files (including Schema and
Project), their full path set, the complete Runtime text, Queue and Review hashes
match the preflight baseline; Runtime remains revision 1446 and user-paused with
three pending jobs. The temporary instance was closed and unloaded before repair.

The final tool keeps the selected ordinary pending job and its ID, cancelling only
the other two clone jobs. It accepts the Queue's normal enqueue/update/dedup result
only after re-reading the copied Queue and verifying exactly one pending job with
the selected ID, source, content hash, pipeline fingerprint, fresh observation
revision, attempt zero and no reserved rerun. Terminal-history dedup still fails
before HTTP. No freshness override, forced requeue or production-core edit is added.
The exact same-input/pipeline regression first reproduces the failure; the final
17 tests pass, along with targeted lint/format, typecheck, syntax and independent
review. The tests cover retained nonempty Review/rerun history rather than relying
on an empty Review collection.

Final diagnostic artifact: 3,271,429 bytes, SHA-256
`507759609dbabb0dd696bd2f0fb71e17e533b763adda73fe972d1e9c60bc01aa`;
source SHA-256 `12d144429a6948fe32382ab27fe14241e02949f1f4adb75497b9ecc916188689`.
The dedicated builder wrote `/tmp/copilot-analysis-retest-vhcYdh`; its copied hash
matches. New zero-request preparation reports 12 retained reviews and two cancelled
other-source clone jobs, with the same source/Schema/model/pipeline hashes. The
single model authorization is still unused at this preparation checkpoint.

### Remote upstream drift warning

The [push-triggered drift check](https://github.com/yydspanda/obsidian-copilot/actions/runs/34938850695)
failed at `2026-09-15T06:51:52Z`: development branch `b73bbbaf` is ahead 83 / behind 20,
while fork `master` is ahead 2 / behind 135. Fetch steps succeeded and the six policy
tests passed; these are real threshold breaches, not repair/test failures. The
Project governance workflow passed. A later read-only GitHub API lookup and fixed-SHA
comparison independently match those counts against canonical
`61619fe427f27c23fbb08b6a040d18ae73c3b219`; the full SHA was not printed in the run log.
The common ancestor remains the frozen `9e2594b4` acceptance baseline. No local
fetch/merge, threshold relaxation or branch mutation was performed. Behind counts
are not conflict-file counts; synchronization requires its own controlled checkpoint.

### Single-source model result and cleanup

`EXP-20260915-002` completes analysis and generation in 19,373 ms with exactly two
HTTP 200 responses, producing one in-memory pending Review for the selected original
job. The proposed create-only path is `Wiki/Acceptance/source-native-acceptance-source.md`,
not the shared page owned by another source. The existing 12 memory Review records
and Manifest remain unchanged. This is a successful isolated compile/validation
checkpoint, not a real Review/Apply or complete Windows acceptance pass.

Post-run checks confirm byte-identical real Runtime at revision 1446, identical
Queue/Review hashes, all 14 scoped file hashes and their full path set unchanged,
12 unchanged real Review records, zero Recovery items and the original user pause.
The proposed target remains absent. The real counts remain 3 queued, 4 awaiting
Review, 38 failed, 11 completed and 4 cancelled. No retry, other-source execution,
Wiki write, Rule edit or upstream merge occurred.

The temporary diagnostic was closed and unloaded; it was not persistently enabled.
Its two generated files and empty plugin directory were removed. All captured
Runtime/result variables were cleared from the renderer, and Copilot remains loaded.
The in-memory proposal is deliberately not published or retained as a real Review.
The monthly experiment records the exact invocation, hashes, hardware, request
metadata, success metrics and replay limitations without retaining private payloads.

## September 15 — upstream synchronization through `61619fe4`

The user approved synchronization of the current development branch only and
explicitly declined synchronization of remote `master`. Starting from clean,
pushed `4dc0528ea1c2eefd0ecc0dcbef71ad844f804055`, a fresh fetch identified 20
missing commits through `61619fe427f27c23fbb08b6a040d18ae73c3b219` (84 ahead / 20
behind before merge). Merge-tree preflight found exactly three conflicts:

- `docs/vault-search-and-indexing.md`: use the upstream Miyo connection/status table;
  the resolved file exactly matches upstream.
- `AgentSessionManager.test.ts`: preserve upstream persistence fixtures and saved-chat
  tests; retain the fork's model-selection descriptor overrides in a separate argument.
- `ChatSingleMessage.test.tsx`: preserve upstream module/test-lifecycle structure and
  selection tooltip coverage; retain the seven expanded Knowledge draft cases.

No production file needed manual conflict resolution. The resolved working tree
differs from the automatic merge-tree result only in those three files, the live
tracker and this checkpoint. Independent semantic review confirms Knowledge lifecycle/Draft seams and
model-selection safeguards remain intact; deleted upstream helpers have no remaining
code references. No built-in prompt, release entry, review threshold or default
bundle-size policy was edited. The only new runtime dependency is upstream's locked
`fflate@0.8.3`; the rest of the lockfile is preserved.

Dependency installation with lockfile handling disabled initially tried to resolve
newer floating Obsidian/CodeMirror versions and failed its peer-dependency check.
No force or legacy-peer override was used. `npm ci --ignore-scripts --prefer-offline
--no-audit --no-fund` installed the exact committed lockfile successfully; package
metadata and the lockfile still match the intended automatic merge.

Offline verification:

- 34 upstream-change Jest suites / 844 tests pass, including both resolved test files.
- 14 fork integration suites / 394 tests pass: model settings/bridge, real Knowledge
  startup/preflight/observation compositions, reviewed Apply, Studio, compiler and
  isolated diagnostic, plus the 41-case bundle guard. These all use fake model ports.
- 36 Node governance/drift/PR-size cases pass. The Jest bundle guard was accidentally
  included in an initial Node-runner invocation; it was rerun with its correct Jest
  runner in the passing fork batch, not counted as a production defect.
- Explicit personal production build passes at 6,427,228 bytes. The same source bytes
  pass the 10,000,000-byte ceiling and fail the unchanged default 5,000,000-byte ceiling.
- Artifact syntax and simulated mobile module loading pass. The complete Obsidian
  review passes with its retained warnings and zero dependency vulnerabilities;
  intentionally invalid manifest fixtures are rejected as expected.
- Full repository formatting and lint pass, with zero lint errors and three existing
  warnings (two hook-naming test fixtures and the settings-search adoption warning).

The upstream Miyo settings tests retain three React `act(...)` warnings. These are
not live Windows results. Production main SHA-256 is
`fde78958631e7c6d22774d95baca7327dc7713907fa064833893a384f91e337d`;
styles SHA-256 is `af35750f263a4192ac0aefcbc0c7c65ed69716c42ce330f25de6151aa5c18003`.
The old Windows artifact remains loaded at this offline checkpoint; the live queue
is still user-paused at revision 1446, with 14 scoped file hashes and 12 Review records
captured read-only for the deployment comparison. No model call or real Wiki operation
has been authorized or performed in this synchronization turn.

### Committed deployment and bounded Windows checks

Merge `0c381e68c628ba401597c14a1dc12d11cb3d8ff8` was pushed to
`fork/knowledge-h3-personal-flow`, 85 ahead / 0 behind the frozen canonical commit.
The normal commit hook additionally reformatted the Markdown table in
`designdocs/GALLERY_REVIEW_COVERAGE.md`; it changed no content or production code.
Remote `master` remains `5fa68724f4bb893eedbb0d7cd6becd875734d688` by user choice.
Its existing lag can still fail the aggregate upstream-drift workflow; this is not
a claim that every remote CI job is green.

The canonical deployment command, from the repository root, was:

```bash
COPILOT_PERSONAL_MAX_BUNDLE_BYTES=10000000 COPILOT_TEST_VAULT_PATH=/mnt/c/Users/yydsp/Obsidian-Copilot-Test OBSIDIAN_BIN='/mnt/c/Program Files/Obsidian/Obsidian.exe' npm run test:vault
```

Its normal lockfile-respecting dependency installation and production build
completed without source or lockfile changes. Copied main/styles hashes match the
offline build above. The CLI reload warned and left the old instance active; a
targeted test-Vault `disablePlugin` → `loadManifests` → `enablePlugin` activated
the replacement instance, version `4.0.8+dev.0c381e68.clean.d235147b24f2`.
Both before and after this fallback the real Queue was explicitly checked as paused.
Studio ends on Activity with status/availability `ready`, four outdated proposals
and no Recovery items.

Read-only baseline comparison after reload and again after gallery cleanup:

- All 14 scoped source/Wiki/Schema/Project files and their path set are unchanged.
  Ordered `{path, hash}` proof SHA-256 remains
  `afc0725e734705a9bbc65c42a1bdd6bef0d400c4f5912295825b863ce8a6ed7c`.
- All 12 Review records are unchanged; `JSON.stringify` of the selected Bundle's
  Review snapshot hashes to `8b09dcec20d307836eacc1712e88c024c89534c5f94d000c15844354740883ba`.
- Bundle owner, Project model key and model configuration projection are unchanged:
  SHA-256 `130ef9ce2f954c6d8e00402a8aa42d85120b5edeb1dffc6f6bfb34865c0202a0`.
- Queue control retains the original user-pause timestamp `1789409681383`.
  Counts remain 3 pending, 4 awaiting Review, 38 failed, 11 completed and 4 cancelled.
  Job identity, source/content/pipeline binding, attempt, status, stage and failure
  are unchanged. There is no queue execution, Apply, Reject or history deletion.
- The full Runtime is **not byte-identical**: reload refreshes input observations,
  moving revision 1446 → 1467. Changed top-level keys are only `revision`, `queues`
  and `inputRevisions`; changed job fields are `updatedAt` and `inputRevision`.
  Final Runtime SHA-256 is `a1a03b79e8ce9a5d49cac2c6ec51b7458de757fd5bfb4f50a2a55403a178479d`.

For rendered upstream UI checks, `npm run gallery:build` generated the existing
adjacent stories. Its three output files were temporarily copied into the Windows
test Vault because a WSL-source symlink is not a usable Windows plugin deployment.
The gallery was loaded without persistently enabling it. Public
`window.__gallery.show(id, {width})` calls exercised all six `Chat/Context Badges`
stories, all eleven `Settings/Miyo Connection Setup` stories, and
`Agent Mode/Agent Status Card/LongErrorRetry`, each at 300 and 600 pixels.
DOM inspection verified a connected, nonzero-size story root, the requested width
and zero root horizontal overflow. These were fake fixtures, not real connections.

Thirty-five cases passed on the first batch. `InvalidAddress` at 600 pixels failed
one `show` call with a generic `Error`; the first probe did not retain its message,
so the precise cause is unknown. Its isolated rerun mounted at exactly 600 pixels,
with zero overflow, the error alert present and the address marked invalid.
Thus all 36 combinations were rendered successfully, but this is not a clean
first-pass or a demonstrated fix for the transient gallery failure.
The `ThisComputer` fixture's Connect → Connected → Disconnect → Offline feedback
passes at both widths; `Checking` displays its status and disables both actions.
These checks do not constitute a full color/contrast audit or real Miyo connectivity
test. The exhaustive catalog audit was not run.

Gallery main SHA-256 is `3c1aa494831bdad44fb1706bf24a8cf6dbbaa00639269c2e14e3cd4c61c1f601`;
gallery styles SHA-256 is `a5537ef4d2650c8ce2c60285e726b2abaf28bc89fee5e6a5f2c20f265752f995`.
Runtime: Windows `10.0.26200`, AMD Ryzen 7 5800H, 16 logical CPUs,
68,564,348,928 bytes RAM; Electron `43.1.1`, Node `24.18.0`, Chrome `150.0.7871.114`.
This is a no-model deployment/render verification, not another model experiment.
No new model invocation, real Wiki write or Rules change was performed.

Window error/unhandled-rejection listeners captured zero events over this bounded
check. The gallery leaf was closed, the plugin unloaded, its three generated files
and empty directory removed, and its API cleared. Captured Runtime/result globals
and temporary listeners were removed; Copilot stays loaded on Studio Activity.
This checkpoint does not complete all Windows acceptance or validate historical
failed jobs; those records and the real queue remain untouched.
