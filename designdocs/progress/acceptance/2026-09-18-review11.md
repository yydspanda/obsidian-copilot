# Review #11 regression checkpoint — 2026-09-18

Task: `PK-H3-REVIEW11-FIX`.
Issue: <https://github.com/yydspanda/obsidian-copilot/issues/11>.
Status: all four review repairs verified. Full-suite gate remains red because of
seven failures in the unchanged log-redaction module; see the exception below.

## Scope and identity

- Development branch: `knowledge-h3-personal-flow`; starting commit
  `e0e88f7b7444885822c75302ca758cf0e55c0a94`. Repairs are working-tree changes.
- Frozen upstream: `61619fe427f27c23fbb08b6a040d18ae73c3b219`.
  Last fetched canonical commit remains `37bf6a120fb8e36e7a6fbb02225e40ff219b3fe8`;
  11 missing upstream commits remain a separate maintenance task. No merge here.
- Formatted source tree: 1,845 files; SHA-256
  `9ae79d470602376a692e22097ac2cc7e3294f7cffeeb8d9921ea3697276e9001`.
- Runtime: Node `v24.14.0`, Linux x64 / WSL. All new filesystem fixtures live in
  temporary directories, not in an Obsidian Vault.
- No model requests, deployment, live Obsidian interactions, commit or push.
  The previously verified Windows artifact `05903630` is unchanged.

## Repairs and regression evidence

| Review finding                                 | Repair                                                                                                                                                          | Observed regression proof                                                                                                                                                                                                                                                       |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Existing-file update escapes Vault             | Resolve the Vault and target before `Vault.process`; reject external real paths and recheck lifecycle/path after awaits.                                        | Before repair, both native file-link and parent-link cases actually overwrote the outside file. After repair, all 8 native cases pass, external bytes stay unchanged, and `Vault.process` is not called for rejected targets.                                                   |
| Case-sensitive Chat attachments disappear      | Deduplicate by the resolved exact `TFile.path`.                                                                                                                 | Both case-variant regressions fail before repair and pass afterward; 10 hook tests pass, including duplicate input and the optional Knowledge chooser.                                                                                                                          |
| Oversized registered source is fully processed | Share the production 8 MiB ceiling; fresh `adapter.stat` before read and actual returned length before detached copy/hash; no watcher retry for known oversize. | The reader's missing-budget regressions fail before repair. A valid mutation removing only the no-retry branch makes both watcher tests fail on repeated reads; restored code passes. Reader/watcher/workflow: 3 suites / 98 tests; broader startup set: 25 suites / 435 tests. |
| Unused model-selection policy                  | Delete the module and its self-only test, with no replacement abstraction.                                                                                      | Repository search finds no remaining source references. The three active selection-policy suites pass all 37 tests.                                                                                                                                                             |

The first watcher test attempt failed because its fixture omitted a deterministic
capture ID; this is not counted as an intended regression failure. The first P1
compatibility batch had a fixture type error while its other 322 tests passed;
that type error was repaired and the native suite subsequently passed. Neither
intermediate batch is described as a successful complete run.

Independent read-only reviews found no further concrete defects in the final
containment and source-budget changes. Production diff: 6 files, +84 / -90 lines.
Only one upstream-owned production file changes: the Chat hook restores the
same exact-path identity used by current upstream. The other production edits
are fork-owned. No prompts, releases, dependencies or build policy changed.

## Final verification

- `npm run format`: pass; only intended source files differ from HEAD.
- `npm run lint`: pass, with 0 errors and 3 existing warnings (two test-hook naming
  warnings and the SettingsPage declarative-settings warning).
- `COPILOT_PERSONAL_MAX_BUNDLE_BYTES=10000000 npm run build`: pass, including
  TypeScript. Default upstream size ceiling is unchanged.
- Bundle: 6,429,711 bytes; SHA-256
  `b1908b12afa05e9a2192697325661beac3ee2eb9ec2a6a4a8fd9a2d35b5ce524`.
- `node --check main.js` and `node scripts/mobile-load-smoke.cjs`: pass.
- `npm run review:obsidian`: pass, including 0 dependency vulnerabilities and the
  expected negative review fixtures. Existing deprecation/CSS warnings remain
  visible; no rules are suppressed. The first sandboxed run failed at npm audit
  with a proxy `connect EPERM`; the approved non-sandboxed full rerun passes.
- `npm run progress:check` and `git diff --check`: pass, including the final archive update.
- Full non-paid suite: **not green** — 722 passed / 1 failed suite; 10,490 passed,
  7 failed and 2 existing skipped tests, 723 suites / 10,499 tests total, 622.759 s.
  All 10 directly relevant suites / 175 tests pass in this same run. Command:
  `npm test -- --runInBand '--testPathIgnorePatterns=/node_modules/|/integration_tests/' --json --outputFile=/tmp/copilot-review11-final-full.json`.
  Real-provider integration tests are excluded. Local fake-server tests require
  approved execution outside the loopback-restricting sandbox.
- JSON result SHA-256:
  `e56e2413f17f872035538e533084b339673a8bbd73b7fa9a7c389891beead0b4`.
  Source tree hashes before and after the run are identical to the identity above.

### Full-suite exception

All seven failures are in `src/utils/redactLog.test.ts`: one long Basic credential
and six 16 MiB secret cases throw `RangeError: Maximum call stack size exceeded`.
The stack points to the email candidate scanner's `RegExp.exec` inside
`replaceEachRun`, not the six secret-rule quantifiers repaired in the earlier
acceptance task. Neither this module nor its test has a working-tree difference
from the starting commit. This checkpoint preserves the failed full run; the
earlier acceptance's passing run is historical evidence, not proof that this
remaining failure is resolved. Log-redaction changes are outside review #11 and
are not silently folded into these four repairs.

The one isolated diagnostic run subsequently produced a completed JSON result
at `/tmp/copilot-review11-redact-isolated.json`: all 37 tests pass. This does not
replace the failed full run or establish that the full-context failure is fixed.
The implementation has no imports and the test imports only this unchanged
module; none of the four repaired modules enters that call chain. The exact
execution-context condition behind the differing results remains unconfirmed.

## Boundaries

- Native tests ran on Linux. Windows path semantics were checked independently,
  but this is not a new Windows physical junction or UI acceptance run.
- The realpath check prevents an already-external symlink/junction update; it is
  not atomic protection against another process swapping paths after the check.
- If a source grows after stat, Obsidian may still allocate its returned buffer.
  The second check prevents the next copy/hash/parser stage and watcher reread;
  it is not a streaming or atomic bounded-read API.
- Simulated mobile module load does not certify Knowledge write support on mobile.
- Upstream drift is still above the configured threshold; that gate is not green.
