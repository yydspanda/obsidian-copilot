# Development Session TODO

## Session Goal

以 Windows 上的 Obsidian Copilot 为唯一首期平台，设计并分阶段实现一套长期个人使用、体验优先的知识操作系统；把助手、笔记和知识引擎连接为可摄入、可审核、可引用、可持续维护的完整闭环。

## Completed Tasks ✅

- [x] 阅读并拆解 SOC Agent 原始方案及其产品决策框架。
- [x] 核对当前仓库的消息、上下文、工具、搜索、记忆、项目隔离和持久化架构。
- [x] 根据用户澄清，将目标从“Copilot 项目总架构”修正为“基于 Copilot 的个人知识库方案”。
- [x] 区分当前已实现能力与 ACP、Planner、MCP 等设计中能力。
- [x] 编写个人知识库产品与技术方案文档。
- [x] 补充原方案方法的保留、改写和舍弃清单。
- [x] 检查文档与代码、现有设计文档之间的一致性。
- [x] 阅读 Karpathy LLM Wiki 原文并提炼 Raw / Wiki / Schema、Ingest / Query / Lint、index / log 模式。
- [x] 核对 Google Cloud OKF v0.1 官方公告、规范、合规规则和非目标。
- [x] 将 LLM Wiki 与 OKF 的采用方式合并进个人知识库主方案。
- [x] 调研 GitHub 上活跃的 LLM Wiki、OKF、个人知识库、Agent Memory 和时序知识图谱项目。
- [x] 对比其技术栈、知识工作流、许可证和与 Obsidian Copilot 的适配程度。
- [x] 形成可采用、延后采用和明确不采用的工程结论。
- [x] 盘点当前项目可直接复用的 Chat 文件拖入、处理进度、ApplyView、Search v3、graph boost、工具和 popout-window 能力。
- [x] 对 `llm-wiki-compiler`、OKF、`llm_wiki`、`claude-obsidian` 和 Graphiti 完成 commit 锁定、许可证与文件级代码审计。
- [x] 建立外部实现的 Copy / Port / Reference 复用分类，并记录上游需要修复的缺陷。
- [x] 编写体验优先的个人知识操作系统 PRD，定义 Knowledge Studio、Golden Flow、验收标准与本地成功指标。
- [x] 将主技术方案路线图改为端到端体验切片，并补充 manifest、pipeline fingerprint、持久队列、事务和可选时态投影。
- [x] 将首期平台范围收敛为 Windows Obsidian Desktop，移除 macOS、Linux、iOS、Android 和浏览器适配承诺。
- [x] 编写 Windows-only commit-by-commit 执行计划，并建立 Now / Next / Later 退出门槛。
- [x] 建立第三方来源声明与许可证落盘规则；五个候选当前明确标记为 audited/reference、未复制源码。
- [x] 完成 Slice 1A 纯 TypeScript 契约、strict runtime schema、语义 validator 与公共导出边界。
- [x] 完成原始字节 SHA-256、pipeline fingerprint、稳定 source identity、Windows Vault path 和输出存在性 freshness 判定。
- [x] 建立 Markdown 文本、PDF locator、OKF round-trip、Windows 路径/碰撞、文件占用和中文/emoji fixture。
- [x] 完成 Source Manifest Repository 与可注入 Storage Port；支持 stable identity、rename、freshness、扩展字段保真与 revision CAS。
- [x] 修复并发 success/failure 覆盖竞态，以单调观察时间和有界 CAS retry 保留较新状态。
- [x] 完成持久 Ingest Queue Core：严格快照、revision CAS、Bundle 隔离、claim ownership、同来源去重、durable source high-watermark 与 exactly-one latest rerun。
- [x] 完成 Queue 纯 core 的暂停/恢复/取消、安全启动状态转换、显式审核、指数退避、最大重试、provider 限流暂停、错误脱敏与同会话基础设施失败恢复；不等同于真实 startup coordinator 已接通。
- [x] 将 applying 设为事务安全边界；Commit E 日志完成前，中断或失败 apply 进入不可绕过的 recovery-required gate。
- [x] 完成 accepted ChangeSet preflight、Vault-global pre-state journal、Windows 确定性写入、单文件原子 CAS、commit marker 与纯 transaction core 的幂等 startup roll-forward；真实启动编排仍是 pending。
- [x] 完成 Queue v2 exact apply/commit marker、v1 严格迁移与提交协调器；Commit G 再严格迁移到 v3，并加入 pending Review Store anchor、accepted terminal identity 与 durable rejection tombstone。
- [x] 完成 provider-neutral 两阶段 Knowledge Compiler Core：strict unknown output、可信 evidence 映射、caller-owned target authorization、Windows exact observation、opaque target binding、runtime hash/id/status、确定性 candidate validation 与 fake-model 回归。
- [x] 从 legacy ApplyView 抽取无写入能力的精确 diff renderer，并修复普通 rerender、popout migration 与定时器的 document/window 归属。
- [x] 完成 strict durable Review Store：完整 proposal、canonical digest、queue job claim、record revision、accepted/rejected payload、CAS 竞争与幂等重放。
- [x] 完成 Review Store → Queue 的 0→1 hand-off：Queue 只有收到同 Bundle、同 exact job claim 的 durable pending record 才进入 `awaiting_review`；接受/拒绝必须匹配该 proposal digest 与 terminal revision 1。
- [x] 增加 `reconcilePendingReview`，可在 proposal 已落盘但 Queue 尚未完成 hand-off 的崩溃窗口后，把同一 attempt 恢复为 awaiting-review；v1/v2 `legacy_unverified` anchor 只能由真实 pending record 升级。
- [x] 完成 opaque multi-file review decision Core：逐文件/逐块选择只传 opaque id，Core 精确重组文本、重新 hash、重新验证；delete 在授权 read-set journal 化前 reject-only。
- [x] 完成 durable Queue review rejection：Queue v3 保存 exact job/changeSet rejection identity，拒绝不构造空 accepted ChangeSet，并原子提升 retained latest rerun。
- [x] 完成 Activity 只读投影、Review/Activity React 面板、reload-only Controller，以及 popout-safe Windows Knowledge Studio ItemView。
- [x] 加入 Windows Desktop 功能级 guard、命令/导航入口与用户文档；保留现有插件 `isDesktopOnly: false`，不扩大 Knowledge Studio 的平台承诺。
- [x] 完成 `knowledge-runtime-v1.json` 持久化基础代码：单一 strict envelope 承载 Queue、Review、Manifest、Vault-global Transaction slot 与 watcher-capture `inputRevision`，所有替换经过完整 envelope 语义校验和 revision CAS。
- [x] 完成 Windows 文件 adapter 的 create/update 代码层：首次 runtime 以完整临时文件 + flush + 排他 hard-link 发布，Wiki create 使用排他创建，update 使用 `Vault.process`，能力声明绑定 store 并在 preflight 前禁用 delete；该代码层阶段尚未完成真实 Windows Obsidian 验收与 Golden Flow 接线，后续 G.2z-d 已补上 bounded fresh create/update、外部编辑和 no-journal Continue 实机证据。
- [x] 完成 Review Store 启动协调器 Core：pending 审核可恢复 exact Queue anchor，rejected 审核可恢复其 pending predecessor 后收敛为 durable rejection，重复启动零 Queue revision 增长；accepted 只输出 identity-only runtime classification 输入，不自动调用模型、开始 apply 或写 Wiki。协调器以有界 revision 重读取得稳定 Review snapshot，并把 observed revision 返回给更高层继续复证。
- [x] 完成 G.2b durable Manifest plan/intent：Compiler 冻结完整 primary-source page projection、ownership/authorization、Manifest revision/digest 与 source hash/pipeline/input revision；Review v2 只能从 immutable plan 投影 accepted filter/rewrite，transaction journal v3 通过 plan digest 持久回链最终 intent。
- [x] 完成 exact atomic Manifest/apply ledger adapter：首次 prepared journal 在 shared envelope 内预留 Manifest read-set 与单调 source revision，页面 committed 后 Manifest success 与 ledger 同 transform 发布；exact replay 不改字节，同 transaction id 不同 identity fail closed。
- [x] 收紧 Runtime v2 authority：普通 Manifest storage 不能创建/修改 `lastSuccessful` 或 reserved commit metadata，active transaction 锁住通用 Manifest CAS；shared page update 原子传播所有 co-owner hash，unsafe runtime-v1 状态保留原字节并 typed fail closed。
- [x] 完成 Runtime v2 启动交叉校验：Manifest success 与 reserved metadata 必须双向存在并指向该 source 最新 ledger；source/hash/pipeline/input/intent/ChangeSet/time、Manifest revision/digest、连续 ledger 链与 Windows shared-page co-owner projection 任一撕裂均在启动时 fail closed。
- [x] 在 prepared journal 发布前拒绝复用已入 ledger 的 transaction id，并把 durable transaction replacement 限定为 immutable payload 下的合法 prepared → applying → committed/recovery 状态迁移。
- [x] 完成 Queue v4 与全写入周期原子授权复证：accepted apply claim 持久化最终 Manifest intent digest；直接 apply 在任何 target observation 前先复证完整 Queue/Review/allocator/Manifest read-set/source-order authority，prepared 发布、prepared → applying、启动读取 unfinished journal、每次共享 envelope mutation 与最终 Manifest+ledger callback 都重复或保留该证明；旧 v3 或 stale Manifest 在任何新 Wiki 文件访问前 fail closed。
- [x] 完成 no-journal 显式恢复 Core：Queue v5 保存 exact abandonment tombstone，Runtime 从同一 atomic envelope 分类 accepted-not-started/active/blocked/finalizing/committed/abandoned，并以 opaque recovery id 在 continue 前重证完整 Manifest authority，或在无任何 journal/commit/ledger 写入证据时原子 abandon；崩溃重放与 prepared-publication 竞争均收敛且不产生 split-brain。
- [x] 完成 layout-ready startup gate Core：严格按 Queue startup recovery → active transaction/commit marker 收敛 → Review hand-off 收敛 → Runtime 单-envelope accepted 批量分类执行；首次 sticky file conflict 只接受同 transaction 的 blocked 重读，Review revision 变化有界重跑。Gate 保留 Runtime/Queue revision 高水位、精确校验 accepted action↔classification 集合，并从最终 atomic snapshot 观察 Vault-global transaction；返回的 `observed_clear` 仍只是乐观展示结果，不会自动 resume、begin accepted apply、continue、abandon 或调用模型。
- [x] 完成 shared-envelope conditional startup release Core：`observed_clear` 只生成 Runtime/Review/Queue revision 乐观令牌，Runtime 在同一次原子 transform 中重证 Vault-global transaction、跨 Bundle 写恢复证据、当前 marker/claim/applying 状态与全部 accepted terminal classification，成功时只把 exact `startup_recovery` Queue 改为 running 并提升 Queue/Runtime revision。普通 `resume()`、`pause → resume` 与两步 generic Queue CAS 都不能清除或洗白 `startup_recovery`/`recovery_required`/`commit_pending_ack` 单调恢复门；同门写入必须保留当前 claim/job/marker，accepted-not-started anchor 在 running/startup 状态均受保护，跨门写入必须等于 accepted begin、startup recovery、current committed journal resolution 或 marker finalization 的精确投影，不能拿历史 ledger 替换当前恢复证据。`applyAbandonments` 对通用 Queue writer 永远只读，只能由显式 Runtime abandon 原子追加；source high-watermark 不可删除、回退或在同 revision 改写，任何推进（含首写）都不得超过共享 allocator，同时保留合法 A→B→A watcher 收敛。commit-then-throw 不猜测成功，旧令牌失效后必须重跑 Gate。`accepted_not_started` 可由显式 exact acceptance 在 running 或保持 startup pause 的情况下建立 apply claim，崩溃则升级为 no-journal recovery。
- [x] 完成 Runtime v3 durable source-observation hand-off Core：watcher 先以全局永久唯一且重试稳定的 `captureId` 幂等分配 opaque token/revision，再以 first-write-wins 绑定 exact source hash/pipeline；Queue high-watermark 只有通过 adapter-private token capability 匹配 bound record 才能推进，并在同一 shared-envelope transform 中把观察标为 consumed。更高但未绑定的 allocation 不会淘汰较低 bound read；更高 bound read 会原子 supersede 较低未终结观察。窄 `SourceObservationHandoff` facade 统一 bind/enqueue/settle，可收敛 bind/Queue commit-then-throw；历史 consumed capture 重放直接返回自身 durable receipt，不调用 Queue 或借用最新 job。Runtime 可按 Bundle 枚举 pending allocated/bound work 供重启恢复，任一 pending allocated/bound observation 都阻止 startup release。v2 迁移只建立 legacy fence/checkpoint，不伪造历史 token；每次 parse 还拒绝 HWM 回退、伪 supersession 和其他 API 不可达 journal。
- [x] 完成 G.2h 插件级 fail-closed 启动前置接线：Project frontmatter 无损保存 untrusted Bundle 值，layout-ready 后从已完成初始化的 Projects 严格解析全部 Bundle，并按 Windows 大小写不敏感边界做跨 Bundle 冲突校验；普通 Projects 独立于 Knowledge Runtime 启动。稳定 Delegating Studio port、动态 Session Store 与启动 Barrier 在缺失/非法/多 Bundle 未选择时不制造占位 Bundle，唯一真实 Bundle 可驱动已恢复 View 切换；有效配置仍停在 workflow-adapters-unavailable，绝不调用 Gate/Release/Queue/model/watcher。ProjectRegister 初始化已 single-flight、成功幂等、失败可重试，cleanup 永久失效；插件卸载在首个 `await` 前同步关闭 Barrier/Session/Port/Project 监听。
- [x] 完成 G.2i Projects lifecycle ownership：ProjectManager、ProjectFileManager、ProjectContextCache、FileCache、ProjectLoadTracker、VaultDataManager 与 project-mode FileParserManager 显式捕获 owning App/Vault，插件热重载或跨 Vault 时同步退役旧 Manager/订阅/监听/内存 cache；Project state 使用 opaque owner，pending write 使用 exact lease，所有初始化/重载/目录扫描共享 prepared-scan CAS，目录代次同时约束 Vault events 与 CRUD continuation，旧 scan、旧操作、旧 cleanup 与同路径 ABA 均不能提交到新 snapshot。新插件先退役旧 ProjectManager 再建立 Project state owner，卸载则在首个 await 前解除 Manager/Project/Vault-data owner，避免状态清空触发旧 autosave或旧 cleanup 拆除新监听；Runtime 动态模块加载受启动代次约束，log flush 绑定显式 Vault。并发 project switch 只有最新请求可发布 selection/UI 结果，ChainManager settings subscription 随 owner 释放。
- [x] 完成 G.2j 独立 Vault source watcher adapter：严格 opaque watch plan、exact App/Vault/adapter owner、同步 capture authority/id、durable allocate-before-read、captured-path exact binary SHA-256、handoff-only commit、安装期 handoff capability 快照、注入端口 data-only success payload 复证、allocate/commit frozen request 有界重试、captured-path read 有界重试、跨 lifecycle/watch-plan continuation fail-close 与同路径多 Bundle 隔离。当前 lifecycle 内观察到的 rename/delete/folder 破坏事件会同步使受影响 source 的后续阶段失权并 quarantine；G.2q 已把启动缺失、大小写漂移与 Windows-key collision 也升级为同代 authoritative blocker。重启 full crawl 只复证当前文件状态，不持久保存停机期间 destructive-event receipt。replace/close 不能取消已经发起的 adapter operation（包括 commit），但其返回后不能进入下一 read/commit 阶段或发布旧通知。配套 exact-byte reader 在未来 executor 的 parser/model 前复证 Queue job hash，并返回 parser 必须消费的同一 bytes。只读 loader/byte parser 已由 G.2l 补齐；production Release/worker composition 仍保持 unavailable。
- [x] 完成 G.2k opaque watch-plan authority builder：从 strict Bundle/Manifest detached snapshot、exact schema bytes 与 caller-projected pipeline behavior profile 复证 source/write boundary，按通用 Windows suffix 无歧义选择 parser。对传入快照，per-source fingerprint 绑定 exact Bundle-config digest、compiler version/configuration、selected parser identity/configuration、schema raw-byte hash、projected model behavior、output language、OKF 与 citation contract；plan 另保留 Manifest revision/digest、submitted-profile digest 与总 digest。Core 只防御性拒绝常见 credential-like 字段，production adapter 仍必须执行显式 allowlist。watcher 构造/替换已删除 raw source array 入口，只接受 module-private token + WeakMap state 认证的 frozen plan；同物理 source 可服务不同 Bundle，空 Manifest Bundle 也要求可调用的 durable handoff。G.2l 已补上当前 durable snapshot 的只读装配与 parser 前后复证；插件启动组装保持 unavailable。
- [x] 完成 G.2l 只读 production-facing workflow-plan Core 与 byte-only parser boundary：通过注入端口从 Manifest facade、exact App/Vault reader、Projects/settings generation 和显式 model allowlist 双采集全部 Bundle；两次 authority digest 不同即整体失败。G.2q-b/c 已把这些端口组装并接入 plugin-owned observation session；parser/model/worker 执行仍未组成可用插件 workflow。Profile Source 精确匹配 `name|provider`，禁止 fallback，只保留版本化 projection-only 行为和 endpoint/routing 哈希摘要，API key、完整 settings、未知模型字段及 credential-like compiler/parser 配置均不进入 profile。Watch plan 为每个 source 持久绑定 config-free parser profile digest，空 Manifest 也复证完整 parser registry。opaque execution binding 在 parser 前后重复复证 Manifest revision/digest 与 schema raw bytes，以 fatal UTF-8、BOM/CRLF 不归一和 byte round-trip 桥接 Compiler schema；首个 UTF-8 text parser 只消费 `readExpected` 返回的同一 bytes，限制字节/字符并在边界检查点检测净 mutation。Loader/Profile/Execution state 均由 WeakMap 隔离且 class/prototype/instance frozen。Queue worker、模型、Review 或 Wiki write 仍未接入；parse job 尚未由不可伪造的 durable Queue/observation capability 认证，不能视为 compiler-ready executor。
- [x] 完成 G.2m 模型执行前双重 capability Core：Queue 为 exact controller/attempt/startedAt 与 Queue-owned `AbortSignal` 签发 process-local revocable claim，并把 claim 绑定到 exact QueueStorage；Runtime 从同一 atomic envelope 证明 unique exact processing job、consumed observation 与 Manifest，legacy checkpoint/high-watermark 不能冒充 observation。opaque execution owner 排他绑定单一 Runtime 与单一 workflow loader generation，相同数据或复用同一个 owner 也不能跨 Runtime/plan 拼接。authorized preparation 在任何 source read 前匹配 Runtime↔workflow Manifest，并以 Runtime → Manifest/schema/profile → Runtime sandwich 在 parser/model 前后重复复证，普通 job DTO、`unbound_read_only` marker、spread/prototype forgery 均不能升级权限。每份 preparation 只允许创建一个 adapter；Compiler 为每次 `compile()` 签发独立 opaque session，并为 exact analysis/generation request 签发一次性 call authority；adapter 在 analysis pin session，generation 必须来自同一 session，跨 Compiler、跨 compile 与重放均 fail closed。Knowledge 私有 route 用 WeakMap 隐藏 credential/transport closure并完整匹配 provider/model/projected configuration digest，job-bound adapter 只用 exact Queue signal、每阶段只发起一次 outer route call、调用前后复证，并只接收有界 JSON text 后在私有边界解码冻结。profile 强制声明 non-streaming/no-fallback；具体 transport 尚未实现，后续仍须独立审计其内部无 streaming/fallback/retry。fresh adapter 在首次调用前仍是可信组装 capability；production composer 必须立即交给预期 Compiler，若未来会暴露该对象则须先增加 composer-issued pre-bind owner。本阶段仍不接 `main.ts`、后台 worker、真实 DeepSeek/其他 provider HTTP、prompt encoder、Review 或 Wiki write，也未新增/修改 AI prompt 内容。
- [x] 完成 G.2n 确定性 Knowledge Prompt Encoder 与 DeepSeek 私有 Transport：用户已明确授权本独立提交新增 Knowledge 编译 prompt。两阶段均固定为 system + canonical INPUT_JSON 两条消息；schema 只作为受 system 权限约束的 Wiki 组织/内容规则，evidence/context/currentContent/outputLanguage 等不可信字符串只进入 JSON 数据区。prompt 合约含 strict 输出 shape、最小 JSON 示例、字符/UTF-8 预算及覆盖实际两阶段消息的 identity；prompt identity 与 DeepSeek route identity 均进入 pipeline profile/fingerprint，concrete route 再绑定完整 profile digest。DeepSeek 首期只允许官方 `https://api.deepseek.com/chat/completions`、当前 `deepseek-v4-flash` / `deepseek-v4-pro`，用注入的 native-fetch-compatible port 透传 exact Queue signal，强制 `stream:false`、JSON Object、exact model/max_tokens、单次 POST、无 fallback/内部 retry/repair。响应从首字节流式累计，8 MiB 前置 cap 后 fatal UTF-8 + strict provider envelope；仅 `stop`、单 choice、exact model、assistant non-empty content 与一致且不超限的 usage 可进入既有 adapter 的第二道 32 MB/strict Core schema。credential 只留在 closure/header；custom endpoint、旧模型、未知/已废弃配置、redirect/SSE/length/tool call/429/5xx 全 fail closed。模块已导出但未接 `main.ts`、settings/key resolver、worker、Review 或 Wiki write，Studio 继续 unavailable、默认零网络。
- [x] 完成 G.2o DeepSeek V4 迁移、生产零网络预检与脱敏失败策略：direct DeepSeek catalog 只保留 reviewed V4 Flash/Pro 为可选 identity，旧 Chat/Reasoner 保留为 disabled retired marker 且 default/Quick Ask/Command/Project 不得静默 fallback；thinking 与 sampling 配置由 provider-specific policy 在请求前互斥校验。插件在 Runtime/Projects/Bundle strict validation 后，用同代 Projects、已 hydrate settings、固定 compiler/parser/prompt/route profile 和主 renderer native fetch 逐一构造并立即丢弃 private route，仅保留脱敏的同步 preflight 结果、绝不 invoke；settings/Projects 变化或卸载同步 close 当前结果 generation。Transport → Adapter → Compiler → Queue 的错误只经 module-private brand 和固定 code policy 投影，真实 429 才能进入 durable rate-limit pause，500/503/network 由 Queue 有界重试，鉴权/余额/request/response/未知 HTTP 终止，transport 无内部 retry。有效预检仍停在 `workflow_adapters_unavailable`，没有启动 Gate、Queue、watcher、model、Review 或 Wiki write。
- [x] 完成 G.2p plugin-owned preflight/recovery lifecycle 与 recovery-only 生产接线：preflight candidate 构造期和同步 `preflight()` 期的 Settings/Projects 重入均以 generation 复证关闭；exact admission 冻结 strict Bundle owners，替换、取消、配置/Runtime identity 变化与卸载同步关闭当前 preflight 和一次性 recovery capability。成功预检后以真实 Runtime Queue/Review/Transaction、Vault file store 和 startup Gate 做 owner-first、逐 Bundle恢复，在首个 attention/blocked 停止；Gate 在 Queue recovery、apply reconciliation、Review reconciliation 和 accepted snapshot 之间复证 current generation，旧代不能在早期 await 后新进入 transaction/file access，已进入的单一 durable phase 仍完成 crash-safe 收敛。Barrier 分别发布 clear/attention/blocked/unavailable，所有外部 code/kind 经固定 allowlist 脱敏。该路径只可收敛 durable Queue/Review bookkeeping 并 roll forward 已持久化、已授权 journal；conditional Release 现在由后续 G.2r observation session 触发，仍没有 worker、watcher、model、新 transaction preparation、no-journal 自动决策或新 Wiki 生成。
- [x] 完成 G.2q-a listener-first observation startup Core：watcher 可先同步注册 listener、再恢复观察并显式 full crawl，重复 `start()` 不二次 crawl，pre-crawl plan replacement 不隐式扫描。这里的 crawl 只覆盖 opaque plan 中已经由 Manifest 注册的 sources，不从 `sourceRoots` 自动发现新文件。启动缺失、exact path 大小写漂移、Windows-key collision、capture failure 与 rename/delete 均成为当前 plan generation 的 authoritative quarantine blocker；同代 recreate 不能自行解封。reconciler 只对当前 plan/pipeline 下、经 `readExpected` 复证相同 raw hash 的 bound observation 做 exact token/hash/pipeline 重放；allocated 与 hash/path drift 不猜测旧字节，仅在更高 revision crawl capture 成功时被 supersede，残留记录在 final reconciliation 继续阻塞。协调器固定 `listener → pre-recovery → crawl → idle → final recovery → idle → blocker proof`，并在 caller generation 取消时同步关闭 watcher/reconciler；真实 Runtime 集成证明成功的 revision 3 crawl 可把旧 allocated + drifted bound 收敛为唯一 pending Queue job，整个 Core 没有 Release、`runNext`、model、Review 或 Wiki port。Runtime conditional Release 同时原子阻断 allocated/bound residual work。该 Core 已由同代 startup observation session 激活，Studio 继续 unavailable。
- [x] 完成 G.2q-b 同代 production observation 内部组装：production preflight 用同一次 Projects/settings/resources snapshot 构造并复用 exact `ProjectKnowledgePipelineProfileSource`，以 module-authenticated、secret-free workflow lease 持有 frozen owners、owned parser capabilities 与 profile resolve authority；Settings/Projects/preflight replacement 或 close 会先清除 capability 引用，再同步通知 observation generation 撤销。`KnowledgeProductionObservationComposer` 在同一 execution owner 下组装真实 Runtime Manifest repository、exact App/Vault source/schema reader、double-collected workflow plan、Runtime observation allocator/binder 与 startup-held Queue；Queue 只把 `enqueue` capability 交给 handoff，private executor 对任何意外 claim/run fail closed。初次收敛后保留 live listener，并以同一 caller signal 支持串行 `reprove()` quiesce/reconcile/blocker proof 和同步 `assertHealthy()`；任何 stale、并发、阻塞或撤销都会关闭会话。该 composer 不执行 conditional Release、`runNext`、model、Review decision、transaction preparation 或 Wiki mutation，Studio 继续 unavailable。
- [x] 完成 G.2q-c plugin-owned observation session 接入：startup Barrier 在同代 recovery `observed_clear` 后才启动 observation port，并保留 live watcher；新的 startup generation、取消、Settings/Projects/Runtime replacement 与 unload 会同步关闭 observation。Barrier 只接受完整 convergence result，observation blocker/diagnostic 不被伪装成 release receipt；Queue 在 fresh Gate/release 前保持 held，未接 `runNext`、worker、model、Review 或 Wiki mutation。
- [x] 完成 G.2r observation → fresh Gate → conditional Release 接线：初次 observation convergence 后先以同一 signal 做 reproof/health assertion，再按配置 Bundle 顺序运行 fresh Gate，并通过 Runtime shared-envelope release 原子重证 revision、恢复证据、accepted terminal state 与 source-observation watermark；仅全部 Bundle 成功/已 running 才返回 released。stale、blocked、observation drift、generation replacement 或 unload 均 fail closed，Queue 不会被 generic resume 绕过；仍未接 worker、Compiler、model、Review decision 或 Wiki mutation。
- [x] 完成 G.2s bounded worker session Core：`KnowledgeProductionWorkerSession` 只接受 exact Queue、显式 conditional-release proof 与同代 assertion；每次 `runOnce()` 最多按稳定 Bundle 顺序调用一次 `runNext`，拒绝未 release、并发、stale、代际失效与重复调度。Observation composer 现在可在 convergence/health proof 后一次性绑定显式 executor；未绑定前 Queue executor 仍 fail-closed。该 Core 不持有 model、credential、Compiler、Review 或 Wiki capability，`main.ts` 尚未提供真实 executor。
- [x] 完成 G.2t production preparation executor bridge：observation composer 保留与 watcher 相同的 authenticated execution plan，并从同一个 Runtime QueueStorage 构造 proof port；worker 只能经 `Queue claim → Runtime proof → Manifest/schema/profile reproof → exact source bytes → byte parser → authorized preparation` 到达固定捕获的 handler。结构相似 plan/proof、handler accessor、signal mismatch 与旧代 authority 均 fail closed；该桥自身不持有 target、model、Review 或 Wiki mutation capability。
- [x] 完成 G.2u preflight-owned model route lease 与首个 production target resolver：零网络预检不再丢弃已验证的 DeepSeek route，而是按 Bundle 保存在 close-incapable opaque lease 中；Settings/Projects/replacement/unload 会先清空 route closure，再撤销 workflow。Lease 只提供“authentic preparation + exact input + captured ports → 内部立即构造并运行 Compiler”，不暴露 API key、settings、route 或 model adapter。renderer native fetch 固定捕获函数与 receiver，后续属性替换不能看到 Authorization header。`ObsidianKnowledgeCompilerTargetResolver` 以 exact App/Vault/adapter owner、Windows case-insensitive loaded index 和 canonical path 做 create-only existence probe 或 authorized exact read；collision、index/stat/read drift、owner replacement、accessor 与取消均脱敏 fail closed。
- [x] 完成 G.2v branded production compile-attempt/input Core：Queue claim 现在额外身份绑定 exact `job + signal + reportStage`，复制 job、替换 signal/reporter 或 claim revoke 后均不能组成执行上下文。Route lease 每代只能一次捕获 generation-owned target/validator ports 并签发唯一 builder；builder 在占用 preparation 前匹配 exact route/profile，只从 authentic preparation 与 exact Queue context 派生 evidence、Manifest target authorization、Queue `createdAt` 和空的未授权 context read-set，并一次性保留 opaque attempt。Lease 不再接受裸 `KnowledgeCompileInput`、preparation 或每次调用 ports，attempt 也不能跨 lease、并发重建或重复消费。首期 evidence 对 text/Markdown 使用完整材料、对 PDF 使用逐页完整非空材料；现有目标固定 write-only grounded，delete/context retrieval 继续 fail closed。Compiler 对 resolver/validator 成功 payload 增加有界 descriptor-only snapshot，accessor/Proxy/cycle/sparse/超限值不能触发 getter、TOCTOU 或泄漏 raw cause。该 Core 尚未注入 production preparation handler，仍不创建 worker 或发起网络。
- [x] 完成 G.2w production candidate validation 与 compile → durable Review hand-off：首版 validator 对完整 candidate 做有界 descriptor-only snapshot，重证 Bundle/ChangeSet/schema/context/target hash，以 parser-owned artifact triple + locator material 校验 citation，并把生成 Markdown 严格解析为 OKF concept/index/log；在 projected link resolver 与 compare-delete 就绪前，任意链接和 delete 均 fail closed。generation-owned handler 只消费唯一 route builder，执行 authentic preparation → compile，并把 proposal/plan/digest/exact six-field Queue claim 先保存到同 Runtime composition 下的 Review Repository，再只投影 durable pending receipt 给 Queue；controlled failure、provider infrastructure、Review conflict 和未知依赖均固定脱敏分类，`no_changes` 只返回稳定 identity。独立真实生产组合测试已从 Runtime observation/Queue claim 经过 workflow reproof、UTF-8 parser、DeepSeek 两阶段、target resolver 和 candidate validator 到达同一 Runtime 的 `awaiting_review`，且不提供 Wiki write capability、不改变 Manifest；该阶段尚未接入 `main.ts`，后续 G.2x 已关闭 worker、Runtime owner 与 exact cancellation 三个接线门槛。
- [x] 完成 G.2x production worker 主链路接线：Runtime-backed Review storage 绑定不可跨 Runtime/代次拼接的 opaque execution owner，handler 在任何模型调用前复证 preparation 与 Review owner；observation composer 内部创建同 Runtime Review Repository、compile handler 与 bounded worker session，`main.ts` 仅在 fresh Gate/conditional release 成功后安装事件驱动 controller。Controller 持续 drain 已释放 Queue、按 durable retry time 使用 owning renderer timer 调度并合并通知。Settings/Projects 变化、generation replacement 与 unload 同步关闭 exact Queue controller；非 applying attempt 的 Queue-owned signal 立即中止并把同 attempt 恢复到 `startup_recovery`，已进入 applying 的事务保持 crash-safe 保护。worker 只执行 exact bytes → UTF-8 parser → DeepSeek analysis/generation → validator → durable Review → `awaiting_review`，自身始终不持有 Review decision、Wiki apply、Query 或恢复决策权限；G.2z-c/d 通过独立窄协调器接通 Activity、Review Reject、显式 reviewed create/update Apply 与 no-journal Recovery，H.1 再通过分离的 read-only coordinator 接通 scoped Query 与 Markdown citation jump。
- [x] 完成 G.2z-a/b Studio durable 只读接线：Queue + Review 从同一 Runtime envelope 原子投影，Wiki target 观察后复读 outer revision 并有界重试；只有 exact durable anchor/job claim/digest/revision/timestamp 一致的 pending Review 可见。Runtime commit 与 Wiki-root Vault 事件仅触发 reload，Review plan 深冻结；所有 mutation capability 继续为 false。live delegate 在 worker release 后才安装，并由 revocable generation lease 绑定 Composer 失效，关闭/替换会同步撤下 delegate、解绑 Runtime/Vault listener。生产边界只传冻结的 Runtime read facade，不持有 Queue/Review/transaction 写能力。

## Pending Tasks 📋

- [x] G.2y Windows Obsidian 实机验收：实机锁定 Electron renderer 将 `import("node:...")` 当作浏览器动态模块请求并抛 `TypeError` 的根因；Runtime 与 Wiki 排他创建现共用经成员校验、操作时才调用的惰性 `window.require` 边界，非桌面插件加载不提前求值 Node 模块。生产包部署到独立 Windows Vault 后，Runtime v3 首次排他发布、后续 `DataAdapter.process`、Manifest CAS、source crawl、exact-byte parser、DeepSeek analysis/generation、candidate validation、durable Review/Queue hand-off 全部通过；明确 OKF frontmatter/无链接 Schema 后恰好两次 HTTP 200 到达 `awaiting_review`，anchor/record/claim/digest/ChangeSet 全部一致，重载零模型请求并恢复同一 pending Review。受控零网络 pending fetch 在 plugin disable 时收到 exact abort，processing job 原子恢复为 `pending/queued` 与 `startup_recovery`，重新启用后 attempt 2 正常收敛；全程 Wiki 文件数量/基线字节不变、目标 Manifest 摘要不变、无 active transaction、Obsidian 无开发者错误。临时 fetch 探针已移除，Copilot 已恢复启用。

### Slice 1A：契约与来源归属 ✅

- [x] 建立 `THIRD_PARTY_NOTICES.md` 和第三方许可证目录规则；实际 Copy 时再同步加入许可证原文与文件映射。
- [x] 建立 Golden Flow 的 Markdown、PDF locator、OKF round-trip、CRLF、盘符/反斜杠、大小写碰撞、保留设备名、文件占用和中文路径 fixture。
- [x] 实现纯 TypeScript `KnowledgeBundleConfig`、`SourceManifestEntry`、`SourceLocator`、`ClaimCitation`、`KnowledgeIngestJob` 和 discriminated `KnowledgeFileChange`。
- [x] 实现 source byte hash、pipeline fingerprint、稳定 source identity、Windows path contract 和输出存在性校验。

### Slice 1B：持久队列与可恢复写入

- [x] 实现可注入的 `QueueStorage`、`IngestExecutor`、`EventSink`、`RetryPolicy` 和队列状态机。
- [x] 支持任务去重、处理中 latest rerun、暂停、取消、指数退避、重试、stale claim 防护和 Queue 纯 core 的安全启动状态恢复。
- [x] 实现 Windows/Vault `QueueStorage` adapter 的 revision compare-and-replace 代码层，以及跨 runtime 重建、并发安全的 per-source watcher-capture `inputRevision` 分配器；真实 Windows `DataAdapter.process` 行为仍由独立实机验收项负责。
- [x] 实现专用 durable observation hand-off、Runtime v3 observation journal、token-capability exact Queue 消费、pending work 重启枚举与窄生产 facade；不再以 scalar allocator 上界授权任意 Queue payload，也不把历史 capture 重放错误关联到较新 Queue job。
- [x] 实现独立 `ObsidianVaultSourceWatcher` production adapter：只接受 opaque `KnowledgeSourceWatchPlan`，绑定 exact App/Vault/adapter，先同步捕获 path/authority/captureId，再 durable allocate，随后用 owning Vault `DataAdapter.readBinary` 读取 exact bytes、计算 raw SHA-256 并仅经 `SourceObservationHandoff` commit；安装时复证并快照 handoff 方法，reader/allocation/settlement success 只接受 detached data payload。allocate 重试复用同一 frozen request；read 重试固定 captured path，首次通过本地复证的 bytes/hash 才成为 commit payload；commit 重试复用同一 frozen token/hash/pipeline request。旧 generation 的当前 adapter call 返回后不能启动下一 read/commit 或发布旧通知，但已发起的 call 不能被取消。同路径多 Bundle 独立观察；仅当前 lifecycle 实际观察到的 rename/delete/folder 事件会同步 quarantine 对应 source 并阻断同路径 ABA。`ObsidianExactSourceArtifactReader.readExpected` 为真实 executor 提供 parser/model 前的 job hash 复证与同一 bytes hand-off。该 adapter 已由同代 startup observation session 激活，但不会运行 worker 或开放 Studio。
- [x] 实现严格、不可旁路的 watch-plan Core builder：重新解析 detached Bundle/Manifest snapshot 并验证二者关系，以 exact schema snapshot 和 caller-projected parser/model/compiler/output/OKF/citation behavior profile 计算 fingerprint；Bundle config、compiler config 与 selected parser config 均进入身份。module-private token、WeakMap state 与 frozen class/prototype/instance 关闭 direct construction、container reflection 和 monkeypatch 旁路。Core 对常见 credential-like key 的拒绝只是纵深防御，production adapter 仍必须执行显式 allowlist。builder 只保留冻结后的 source/Bundle authority，不保留 schema bytes 或配置值。
- [x] 实现只读 production-facing snapshot loader Core、exact schema bridge、显式 allowlist profile source 与 Knowledge 专用 UTF-8 byte parser，定义并测试 Manifest facade、exact reader、Projects/settings generation 的可注入边界；G.2q-b/c 将其接入同代 startup observation session，G.2x 再接入 released compiler worker。不传完整 provider settings，也不沿用会二次按路径读取或走闭源缓存的 legacy `FileParserManager`。
- [x] 实现 Knowledge 专用 provider execution binding、model adapter、确定性 prompt encoder、首个 DeepSeek private transport、生产 profile/credential/native-fetch 零网络 preflight 与脱敏 failure projection：module-private Queue/Runtime/observation capability 重新绑定 source identity，不信任 `unbound_read_only` marker 或同型 job DTO；私有 route/credential closure 匹配 endpoint/routing/model configuration，DeepSeek concrete route 再绑定完整 pipeline profile。禁止回读 settings、复用 ChatModelManager/LangChain fallback、`safeFetch/requestUrl` 或依赖可变 provider defaults。workflow plan 在 source/parser/model 边界稳定复读 Manifest、schema 与 Projects/settings profile，Runtime proof 来自同一 atomic envelope；exact execution owner 与 per-compile opaque session 阻断跨 Runtime/plan/compiler 阶段拼接。G.2x 的 production composer 在 fresh adapter 暴露前把它直接交给预期 Compiler，并以同一代次覆盖 App/Vault/parser/Queue/Review lifecycle。
- [x] 完成 listener-before-crawl、allocated/bound observation recovery、重启后当前状态 revalidation 与权威 full crawl 的独立 Core；没有 durable destructive-event receipt，结果只声明 observation converged，不携带 Queue release authority；现已由插件同代 observation session 持有生命周期。
- [x] 以同代 secret-free workflow lease 和 observation-only production composer 组装 Manifest/schema/profile loader、startup-held enqueue-only Queue 与 G.2q observation Core；支持同步撤销、初次收敛后的 repeatable reproof 与 synchronous health assertion，并已由 `main.ts` 的 recovery-clear 生命周期接入。
- [x] 将 G.2v branded compile-attempt builder、production candidate validator、同 Runtime Review hand-off 与受控 failure/no-changes 投影注入 production preparation handler，并以真实 Runtime + DeepSeek 组合测试证明 Queue → `awaiting_review`；G.2x 已由 `main.ts` 的 plugin-owned worker composer 持有该 handler，G.2z-a/b 再接入只读 Studio。`no_changes` 的 durable source-success/Manifest 语义仍由下方独立任务约束。
- [x] 为 Runtime-backed Queue/Review/handler 增加不可跨 Runtime 拼接的 owner brand/capability；Review storage 与 authentic preparation 必须匹配 exact execution owner，错误组合会在模型调用前 fail closed。
- [x] 让 Settings/Projects/Runtime replacement 与 unload 通过 Queue-owned active controller 同步中止 exact Queue signal 和已经进入 native fetch 的请求，并定义可恢复的 durable unwind；非 applying attempt 恢复到同 attempt 的 durable `startup_recovery`，applying 保持不可中断的 crash-safe 边界。
- [x] 更新并迁移插件 DeepSeek model catalog 与 Knowledge-effective 配置：只接受 `deepseek-v4-flash` / `deepseek-v4-pro`，旧 `deepseek-chat` / `deepseek-reasoner` 不 alias、不 fallback；thinking profile 使用可审计的零温度占位且 wire 不发送 `temperature/top_p`，Minimal 明确关闭 thinking 后才使用采样。direct DeepSeek 不支持的 Frequency Penalty 与 Knowledge 未映射的 `numCtx`、Responses API、prompt caching、provider routing/custom endpoint 均在请求前拒绝。
- [ ] 在接入真实 durable adapter 并长期使用前定义 terminal job 归档/压缩策略，避免运行队列无限增长。
- [ ] 终态归档必须原子协调 Queue jobs、pending review anchors、accepted/rejected identity、Review Store terminal records、source high-watermark 与 observation terminal receipts；若删除历史，必须保留 capture replay、allocator floor 和旧输入不可复活所需的等价 tombstone，不能留下悬空记录。
- [x] 实现 before-hash CAS、完整 pre-state journal、确定性写入顺序、commit marker 和 transaction 纯 core 启动 roll-forward。
- [x] 在纯 core 协调器层验证任一断点后都不会出现“任务/manifest 已成功但 Wiki 页面未完成”的状态。
- [x] 实现 Windows/Vault `TransactionStorage` 的 Vault-global revision/token CAS slot 代码层，并以永久 envelope slot 的 `null` 清除避免删除状态文件。
- [x] 实现 `KnowledgeFileStore` 的排他 create 与 `Vault.process` update compare-and-swap 代码层；after-state replay 可识别 `already_after`，第三状态 fail closed。
- [ ] 实现可证明安全的 compare-and-delete；当前生产 capability 固定 `delete: false`，精确 before 确实需要删除时必须拒绝，不能用 read-then-delete 冒充。
- [ ] 在真实 Windows Obsidian test Vault 验证 `DataAdapter.process`/`Vault.process` 的 callback 串行化、双实例竞争、返回值、插件重载、外部编辑与崩溃行为；完成前不宣称 power-loss durability 或 production-ready CAS。
- [x] 实现 exact `transactionId/revision/changeSetDigest/intent/journal/receipt` 幂等的 `ApplyCommitManifestPort` adapter/ledger；同 identity 重放是 byte-preserving 单一逻辑成功，同 key 不同 digest fail closed。
- [x] 将完整 post-compile `generatedPages`、ownership/authorization、Manifest revision/digest 与 per-source hash/pipeline/`inputRevision` 作为 durable plan/intent 写入 Review/transaction journal，并在 Runtime prepared reservation 与最终 commit 重复复证。
- [ ] 将 schema、非 target link 与 source artifact 的 read-set 写入 journal，并在首次写入与恢复前复证；Manifest target authorization（revision、ownership、co-owner、sole-source、last-generated hash）子集已完成。完成其余 dependency 与 safe compare-delete 前真实 UI/apply 继续不启用 compiler delete。
- [x] 在 Manifest+ledger atomic callback 内重证 exact Queue apply claim、review identity 与 allocator/source high-watermark；coordinator 的早期 Queue verify 只作快速诊断，最终授权与 Manifest/ledger 同一原子 transform，exact ledger replay 继续最先返回。
- [ ] 为 outer runtime revision 预留 transaction progress、Manifest/ledger、Queue marker、journal ack/release 所需容量，避免接近 `Number.MAX_SAFE_INTEGER` 时文件 CAS 后才耗尽 envelope revision。
- [x] 定义并实现 `no_changes` 的 durable source-success/Manifest revision 语义；无文件变更不再只把 Queue terminal 冒充成功摄入。
  - [x] 以独立 no-change plan/marker 绑定完整编译证据、Manifest read-set 与 Queue claim；不伪造空 ChangeSet/Review，也不覆盖既有 accepted-page provenance。
  - [x] 在同一 Runtime outer CAS 内原子提交 Queue completed 与 Manifest revision/marker，覆盖冲突、重启、commit-then-throw、ACK-lost 与旧 attempt 的 fail-closed/幂等行为。
  - [x] Core/Runtime freshness 可验证显式首次零页面 no-change，并只从最新 exact Apply/`no_changes` proof 投影可复用结果；保留既有页面时，每个 Manifest 授权路径都必须带当前 SHA-256，production reader 再对 exact Windows path 做有界、顺序、hash-only 复证。旧 malformed 空 `lastSuccessful`、缺 hash 或无法证明的输出继续 fail closed。
  - [x] Runtime v4 作为 downgrade fence：v3 中当前可复用却没有 Manifest/ledger proof 的 completed job 会在一次原子迁移中降为 `pending/queued` 并重跑，不伪造 marker；旧二进制会拒绝 v4，重启迁移幂等。
- [x] 将 Runtime 验证的 freshness 接入 production observation → Queue admission：只有 Manifest 授权的每个生成页当前为 exact file 且 SHA-256 一致，或存在显式首次零页 `no_changes` proof，才允许 exact-input high-watermark skip。冷启动 full crawl 及已跟踪生成输出的 create/modify/delete/rename/folder 事件会重新观察原 source owner；missing、directory 或 byte drift 会创建 repair job，或为 processing/awaiting-review 工作保留唯一 repair rerun。shared page 会重查全部 owner；Apply 期间延后检查，交由强制 generation refresh 复证。authority、identity 或 I/O 无法证明时不写 Queue；有实际变更的 repair 仍须经过 Review，绝不直接写 Wiki 或自动 Apply。若模型对仍然 stale 的输出返回 `no_changes`，Queue 不提交 success marker，而是记录脱敏、不可自动重试的 `output_repair_unresolved`，折叠同输入 rerun 并阻止重启重复花费；只有一次后续 `up_to_date` observation 才解除该输入 blocker，source/pipeline 已变化的 divergent rerun 不受影响。
- [ ] 补 production-shaped 非空 Apply 组合门禁：完整输出 cold scan 必须零模型，missing/hash drift 必须恰好创建一次 repair，shared-owner Apply 交错不得制造冗余模型任务；当前 intact/missing 行为已有 Windows acceptance，hash drift、shared-owner 与 Apply 交错已有分层自动化，尚缺单一 Composer E2E fixture。
- [ ] 优化大 Vault freshness 成本：按 Runtime revision 缓存 envelope digest 或引入 source-scoped proof digest；target resolver 只索引本批授权 path key 而不是首尾各构建一次全 Vault index；watcher capture identity 改为有界 generation-scoped 单调序列，避免长期 generation 的集合增长。
- [x] 增加 Runtime-owned source retirement/tombstone authority：把来源从 active Manifest 移入受保护 tombstone，原子 terminalize 目标 allocated/bound observation，保留 Queue/Review/input/apply ledger 历史并永久保留 identity/path/revision floor；普通 registration、Manifest writer 与旧 Runtime 均不得绕过或静默复活该边界。
- [ ] 在真实写盘前决定 CAS 后、progress 前崩溃的 content ABA 策略：接受 content-addressed at-least-once，或增加 mutation-intent marker 并在精确 before 状态 fail closed。
- [ ] 为已经创建 journal 的 transaction `recovery_required` 增加显式重新校验、继续修复或安全 rollback 方案；当前 Recovery UI 对 active/blocked/finalizing/global/Queue blocker 仅允许重新检查，不冒充通用回滚。
- [x] 为“accepted apply claim 已落盘、transaction journal 尚未创建即崩溃”完成 no-journal 显式恢复 Core 与 production Studio 接线：先从同一 Runtime snapshot 证明 exact Review/Queue/allocator 身份以及 journal、commit marker、source-input ledger 均不存在，再允许 Continue 或 Abandon；`accepted_not_started` 只允许 Continue，`requires_decision` 才允许二者。Manifest 漂移会阻止 Continue，但不阻止经原子证明的 Abandon；所有动作完成或进入新恢复门后重建完整启动代次。
- [ ] 严格解析并脱敏 file/projection adapter 的运行时成功返回值，非法 adapter payload 统一映射为受控 infrastructure error。

### Slice 1C：Golden Flow 产品闭环

- [x] 为 Knowledge Studio 加入 Windows Desktop 功能级 platform guard、非支持平台提示，并记录 `manifest.json` 保持非 desktop-only 的兼容决定与用户文档。
- [x] 已在独立 Windows 测试 Vault 验证 Runtime/Queue/Review adapter、source crawl、DeepSeek compile-to-Review、reload/cancellation、G.2z-c Activity/Reject，以及 G.2z-d fresh create/update Apply、外部修改 fail-closed、no-journal Recovery Continue 与冷重载幂等；Continue/Abandon 完整 action matrix 由自动化覆盖。功能 guard 不替代原子 adapter，NTFS/OneDrive/junction、双实例竞争和 crash/power-loss 仍是独立门槛。
- [x] 在 Chat 的 Vault 来源拖入中增加 `Use in this chat` / `Add to Knowledge` 分流。
  - [x] H.3a Vault source slice：从 Obsidian Vault 拖入 `.md/.markdown/.txt/.pdf` 时先显示显式 intent card；`Use in this chat` 只加入当前 Chat context，`Add to Knowledge` 只在 Windows live generation、唯一 Bundle、唯一 source root、exact file 已位于 root 且存在唯一 parser owner 时幂等注册 Manifest，随后触发 fresh generation/Activity。选择前不改变任一状态，Add 不隐式加入 Chat。
  - [x] PDF 使用独立 byte-only production parser：复制已复证的 raw bytes 后调用 Obsidian 公共 `loadPdfJs`，关闭动态求值，按 1-based 连续页提取有界文本；原 PDF SHA-256、宿主 API 版本与确定性抽取契约进入 artifact/profile，畸形、加密、全空/扫描件和超限材料 fail closed，不复用 PDFCache、Miyo 或 Brevilabs。
  - [x] 以 Knowledge Studio **Import folder** 取代“先手动搬入 Vault”的外部来源操作：只接受用户显式选择的单个文件夹，把 `.md/.markdown/.txt/.pdf` 复制到唯一 Bundle 的 `<sourceRoot>/<selected root>/<relative path>` 并保留层级；外部原件只读，不持久化绝对路径，Vault 副本以 `managed_copy/folder_import` 注册。同字节目标复用，不同字节冲突绝不覆盖；逐文件 partial/resumable，整批最多请求一次 generation refresh，后续仍须显式 Review/Apply。URL/浏览器导入和 Windows Explorer 单文件导入按产品决定不做独立入口。
- [x] 完成纯 Core 单来源两阶段 compile；生成阶段只能返回已批准的 opaque target id，不能提交 path、operation、hash、source refs、validation 或 status。
- [x] 把已隔离并在独立 production chain 中验证的 parser artifact、DeepSeek JSON-Object adapter、target authorization/resolver、projected OKF/link validator 与 compile → Review handler 接入 `main.ts` 持有的真实 worker；adapter 不解析 fenced JSON、不静默修复模型输出，也不在 transport 内重试。
- [x] 抽取 ApplyView 的纯 diff 展示，并以独立 Review Core/UI 支持多文件 create/update、来源、校验和逐文件/逐块审核；delete 可见但 reject-only。
- [x] 建立最小 Knowledge Studio / Activity 表面，显示解析、分析、关联、生成、校验、审核、apply、finalizing 和 recovery 阶段；真实 adapter 未接通前明确 fail closed。
- [x] 将 Review startup reconciliation、active transaction recovery 与 no-journal atomic classification 组装进纯 TypeScript layout-ready startup gate；Gate 只收敛已有 journal/commit 证据，不执行 no-journal 决策或 Queue resume。
- [x] 为 clear startup observation 实现独立 conditional release Core；observation 变化保持原字节并要求重跑 Gate；非 startup pause 与 recovery evidence 作为显式 blocker 原样保留，须先由对应用户/限流/恢复流程改变状态，再取得 fresh Gate observation，不能降级为普通 Queue resume。
- [x] 接入生产 Project Bundle 配置源、插件级 stable delegating Studio port、动态 Bundle session 与只证明 Runtime/Projects/config 的 layout-ready 前置 Barrier；无唯一真实 Bundle 时 Controller 不启动，旧 View 不锁定 sentinel，所有状态继续 `adapter_unavailable`。
- [x] 把 background compile → durable Review 连接到 Studio live Activity/Review、显式 reviewed create/update Apply 与限定 no-journal Recovery UI；保留的 user/rate-limit pause、empty/running Queue、observation-converged 或一次 `assertHealthy()` 均不构成新的 claim/apply authority，系统不会自动 Continue、自动接受提案或把旧 `observed_clear` 当作写权限。
  - [x] G.2z-a：增加 Queue + Review 的同一 Runtime 原子只读投影；Wiki target 读取后执行有界复证，拒绝跨 revision 撕裂快照。
  - [x] G.2z-b：接通只读 live Activity 与 exact pending Review diff；只显示与 Queue durable anchor 精确一致的 pending record，命令能力继续禁用且不写 Wiki。
  - [x] G.2z-c：完成 durable Activity pause/resume/cancel/retry 与 whole-proposal Review Reject；自动化覆盖 exact snapshot revision、stale command、commit-then-throw、startup reconciliation 与 stale high-watermark Retry，Windows 测试 Vault 已验证 Pause/Resume、冷重载保留 user pause、原子 Reject 及 Wiki/Manifest/transaction/ledger 零误写。
  - [x] G.2z-d：实现显式 reviewed create/update Accept → exact Queue apply claim → transaction Apply → Manifest/ledger/Queue/journal finalize，以及 action-matrix 驱动的 Recovery 页；正常结果只在完整终结后返回 `applied`，不确定持久化窗口返回 `recovery_required` 并重建启动代次。
  - [x] G.2z-d Windows 实机：已验收 fresh create/update、block selection、冷重载幂等、外部修改 fail-closed，以及受控故障产生的真实 no-journal Recovery 决策；全过程未手工改写 Runtime。
  - [x] G.2z-e：部署 Windows 测试 Vault，验收双 pending Review、外部修改 stale、reload/disable、主窗口/popout 与 Wiki 零误写。
- [x] 完成当前 H.1–H.3 范围的 Windows 端到端门禁；grounded answer、Markdown citation jump、Save-to-Wiki、Vault-source Chat intent，以及真实 PDF ingest → accepted provenance → exact-page 交互均已通过 bounded Windows acceptance。完整 Golden Flow 仍由 production-shaped composition、source lifecycle 与更广 Windows 可靠性项目独立跟踪。
  - [x] H.1：从同一 Runtime envelope 投影当前 Manifest + exact ledger + accepted Review 的 applied provenance；pending、rejected、accepted-but-uncommitted 与已被新版本替代的历史页面不得进入 Query corpus。
  - [x] H.1：对已证明页面执行 exact-byte SHA-256 复证，再在不可变内存快照上做 Markdown-only scoped lexical retrieval；不得回退到全 Vault，也不得依赖 mtime 或弱 chunk hash。
  - [x] H.1：接通 Knowledge Studio Query 的临时结果与 Markdown citation jump；查询、取消或代次替换后的晚到结果不得发布，Query 本身不得写 Runtime 或 Wiki。
  - [x] H.1 Windows 实机：独立测试 Vault 中 Query 页签随 production generation 发布；两次真实 UI 查询均命中 current applied Wiki，首个 opaque quote citation 打开 Bundle source 并精确选择对应 Markdown 范围，查询前后 Runtime/Wiki/Source SHA-256 不变且无 active transaction 或 Obsidian 开发者错误。
  - [x] H.2 Core/生产接线/UI：Query 只把有界 applied-Wiki context 与逐条复证的 exact Markdown source excerpt 交给独立 answer route；Wiki context 不可引用，所有 claim 只能使用本次请求 allowlist 内的 evidence id，并显式区分来源事实、推断与证据不足。Preflight 同一 lease 分别持有 Compiler/answer route；answer 每次最多一次非流式 DeepSeek JSON 请求，无 fallback、repair 或内部 retry。模型返回后必须复读完整 Wiki/source provenance 并再次复证实际使用的 locator，漂移、取消或换代均不得发布；Query 全程无 Runtime/Wiki 写权限。
  - [x] H.2 自动化发布门禁：真实 DeepSeek answer integration、全仓 Jest、TypeScript、Prettier、ESLint、production build、secret scan 与 diff check 全部通过；实际结果记录在 Testing Checklist。
  - [x] H.2 Windows 实机：真实 Query 表单触发 DeepSeek grounded answer，验收 Supported/Partial/Insufficient、Source fact/Inference、opaque citation jump、reload/换代主动取消、无证据零请求，以及查询前后 Runtime/Wiki/Source hash 不变；显式 refresh 的旧引用撤销已由 H.1 实机与 H.2 自动化共同覆盖。
  - [x] H.3 总体门禁：`Save to Wiki` → `query_writeback` ChangeSet → Review/Apply、Chat `Use in this chat` / `Add to Knowledge` 分流，以及真实 PDF production ingest → reviewed Apply provenance → exact-page navigation 均已接通并完成 bounded Windows 验收。
    - [x] H.3a Chat Vault source chooser：`.md/.markdown/.txt/.pdf` 的 one-use/durable intent 分流、generation-bound Manifest registration、idempotent existing source receipt 与 Activity refresh 已接通。外部材料收敛到 Knowledge Studio 的 folder-only **Import folder**，不再以 Windows Explorer 单文件 capture 作为待实现产品项。
    - [x] PDF production parser 与严格 boundary：`loadPdfJs` 只接收 parser-owned byte copy，`isEvalSupported: false`，页数/总字符/原始字节有硬限制；strict verifier 拒绝 getter、extra/sparse/跳页、全空、字符超限与 raw-hash drift，并把 deeply-frozen `PdfArtifactObservation` 交给现有逐页 Compiler evidence。Workflow bridge、profile registry、取消清理与 host-version fingerprint 均有聚焦自动化。
    - [x] PDF page jump 代码与自动化：只接纳 shared validator 证明的 positive safe-integer page 和 exact `.pdf` TFile；使用 `readBinary` 对 locator 的 raw artifact SHA-256 做打开前/后复证，并仅通过 captured Workspace 的 canonical `sourcePath#page=N` `openLinkText` 契约打开。UI 只回传 opaque citation ref，不使用 private PDF view/DOM/eState，也不把该能力冒充 PDF ingestion 或 H.2 PDF evidence。
    - [x] Save-to-Wiki 生产代码与自动化：只对当前、未撤销且含已验证 claim 的 Supported/Partial 答案开放。协调器重证答案、applied-Wiki snapshot 与已用 citation，产生 content-addressed managed Markdown capture，以 exact `managed_copy` origin 注册 Manifest，再交由真实 watcher → durable Queue → Compiler `query_writeback` → Review → transaction → Apply 链路。Save 本身不直接写 Wiki，不伪造 queued receipt，也不绕过显式 Review。
    - [x] H.3 Save-to-Wiki Windows 实机：Obsidian 1.13.4 专用 Vault 以真实 DeepSeek Supported answer 创建 managed source，约 0.9 秒观察窗内 Runtime 变化而既有 Wiki SHA-256 完全不变；兼容的 create-only Schema 随后产生 `query_writeback`/`query_writeback_source_compile` Review。`Accept all` 后、`Submit review` 前 Wiki 仍未改变，显式提交再经持久事务与一次真实启动恢复完成 Apply。最终 source `source-7468…` 为 `completed/completed`，Review accepted、Manifest `9 → 10`、ledger 绑定同一 transaction、active transaction 为空；目标页 SHA-256 `049f600ec2b82232bcdac5879d8b3ee3af751f82abafe558448d91dcf0d6e9ea` 与提案 afterHash 一致，既有两页 hash 不变，诊断探针已移除且 `dev:errors`/error console 为空。
    - [x] H.3 Apply 父目录 fail-fast：实机发现 nested create 的父目录不存在时，Windows CAS 按设计拒绝隐式 `mkdir`，原链路会在 durable acceptance 后进入恢复。现已把 `requiresExistingParentForCreate` 纳入 file-store capability，并在 durable Review acceptance 前后都复证 parent；missing/file parent 返回结构化 blocked diagnostics，不创建 journal、不接受 Review、不触发 CAS。自动化同时锁定缺字段 fail-closed、父目录 I/O 脱敏与 native creator 零隐式建目录。
    - [x] H.3 Chat Windows 实机：Obsidian 1.13.4 专用 Vault 在 Bundle paused 下验证两种 intent。选择前 Runtime SHA-256 不变；`Use in this chat` 只增加当前 Chat badge，Manifest/Queue 零变化；`Add to Knowledge` 不增加 Chat context，持久化 exact `user_managed`/`chat_add_to_knowledge` Manifest origin、pending/queued job、high-watermark 与 Activity 行。重复 Add 后 Runtime SHA-256 完全不变。测试 job 随后通过真实 Activity Cancel 终结，Bundle 恢复 Running，active transaction 为空，`dev:errors` 与 error console 均为空。
    - [x] H.3 PDF Windows 实机：Obsidian 1.13.4 全新隔离 Vault 通过真实 Chat `Add to Knowledge` 完成 `user_managed` Manifest → watcher/Queue → DeepSeek 两阶段 Compiler → Review/Apply；proposal afterHash 与最终 Wiki SHA-256 同为 `c1278d61…`，3 个 accepted provenance locator 分别绑定 raw PDF SHA-256 `fc8c5840…` 的第 1/3/5 页。Knowledge Query 按 H.2 边界返回 `Insufficient evidence`，但 retrieval hit 正确发布 3 个 PDF page chip；首/中/末页、中文和空格路径、主窗口第 3 页重复点击、真实 popout 第 3 页重复点击及重载后第 3/5 页均以每次 2 次 exact-byte 复证和一次 canonical `#page=N` 打开通过。source drift 时旧引用只复读 1 次且零打开，恢复原始字节后 Queue 收敛；query 换代与 plugin reload 后旧 opaque ref 均为零读取/零打开，reload 后新 port 继续可用。最终 active transaction 为空，探针已恢复并删除，`dev:errors`、warn/error console 均为空。
- [x] 完成首个 bounded vertical slice：同一 PDF source 已经真实 `Add to Knowledge → queue → DeepSeek compile → Review/Apply → applied provenance → Query retrieval/citation`；随后完成 v3→v4、真实模型 `no_changes` 与 exact-input high-watermark skip。本轮又接通并实机验证 Runtime-proven output admission：完整输出 cold scan/reload 继续零模型 skip，删除已跟踪输出会进入真实 repair Queue/Review，生产端 Reject 保持 missing 状态且不写 Wiki；测试 harness 恢复 exact backup 后重新收敛为零模型 clean state。byte drift、directory 与 shared-owner/Apply 交错由自动化覆盖；H.2 仍按设计不将 PDF 作为模型综合证据。

### Parallel Reliability Track

- [ ] 在最终消息装配点执行 L1-L5 总 token 预算。
- [ ] 使用确定性 fallback artifact ID。
- [ ] 修复聊天重载后的 context envelope 与共享 MemoryManager 恢复一致性；旧 `handleProjectSwitch()` 已发出的 clear/save 目前不能由外层 ProjectManager generation guard 撤销。
- [ ] 为同一生命周期的 project switch 增加 request-scoped context/load authority；当前旧请求不能发布 selection/UI，但已经进入 `loadProjectContext()` 的缓存、解析与 `ProjectLoadTracker` 子操作不会被后续 switch 撤销，可能继续消耗资源并把旧项目进度写入共享 loading atom。
- [ ] 把非 Project 模式的 `PDFParser`/`PDFCache` 从动态全局 `app.vault` 改为显式 Vault owner；当前 project-mode `Docs4LLMParser` 已隔离，但普通 PDF 解析响应仍可能在跨 Vault 切换后写入错误的全局 PDF cache。
- [ ] 把 `VectorStoreManager` 从永久 global-app singleton 改为 exact App/Vault lifecycle owner，并在 unload/rebind 时停止旧索引 continuation、移除旧 Vault listener；当前 G.2i 只隔离 Projects/Bundle discovery，不宣称语义索引已跨 Vault 隔离。
- [ ] 为已进入 provider 初始化的旧 ChainManager 增加最终模型提交 authority；当前 dispose 会解除订阅并停止 await 后续 housekeeping，但无法取消已经交给共享 ChatModelManager 的 provider 构造。

## Architecture Decisions

- 新方案以个人知识资产可拥有、可追溯、可迁移为产品边界。
- 以 `MessageRepository`、`ChatManager`、L1-L5 `PromptContextEnvelope` 和 `ToolRegistry` 为当前架构基线。
- LangChain 路径按当前已实现能力描述；ACP、显式 Planner 和动态 MCP 接入按演进目标描述。
- 迁移 SOC 方案的控制流、证据边界、权限、候选式学习和薄入口原则，不迁移告警、租户、Kafka、PostgreSQL、复核队列等领域结构。
- 将 Karpathy 的 LLM-maintained Wiki 作为知识复利层，将 OKF 作为该层的可选标准化文件契约；二者都不替代 Copilot Runtime。
- 不引入第二套桌面应用、Python sidecar 或图数据库作为 MVP 前提；优先在现有 TypeScript Runtime 内实现编译式 Wiki 契约。
- 优先借鉴 `llm-wiki-compiler` 的两阶段编译、manifest、ChangeSet、lint/eval 和 OKF 互操作，但在依赖和代码审计前不直接接入其 Node 24 CLI Runtime。
- Chat 继续作为即时助手入口，新增全页 Knowledge Studio 承载 Sources、Wiki、Review、Activity、Graph 和 Health。
- 体验优先不等于直接写盘：信任机制通过阶段进度、citation、multi-file diff、事务和撤销融入主流程。
- 当前 Search v3 是基础检索底座；只增加 Wiki 渐进发现和一跳图扩展，不引入 LanceDB 或第二套基础搜索。
- Source hash 必须与 pipeline fingerprint 共同决定幂等，避免 schema、parser 或模型变化后错误跳过。
- `llm_wiki` 主要提供 Knowledge Studio、队列、来源卡片、Review Inbox 和图谱 UX；不引入 Tauri/Rust Runtime。
- `claude-obsidian` 主要提供 hot/index/domain/page、Manifest 与 Ingest/Query/Lint 工作流；不复制 Bash 锁和自动 Git hook。
- Graphiti 当前只贡献 provenance 和双时态契约；只有真实历史/多跳需求达到门槛后才作为可重建外部投影接入。
- 外部源码按 Copy / Port / Reference 管理；首次复制时同步提交 attribution、许可证、原 commit/path 和修改说明。
- 当前唯一产品与验收平台是 Windows Obsidian Desktop；不为 macOS、Linux、iOS、Android 或浏览器增加兼容工作。
- 允许在 adapter 边缘使用有明确体验收益的 Windows/Node/Electron 能力，但纯知识契约保持 TypeScript 与 I/O 无关。
- 不因平台收敛直接嵌入 Node 24 CLI；所有依赖仍须兼容 Obsidian 实际捆绑的 Windows Runtime。
- Manifest 不保存 processing 状态；持久来源状态与当前 Queue Job 在 UI 层按 `sourceId` 联结。
- `sourceContentHash` 使用原始文件字节；citation quote 只统一换行符；ChangeSet before/after hash 保留精确文本字节语义。
- Windows Vault path 在领域边界选择“验证并拒绝”，不静默修复盘符、UNC、反斜杠、保留设备名或碰撞目标。
- production profile source 只能向 Pipeline fingerprint 传入显式 allowlisted model behavior 与受检静态 JSON 配置；Core 另防御性拒绝常见 credential-like 字段，但不把黑名单当成 allowlist 的替代品。Knowledge model adapter 只消费该 projection 和匹配的私有 route capability；首个 DeepSeek route factory 把 credential/native-fetch port 限定在私有 closure。零网络 preflight 只验证并保留 opaque route lease；released worker 只能通过该 lease 使用 exact selected values，禁止再次把完整 settings 暴露给 Core。
- Queue 的 `inputRevision` 必须由 source adapter 按 source 严格单调分配、跨重启保存，并在相同内容观察时同样推进；不得使用内存计数器或文件 mtime 代替。
- source adapter 必须在 watcher handler 开始、任何异步读取/解析之前生成全局永久唯一且跨重试稳定的 `captureId`（生产 adapter 使用随机 UUID/等价 opaque id）并分配 opaque observation token/`inputRevision`，再以 first-write-wins hand-off 绑定随后实际读到的 bytes hash 与 pipeline fingerprint。Queue adapter 只能凭该 token 原子消费 exact bound record；相同 capture 重放不增 revision且已 consumed 时不再调用 Queue，不同 payload 冲突，较新未绑定 allocation 不会丢弃仍可用的较低 bound read，较新 bound read 才会淘汰较低未终结观察。独立 Obsidian watcher、opaque plan Core、双采集 snapshot loader、UTF-8/PDF byte parser、durable Queue/Runtime job binding、重启 observation reconciler/coordinator、确定性 prompt、DeepSeek private transport、零网络 credential/native-fetch preflight 与 released compiler worker 已组成 exact generation lifecycle；Studio live Activity/Review、exact Activity/literal Reject、显式 create/update Apply、限定 no-journal Recovery，以及 H.2 current-applied-Wiki grounded Query/Markdown citation jump 已接通。Grounded answer 是与 worker/Compiler 分离的 generation-owned、Bundle-scoped、read-only capability；同一 production lease 管理两条私有 route，但 prompt、schema、request authority 与入口互不复用。PDF ingestion 使用 Obsidian 公共 local PDF engine，把 parser-owned byte copy 转成 raw-hash-bound 逐页 evidence；PDF page jump 使用 raw-byte hash reproof + public `#page=N` adapter/UI，二者已通过同一 Windows production-chain 实机门禁。Save to Wiki 已接通 managed capture → Manifest → watcher/Queue → Compiler `query_writeback` → Review → transaction → Apply 的生产代码与自动化，并已通过完整 Windows capture-through-Apply/Recovery 验收。
- model execution authority 固定为 Queue local claim + Runtime atomic proof + workflow stable reproof 三层：Queue claim 绑定 exact QueueStorage 并在退出 executor 前同步 revoke；opaque owner 排他注册 exact Runtime/workflow loader；Runtime proof 必须同时命中 exact processing attempt、consumed observation 与 Manifest；workflow 在 source read/parser/model 前后复证 retained Manifest/schema/profile。任何层都不接受同型 DTO 替代，模型 adapter 也不接受 caller 替换 `AbortSignal`。每份 authorized preparation 只能绑定一个 adapter；每次 compile 使用独立 opaque session，analysis/generation 必须同源；private route 没有 public invoke，provider output 必须先经过 bounded data-only snapshot，再通过 post-call authority proof 才能发布。
- production Compiler 入口固定为 `route lease → generation-owned attempt builder → opaque one-shot attempt → route lease`：Queue claim 同时绑定 exact processing job、AbortSignal 与 stage reporter，builder 只从 matching preparation/context 派生 Compiler DTO，context pages 在 retrieval authority 出现前必须为空，Manifest 既有页只给 write-only grounded authority。任何裸 DTO、逐 attempt port、复制 context 或跨 lease attempt 都不能到达 model adapter。
- Compiler source identity 与 ChangeSet/review identity 同时包含 `inputRevision`；因此来源 A→B→A 不会复用第一次 A 的审核实例。该来源观察 ABA 已关闭，不等同于仍待决定的文件写入 content ABA。
- Queue 在 claim 外持久每个 source 的 `sourceHighWatermarks`（revision + source hash + pipeline fingerprint）；等价的更新观察不能改写已认领输入，但必须推进 high-watermark 以拒绝后到的旧内容。
- `QueueStorage.write` 的 expected revision 比较与完整 snapshot 替换必须是 adapter 内的同一原子操作。
- Queue snapshot 使用 strict、显式版本化 schema；新增持久字段必须配套版本升级和迁移，不得依靠静默宽容读取。
- Queue 执行语义是 at-least-once：持久 claim 保证同一时刻至多一个 active job，attempt ownership 拒绝迟到结果，但终态写入前崩溃仍可能重放模型调用。
- EventSink 只提供非阻塞 post-commit 通知，持久 Queue snapshot 才是状态真相。
- ChangeSet journal v3 使用 Vault-global 单活动槽；完整 Bundle、accepted ChangeSet、精确 before/after、最终 Manifest intent/plan digest，以及 owning source id/hash/pipeline/input revision/job attempt/start 一并持久化；claim source 必须存在于 ChangeSet source refs。
- 单文件写入只能通过 adapter 原子 compare-and-swap；全局 journal 提供可恢复语义，但 Obsidian Vault 不具备真正跨文件原子性。
- 自动恢复只做幂等 roll-forward；divergent state 进入 sticky `recovery_required`，不自动 rollback 或覆盖用户编辑；committed marker 对后续用户修改保持权威。
- Queue v5 保留 exact `applyClaim`、pending review anchor、review rejection tombstone、no-journal apply abandonment tombstone 与 `commit_pending_ack` marker；审核 hand-off 固定为 durable pending record revision 0 → exact accepted/rejected record revision 1。accepted apply claim 同时绑定 proposal digest、accepted digest、最终 Manifest intent digest、review revision/time 与完整 job claim；启动恢复改为 failed 后仍原样保留。v1/v2 无法证明的 review/apply state，以及 v3 已开始但未保存 intent digest 的审核 apply，都以 `legacy_unverified` 明确 fail closed；v4 严格迁移时只初始化空 abandonment 历史，不猜测 cancelled job 的旧决策。
- no-journal recovery id 只由跨完整生命周期仍会保留的 accepted Review identity 派生，不把最终成功后已不再保存的 `startedAt` 放入 id；action 时仍必须从当前 exact Queue claim 重新取得并核验 `startedAt`。continue 先由 Runtime 重证 Queue/Review/high-watermark/allocator/Manifest/source-order，再由 prepared-journal atomic publication 重证；abandon 则在同一 envelope transform 中要求任何 phase 的 active journal、Queue commit marker 与同 source/input ledger 均不存在，保留 immutable accepted Review，并写 cancelled job + exact tombstone。
- startup gate 必须持有严格的当前 Bundle 配置，因为 prepared/applying journal 的已有证据恢复可能真实观察并 CAS Wiki；Gate 先恢复 Queue，再收敛 active transaction/commit marker、Review pending/rejected hand-off，最后由 Runtime 在同一 envelope 中按 exact Review revision 批量返回 Queue、Vault-global transaction 与 accepted classifications。Gate 用统一 opaque recovery id 精确证明 accepted actions/classifications 一一对应，并拒绝 Runtime revision 回退、Queue revision 回退或同 revision 不同内容；`observed_clear` 不直接释放 startup pause。独立 release coordinator 只传递 Runtime/Review/Queue revision，Runtime 在同一 envelope mutation 中重证全部写恢复和 accepted terminal evidence后才释放；generic Queue facade 将 `startup_recovery`、`recovery_required` 与 `commit_pending_ack` 作为单调门，同门 mutation 必须保留 current claim/job/marker，running/startup 的 accepted-not-started pending anchor 也不能被 generic CAS 擦除；跨门 mutation 必须等于 exact accepted-begin、startup-recovery、current-active-committed resolution 或固定 metadata 的 marker-finalization 投影，任何一步都不能借历史 ledger 或伪造 abandonment 替换证据或降级为 running/user/rate-limit。Runtime 专用 abandon 是 append-only tombstone 的唯一写 authority；watcher high-watermark 不可删除、回退、同 revision 改写或超过 allocator，合法较新观察仍可更新 processing/awaiting job 的 rerun 状态与单调 `updatedAt`。没有 durable release receipt 时，commit-then-throw 后旧令牌只会失效，调用者必须重跑 Gate，不能用 `revision + 1` 猜测成功。
- Review 启动协调器只拥有 Review/Queue port：按 `recordedAt + changeSetId` 稳定扫描，pending/rejected 做 exact、可重放的 Queue 收敛；accepted 只返回供更高层 journal/ledger/no-journal 分类的 identity，绝不在启动时调用 `beginReviewApply`。每轮 mutation 后重读 Review revision，持续变化超过有界次数即 fail closed；返回 revision 仍在最终 startup gate 复证。它已接入 plugin startup，但只负责收敛既有证据，不能自动 Accept 或开始 Apply。
- applying executor 必须返回 `completed + exact commitReceipt`；`IngestQueue.runNext` 只对外转换为 `commit_ready`，Queue 在 Manifest 之前仍保持 processing/applying。审核接受转换为新的 durable applying claim，不能直接完成 job。
- 页面提交后的协议固定为只读 `queue claim verify`，再持久执行 `atomic authority reproof+manifest+ledger → queue marker → journal ack → queue release`；最终 callback 从同一 runtime envelope 复证 Queue claim、accepted Review payload/intent 及 `allocator ≥ source high-watermark ≥ apply claim`，再按 exact transaction/revision/ChangeSet/intent/journal/receipt digest 幂等发布成功。较新的同内容观察可并存；较新的不同内容必须由 retained rerun 或启动恢复后 promoted successor 精确证明。
- 每个 retained rerun 必须精确等于该 source 当前 high-watermark；终结旧 apply 时若已有同 source active successor，rerun 继续归 successor 所有，不能被重复提升为第二个 active job。
- `commit_pending_ack` Queue marker 必须在每次 runtime parse 时与同一 transaction 的 apply ledger 精确匹配 source/hash/pipeline/input/ChangeSet/digest/revision/time；active journal 已清除但 ledger 缺失或 marker 撕裂时启动直接 fail closed。
- 直接 `ChangeSetTransaction.apply` 在 target observation 前必须通过 runtime authority port 复证 Queue/Review/high-watermark/allocator 与 Manifest read-set/source commit ordering；prepared 发布在同一 shared-envelope transform 内重复证明以关闭预检竞争。prepared → applying、启动读取 prepared/applying journal 以及活动事务期间的每次 envelope mutation 都保留同一完整 reservation；Queue v3 缺少最终 intent digest或 Manifest 已漂移时，不会先观察或修改 Wiki 文件再到最终 ledger 才失败。
- 清除 Queue marker 后仍保留 `startup_recovery` 暂停；必须重跑 Gate 并通过 conditional release，普通 Resume 不能清除该 gate。
- Compiler stage 1 只能从 caller-owned target catalog 读取/修改已知页；catalog 外路径只拥有 create-only 权限，resolver 必须使用 Windows 大小写不敏感 existence probe，既有内容不得送入生成模型。
- Compiler delete 只允许 Manifest 标记为 generated、由当前 primary source 独占且当前 bytes 仍匹配 last-generated hash 的目标；该授权已进入 Review plan/journal intent，并在 Runtime reservation/commit 复证，但安全 compare-delete 与其余 semantic dependency 未完成，产品仍保持 reject-only。
- Stage 2 只接收 runtime 绑定后的 create/update targets，并按 exact target-set digest 返回 `targetId + write/unchanged`；delete 原文、自由路径、operation、hash、validation 和 status 永不进入模型输出契约。
- 模型 citation 只能选择本次实际提供的 evidence id；source/artifact identity、locator 与 quote hash 来自 parser-owned registry，普通 claim 必须至少有一条 material-valid `supports`，`context/contradicts` 不构成 grounding。
- Candidate validator 是构造 proposed ChangeSet 的必需端口；三项 validation flag 均须由确定性 runtime 返回 true，Compiler 永不构造 accepted/applied 状态，apply-time 仍独立复验。
- legacy ApplyView 的 `Vault.create/modify` 路径继续只服务既有单文件工具；Knowledge Review 只复用纯 diff renderer，绝不把该路径当作 ChangeSet transaction writer。
- Review UI 只能提交 proposal/snapshot identity 与 opaque change/block ids；选择后的 content/hash/validation/accepted status 与完整 ChangeSet digest 由 Core 重建，同时保留 proposal-owned change identity/order，UI 无 Vault/App/storage 依赖。
- Durable Review Store v2 独立于 Queue 保存完整 proposal、Manifest commit plan 和终态 payload/intent；Queue 不能只靠 `awaiting_review.changeSetId` 支撑重启恢复。executor 必须先保存 pending record，再把精确 receipt 交给 Queue；启动编排器在重新 claim 前必须扫描并调用 `reconcilePendingReview`。
- `awaiting_review` 不能走通用 `cancel()`；拒绝必须先写入 durable Review Store，再由 `rejectReview()` 原子移除 pending anchor、写 rejection tombstone 并提升 retained rerun。
- 同一 accepted receipt 在 active applying claim 内重放不增加 revision/event；冲突 receipt fail closed。显式 reviewed-Apply 协调器对 acceptance、apply claim、transaction 与 finalization 的不确定提交逐层执行 exact reread/replay；apply 已完成并清除 claim 后的晚到接受不会重新 Apply。
- Compiler delete 的 Manifest ownership、source provenance 与 last-generated hash read-set 已持久复证；在原子 compare-delete、schema/link/source-artifact dependency 与 Windows 实机验收完成前仍始终以 `reject_only` 呈现。
- Activity 的唯一事实源是 strict durable Queue snapshot；EventSink 只是 reload hint，`applyCommit`/`commit_pending_ack` 清除前 completed job 必须显示为 finalizing。
- Queue v3 引入、v4 保留的 `reviewRejections` 为拒绝动作提供跨崩溃幂等和 identity conflict 防护；拒绝先持久化 Review Store，再转换 Queue job，不制造空 accepted ChangeSet。
- Knowledge Studio 只在 Windows Obsidian Desktop 注册。整个 Copilot 插件仍保留原有平台范围，因此 `manifest.json` 不改为 desktop-only；这不构成对其他平台 Knowledge Studio 的支持承诺。
- 当前 Windows 启动初始化插件私有 `knowledge-runtime-v1.json`（文件名稳定、outer schema v4），再由 layout-ready Barrier 等待普通 Projects readiness、严格验证全部 project-owned Bundle 并执行 generation-owned 零网络 preflight。成功预检后由 recovery-only startup Gate 先收敛已有 durable bookkeeping 和已授权 journal；只有 `observed_clear` 才启动同代 observation reconciler/coordinator，注册 Vault listeners 并把合法 source observation 放入 held Queue。初次 observation 收敛后，session 会 reprove observation、运行 fresh Gate 并尝试 Runtime conditional release；只有所有配置 Bundle 通过，`main.ts` 才用预检保留的 opaque route lease 安装同代 worker、live Studio read generation 与窄 command adapter。Released worker 可真实运行 exact-byte parser 与 DeepSeek 两阶段并把候选持久到同 Runtime Review 后进入 `awaiting_review`；worker 本身没有 Accept/Apply/Query 权限。Studio 从同一 Runtime 原子快照读取 Activity/Review，并把 Reject、reviewed Apply、no-journal Recovery 与 H.2 read-only grounded Query 分别委托给 generation-owned 窄协调器；Query 仅接纳 current Manifest + latest ledger + accepted Review 精确证明且当前 SHA-256 匹配的 Wiki 页面，模型只收到有界 Wiki context 与已复证 Markdown source excerpt，引用只通过 current opaque ref 打开 exact locator。Settings/Projects/Composer 失效会同步撤下旧代并取消 answer route，已进入持久化边界的动作纳入 Vault drain，随后启动或等待新的安全 generation。
- Projects 生命周期以显式 App/Vault、opaque state owner 与 exact pending-write lease 为边界；新插件在任何 state reset 前同步退役旧 ProjectManager，旧 scan/CRUD/cleanup 不能发布到新 owner，同一生命周期 project switch 只允许最新请求发布 selection/UI。已经发给 Obsidian adapter 的单次 I/O 无法取消，因此 continuation 停止后由新 lifecycle rescan 收敛；同生命周期的旧 project context/load 子操作尚待 request-scoped authority，可重建的 context/file cache 若要做到同 Vault 已发出 I/O 的绝对隔离，仍需 epoch 路径或 adapter-level CAS。
- `knowledge-runtime-v1.json` 是首期个人规模的共享 envelope：跨子系统 CAS 简单，但每次 mutation 都全量 parse/validate/stringify/rewrite，存在 O(envelope size) 写放大和共享腐坏故障域。真实长期使用前必须设 size/latency threshold、协调 terminal archive/compaction，并在超过门槛时分片；它不是产品必须永久保留的核心格式。
- Runtime outer schema v4 保留原有受限的 v1→v2 与 v2→v3 迁移；v3→v4 则是 no-change proof 的 downgrade fence。它以 Queue 真实 latest-source ordering 反向核验当前可复用 completed outcome；没有 exact apply ledger/no-change marker 证明的当前 job 会保留 attempt 计数、其他历史 jobs、observation/high-watermark 后降为待重跑，并按设计清除自身 terminal fields，而不是伪造成功 proof。
- Runtime v4 每次 parse 都交叉验证 Manifest、reserved apply/no-change extension、apply ledger，以及 observation journal/Queue watermark；exact duplicate observation 允许 high-watermark 单调前进而复用较旧的 exact job/marker。历史 ledger/marker 可保留，但当前 proof、revision 与连续 digest 链不能倒退或断裂，多来源同 Windows path 必须保持 exact shared ownership/hash。
- Exact-input Queue 去重不再只信任 source high-watermark。Production admission 先读取 Runtime 证明的最新 Apply/no-change authority，再顺序观察 exact Manifest 输出，并只在 SHA-256 全部匹配且 Runtime authority sandwich 稳定时复用成功；输出事件仅反向触发原 source observation，不会把 Wiki 文件升级为 source、parser input 或写 authority。
- `SourceManifestStorage` 的通用路径只允许注册/重命名/失败观察和非保留 metadata；`lastSuccessful` 与 runtime reserved commit extension 只能由 atomic Manifest+ledger adapter 修改。shared page 的实际 owner 集由 exact Manifest digest 冻结，commit 时要求 owner authority 一致并同步推进所有 owner hash。
- 首次 runtime 文件以同目录完整临时文件、handle flush 和排他 hard-link 发布，避免并发启动暴露空/半 JSON；目录级掉电持久性与 NTFS/OneDrive/junction 行为仍属于 Windows 实机验收，不作超出证据的承诺。

## Testing Checklist

- [x] 核对所有“当前能力”均能在代码或现有设计文档中找到依据。
- [x] 核对所有规划能力均明确标记为目标或阶段项。
- [x] 运行 Markdown 格式检查。
- [x] 检查 Git diff，确保未修改无关文件。
- [x] 检查新增 PRD、主方案与复用台账之间的链接和决策一致性。
- [x] 检查 Markdown heading/fence/link 结构并运行 `git diff --check`；仓库没有安装 `node_modules`，未调用 Prettier。
- [x] 确认没有修改 DeerFlow/SOC 文件或把 `.env.test` 纳入版本控制。
- [x] Knowledge foundation 通过 TypeScript `noEmit`、目标 ESLint、本次变更文件 Prettier check 与 16 个 Jest suite / 444 个测试。
- [x] Commit E 集成后全仓单元回归通过：131 个 Jest suite / 2551 个测试；现有测试中的预期 console 警告不影响结果。
- [x] Commit F 通过 5 个定向 Jest suite / 79 个测试、TypeScript `noEmit`、全仓 ESLint 与本次变更文件 Prettier check；集成后全仓 135 个 Jest suite / 2623 个测试全部通过。
- [x] Commit G 通过 TypeScript `noEmit`、全仓 ESLint、15 个定向 Jest suite / 190 个测试，以及全仓 148 个 Jest suite / 2750 个测试；预期的既有 keychain/解密 console 输出不影响结果。
- [x] Commit G 新增/修改文件均经 Prettier 格式化；全仓 `npm run format:check` 只被本次未修改的既有 `src/LLMProviders/chatModelManager.ts` 格式基线阻塞，继续不夹带修改。
- [x] G.1 runtime adapter foundation 通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier check，以及全仓 151 个 Jest suite / 2794 个测试；既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2 Review startup coordinator 的 12 个定向测试通过：pending/rejected 收敛、重启幂等、accepted 零写入分类、Review revision 前进/持续 churn、并发 terminal decision 导致旧 Queue 操作失败后的 revision retry、稳定多记录排序、双 runtime pending/rejected 竞争、commit-then-throw 后置状态证明及 source identity 冲突；TypeScript `noEmit` 与变更文件 ESLint 通过。
- [x] G.2a 集成后全仓回归通过：152 个 Jest suite / 2806 个测试、TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier check 与 `git diff --check`；既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2b durable Manifest plan/intent 与 atomic ledger 通过 13 个定向 Jest suite / 273 个测试；最终全仓回归 154 个 Jest suite / 2871 个测试全部通过，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2c Queue v4 与 atomic apply-authority reproof 本轮通过 9 个定向 Jest suite / 234 个测试，以及全仓 154 个 Jest suite / 2894 个测试；覆盖 target observation 前 authority preflight、prepared/applying/startup 全 Manifest reservation、unfinished-journal 写前授权、Queue marker↔ledger、latest-rerun/source-ABA 与 existing-successor 回归，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2d no-journal recovery 与 Queue v5 本轮通过 Runtime 71 个测试、Queue/Core 聚焦 101 个测试及真实 compile→review→startup recovery→explicit continue→Wiki/Manifest/ledger/Queue ack 集成闭环；最终全仓 155 个 Jest suite / 2921 个测试全部通过，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。覆盖 accepted-not-started/active/blocked/finalizing/committed/abandoned 分类、stale Manifest、commit-then-throw、prepared publication 竞争、三代 rerun 保留与 exact abandonment replay；既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2e layout-ready startup gate 通过 Gate/Runtime/no-journal/真实 pipeline 聚焦 4 个 suite / 108 个测试，以及全仓 156 个 Jest suite / 2947 个测试；覆盖固定恢复顺序、同 transaction sticky conflict 归一、Runtime/Queue revision 单调性、同 revision Queue 内容一致性、accepted action↔classification 精确集合、atomic Vault-global transaction、no-journal/accepted-not-started attention、active/finalizing/Queue-only blocker、Review revision churn、真实 accepted-before-apply 字节零写入与无自动 action，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2f atomic conditional startup release 通过 Release/Runtime 聚焦 2 个 suite / 106 个测试、Queue/真实 pipeline 聚焦 2 个 suite / 55 个测试，以及全仓 157 个 Jest suite / 2980 个测试；覆盖 exact release、absent/running byte-preserving no-op、Runtime/Review/Queue stale token、user/rate-limit pause保真、generic resume/pause/CAS bypass、running/startup 下显式 exact accepted start 与 anchor 防擦除、三类恢复门单调强化、同门 claim/job/marker 保留、历史 ledger/伪造 abandonment 替换、fixed finalization metadata 与两步 CAS 降级防护、startup apply 期间真实 allocator-backed watcher divergent rerun、high-watermark 回退拒绝、较新同内容收敛与未分配首写拒绝、active transaction、claim/failed apply/commit marker、accepted-not-started blocker、abandoned/committed terminal、跨 Bundle recovery quarantine、双实例竞争、commit-then-throw 后强制重跑 Gate 与真实 stale→re-gate→release 闭环，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2g Runtime v3 durable source-observation hand-off 通过 Runtime/Queue/真实 pipeline 聚焦 3 个 suite / 167 个测试，以及全仓 157 个 Jest suite / 2997 个测试；覆盖全局 capture identity、并发幂等 allocation、first-write-wins 并发 binding、adapter-private Queue token capability、higher-unbound/older-bound 两种顺序、bind 与 Queue commit-then-throw、A→B→retry A terminal replay、无新 clock/entropy 的精确重放、重启 pending work 枚举与消费、startup blocker、v2 migration fence/checkpoint 和不可达 journal corruption matrix，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2h Project Bundle config 与插件级 fail-closed 前置接线通过 12 个定向 Jest suite / 97 个测试，以及全仓 164 个 Jest suite / 3055 个测试；覆盖 normal/hidden frontmatter 原值保存、普通编辑保留与显式删除、strict aggregate config、跨 Bundle Windows 边界冲突、ProjectRegister single-flight/cleanup、layout/runtime 到达顺序、stable delegate generation cancellation、动态 exact Bundle session、旧 View 切换及 unload fail-close，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2i Projects/插件 lifecycle ownership 通过 16 个聚焦 Jest suite / 119 个测试，以及全仓 175 个 Jest suite / 3124 个测试；覆盖 same-App hot reload、跨 App/Vault rebind、opaque state owner/lease、prepared-scan CAS、初始化/重载/目录扫描竞争、目录 B→C、旧目录 event/CRUD 反向恢复、migration/frontmatter continuation、ProjectLoadTracker、ProjectManager selection latest-wins、ChainManager subscription teardown、Runtime 动态加载、VaultDataManager listener/metadata owner 与 log flush/clear/open 的显式 Vault 归属，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2j Vault source watcher adapter 通过 watcher 聚焦 1 个 suite / 21 个测试、Runtime/Queue/真实提交管线相关 4 个 suite / 188 个测试，以及全仓 176 个 Jest suite / 3145 个测试；覆盖 exact App/Vault/adapter owner、listener-before-scan、allocate-before-read、BOM/CRLF/Unicode exact bytes、captured path、retry payload freeze、reader/allocation/settlement success 复证、capture-id factory 同步重入、partial EventRef 回滚、close/plan/source generation 交错、rename/delete/folder quarantine、old-path ABA、无关 source 在途任务保留、同路径多 Bundle 与 Runtime v3/Queue v5 乱序收敛，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2k opaque watch-plan builder 最终通过 fingerprint/relationship/builder/watcher 聚焦 4 个 suite / 145 个测试，以及全仓 177 个 Jest suite / 3217 个测试；覆盖 strict Bundle↔Manifest source/generated-root boundary、Manifest reorder/revision identity、exact BOM/CRLF/中文 schema bytes、真实 `Uint8Array` brand/覆写 `slice()` 与跨 Bundle schema conflict、Bundle/Compiler/parser/model 关键行为 fingerprint、通用 parser routing、常见 credential-like key/cyclic/non-JSON/accessor/`__proto__` 配置、单次数组长度快照与稀疏数组拒绝、Windows collision、跨 Bundle 同物理 source、空 Bundle malformed handoff、wrong-token direct construction、WeakMap state 不可反射、class/prototype monkeypatch 拒绝、同路径多 Bundle/notification replacement 重入、handoff capability 快照、injected success DTO getter/TOCTOU、启动时缺失 source 后续捕获，以及 replacement 后已开始 commit 可完成但禁止旧通知。并通过 TypeScript `noEmit`、全仓 ESLint、production build、变更文件 Prettier 与 `git diff --check`；全仓 `format:check` 仍只被本轮未修改的既有 `src/LLMProviders/chatModelManager.ts` 基线阻塞。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2l 只读 workflow-plan/profile/byte-parser boundary 通过 6 个聚焦 suite / 152 个测试，以及全仓 181 个 Jest suite / 3298 个测试；覆盖 exact model/no fallback、unknown/API-key getter 零读取、无关 model 重排/新增稳定性、secret/config guard、endpoint/routing 哈希摘要、原输入 mutation 与 WeakMap reflection、double collect/change detection、10,001 model/parser registry 资源上限、空 Manifest parser registry、wrong-token execution/loader forgery、Manifest/schema/generation parser 前后 stale、exact reader-owned bytes、parser resolve/reject mutation 与 success/invalid `Proxy` 校验期 mutation、malformed DTO/accessor、fatal UTF-8、BOM/CRLF/中文/emoji byte round-trip、schema byte/parsed character/parser byte 上限、取消与 deep freeze。并通过 TypeScript `noEmit`、全仓 ESLint、production build、变更文件 Prettier 与 `git diff --check`；全仓 `format:check` 仍只被本轮未修改的既有 `src/LLMProviders/chatModelManager.ts` 格式基线阻塞。首次沙箱内 Jest 因 localhost `EPERM` 失败，按相同参数在允许本地测试通道的环境重跑并全绿；既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2m Queue/Runtime/workflow/model execution capability boundary 通过 10 个聚焦 Jest suite / 345 个测试，以及全仓 185 个 Jest suite / 3361 个测试；覆盖 exact QueueStorage claim、同步 revoke、strict executor-result descriptor snapshot、unique Queue tuple/consumed observation/Manifest atomic proof、同 owner 跨 Runtime/loader generation 拒绝、Runtime↔workflow proof sandwich、preparation one-shot、Manifest/schema/profile drift、单 preparation 单 adapter、Compiler one-shot call authority、per-compile opaque session、跨 Compiler/跨 compile/重放拒绝、private route/profile digest、固定 wire/decoded-object 上限及真实 profile projection。并通过 TypeScript `noEmit`、全仓 ESLint、production build、变更文件 Prettier 与 `git diff --check`；全仓 `format:check` 仍只被本轮未修改的既有 `src/LLMProviders/chatModelManager.ts` 格式基线阻塞。未运行尚不存在的真实 provider transport 集成测试，既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2n Prompt/DeepSeek route 最终通过 4 个聚焦 Jest suite / 99 个测试，以及全仓 187 个 Jest suite / 3393 个测试；覆盖 canonical two-message prompt、受限 Schema policy 与 prompt-injection 数据隔离、descriptor-only Proxy snapshot、actual prompt/provider-route identity 进入 pipeline fingerprint、完整 profile digest、旧模型/custom endpoint/unsupported setting/credential fail-close、thinking/sampling 参数互斥、exact Queue signal、HTTP 返回后及 pending read 期间非阻塞 cancel、两阶段单次 POST、JSON Object/no streaming/fallback/retry、keep-alive whitespace、429 body 零读取、network/header cause 脱敏、redirect/URL/SSE/length/model/choice/usage/tool/function-call rejection、fatal UTF-8、声明/实际响应 byte cap 与 one-byte tiny-chunk 有界 buffer。并通过 TypeScript `noEmit`、全仓 ESLint、production build、变更文件 Prettier 与 `git diff --check`；全仓 `format:check` 仍只被本轮未修改的既有 `src/LLMProviders/chatModelManager.ts` 格式基线阻塞。真实 DeepSeek 网络 smoke、Windows Obsidian native-fetch/CORS/proxy/abort/reload 验收与 production credential/worker 接线仍未运行，既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2o DeepSeek V4 catalog、错误投影与零网络 production preflight 最终通过 15 个聚焦 Jest suite / 313 个测试，以及全仓 194 个 Jest suite / 3465 个测试；覆盖 V4 migration/dedup/idempotence、retired/unknown direct model 的 user-selection/default/Project/Command/Quick Ask fail-close、High→high 与 XHigh→max、Low/Medium 拒绝、thinking/sampling/UI/Project 动态归一、fallback 最终模型与非法 base temperature 区分、production profile/parser identity、exact credential precedence/accessor rejection、eager zero-fetch preflight/close、Profile/Transport/Adapter/Compiler private brand 防伪、Compiler 与 Queue executor error 的 exact attempt signal 防重放，以及 400/401/402/422/429/500/503/未知 HTTP → durable fail/retry/rate-limit pause 矩阵和 network/invalid response 脱敏。通过 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build 与 `git diff --check`；未运行真实 DeepSeek 网络 smoke、`main.ts` 插件级生命周期自动化或 Windows Obsidian native-fetch/CORS/proxy/abort/reload 实机验收，production worker/Gate/Queue/Review/Wiki 仍未接线，`.env.test` 与真实 API key 未进入提交。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2p generation-owned preflight 与 recovery-only production Gate 最终通过核心 composer/port 2 个聚焦 Jest suite / 24 个测试，以及全仓 197 个 Jest suite / 3510 个测试；覆盖构造/preflight 同步重入、exact frozen admission、Settings/Projects/Runtime/unload 失效、one-shot 顺序/并发防重放、Vault-global owner-first、各 durable phase 间 generation 复证、已进入 apply phase 的 crash-safe 完成、attention/blocked/unavailable 脱敏、真实 authorized prepared journal → Wiki/Manifest/ledger/Queue/journal 全收敛、外部文件冲突零覆盖并持久化 `recovery_required`，以及无 Release/worker/model/new transaction/no-journal 自动 action。通过完整 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build 与 `git diff --check`；未运行真实 DeepSeek 网络、Windows native exclusive-create/NTFS/OneDrive/crash 实机验收，组合测试的 Runtime file 为内存实现且 exclusive creator 为可观察 mock。既有 keychain/解密预期 console 输出不影响结果，`.env.test` 与真实 API key 未进入提交。
- [x] G.2q-a listener-first observation startup Core 最终通过 watcher/Runtime/reconciler/coordinator 4 个聚焦 Jest suite / 187 个测试，以及全仓 199 个 Jest suite / 3555 个测试；覆盖 listener-before-crawl、startup missing/case drift/Windows collision/capture failure/destructive-event quarantine、allocated/bound exact restart reconciliation、成功 crawl supersession、residual blocker、real Runtime/Queue convergence、allocated Release blocker、plan replacement/getFiles 同步重入、非可治愈 pre-crawl failure 零 crawl、diagnostic 同步退役、generation assertion 失效关闭，以及 error-code allowlist/WeakSet brand/freeze 防伪。通过完整 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build 与 `git diff --check`；未接 `main.ts`、conditional Release、worker/model/Review/Wiki，未运行真实 DeepSeek 网络或 Windows Obsidian 实机验收。既有 keychain/解密预期 console 输出不影响结果，`.env.test` 与真实 API key 未进入提交。
- [x] G.2q-b 同代 workflow lease 与 production observation composer 最终通过 4 个聚焦 Jest suite / 68 个测试，以及全仓 200 个 Jest suite / 3581 个测试；覆盖 authentic shared ProfileSource 对本次输入 snapshot 的独立等价复证及 subclass override 拒绝、detached owner 与 owned parser capability、同步 invalidation、真实 Runtime Manifest/schema/source 双采集、listener-first crawl、startup-held enqueue-only Queue、启动后 modify → revision 2 收敛、重复 quiesce/reconcile/blocker reproof、同步 health、外层 generation 在最终 blocker inspection 中失效、rename 与 lease replacement 关闭。通过完整 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build、文档 Prettier 与 `git diff --check`；没有接 `main.ts`、conditional Release、`runNext`、worker/model/Review/Wiki，也没有运行真实 DeepSeek 网络或 Windows Obsidian 实机验收。既有 keychain/解密预期 console 输出不影响结果，`.env.test` 与真实 API key 未进入提交。
- [x] G.2q-c plugin-owned observation session 接入通过 4 个聚焦 Jest suite / 72 个测试，以及全仓 200 个 Jest suite / 3583 个测试；覆盖 recovery clear 后才启动 observation、完整 convergence result 校验、observation blocker/diagnostic fail-closed、live watcher 保留、startup generation/cancel/Settings/Projects/Runtime replacement/unload 关闭，以及 enqueue-only Queue 不升级为 worker。通过 TypeScript `noEmit`、全仓 ESLint、全仓 `format:check`、production build、文档 Prettier 与 `git diff --check`；未运行真实 DeepSeek 网络或 Windows Obsidian 实机验收，conditional Release、worker/model/Review/Wiki 仍未接线。既有 keychain/解密预期 console 输出不影响结果，`.env.test` 与真实 API key 未进入提交。
- [x] G.2r observation → fresh Gate → conditional Release 接线通过 2 个聚焦 Jest suite / 37 个测试（含 Barrier/recovery composer）；覆盖 convergence 后同 signal reproof、fresh per-Bundle Gate、Runtime 原子 release、release blocked/observation drift/generation close fail-closed 与 released Queue 不开放 worker。通过 TypeScript `noEmit`、全仓 ESLint、文档 Prettier 与 `git diff --check`；production build/full regression 在本轮最终提交前重跑，未运行真实 DeepSeek 网络或 Windows Obsidian 实机验收。
- [x] G.2s bounded worker session Core 通过 1 个聚焦 Jest suite / 3 个测试；覆盖未 release 不 claim、稳定 Bundle 顺序单次 pass、并发/关闭/代际失效拒绝。通过 TypeScript `noEmit`、变更文件 Prettier 与 `git diff --check`；尚未接 production Queue executor、真实模型或 Windows Obsidian 实机。
- [x] G.2t exact preparation bridge 通过 preparation/observation/worker 3 个聚焦 Jest suite / 9 个测试；覆盖 authentic plan/proof 依赖边界、结构伪造和 accessor 零调用拒绝、既有 observation convergence/reproof 与 worker release gate 回归。通过 TypeScript `noEmit`、ESLint 与变更文件 Prettier；真实 Compiler/DeepSeek/Review handler 和 Windows Obsidian 实机仍待后续。
- [x] G.2u route lease/target resolver 通过 preflight/lifecycle/route-lease/target-resolver 4 个聚焦 Jest suite / 52 个测试，并连同 exact-signal model/DeepSeek 回归达到 6 suite / 112 tests；全量单元回归 204 suite / 3609 tests。覆盖 preflight 零网络 route retention、真实 Queue preparation 上的 no-change/proposal 与单次 reservation、opaque lease 同步撤销/迟到结果拒绝与 credential canary 隔离、exact renderer fetch capture、Windows case alias/collision、create-only 零内容读取、authorized canonical read、folder/missing、index/stat/read/owner drift、恶意 accessor 与取消。通过 TypeScript `noEmit`、全仓 ESLint、Prettier、production build 与 `git diff --check`；尚未运行真实 DeepSeek 网络、worker/Review hand-off 或 Windows Obsidian 实机，pending native fetch 仍须由 Queue-owned controller 在启用 worker 前中止。
- [x] G.2v branded compile attempt/input 与 dependency payload hardening 通过 Compiler security/default/model-adapter、production input/route-lease、Queue 6 个聚焦 Jest suite / 165 个测试，以及全仓 205 个 Jest suite / 3620 个测试。覆盖 exact job/claim/signal/reporter、复制或 accessor context、单 generation builder、错误 profile 不烧毁 preparation、派生 text/Markdown/PDF evidence、2048 evidence/256 authorization 精确边界、Manifest co-owner write-only authority、createdAt、跨 lease、顺序/并发 build/compile 重放、close/stage drift 零模型调用，以及 resolver/validator accessor 的零 getter data-only snapshot。通过 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build、secret scan 与 `git diff --check`；未运行真实 DeepSeek 网络、production worker/Review hand-off 或 Windows Obsidian 实机，近 8M 全文 source 的最终 model-context 预算与 Queue-owned native fetch abort/unwind 仍是启用 worker 前的显式 blocker。
- [x] G.2w production candidate validator 与 compile → durable Review hand-off 最终通过 validator/handler 2 个聚焦 Jest suite / 46 个测试，以及全仓 207 个 Jest suite / 3666 个测试；覆盖 bounded descriptor-only snapshot、schema/context/target hash、analysis→bound target→draft change/citation/sourceRefs 关系、parser-owned locator material、YAML→OKF、raw HTML/Markdown/wiki/autolink/bare scheme/www/email linkless fail-close、delete fail-close、proposal/plan/digest/exact job claim 持久化、pending receipt、deterministic Review conflict 非重试、未知依赖脱敏、AbortError 不冒充 Queue cancellation 及稳定 `no_changes` identity。真实 DeepSeek production integration 以 exact Queue-owned signal 完成 2 次 HTTP 200 的 analysis → generation，从 Runtime observation/claim/workflow/parser/resolver/validator 到同 Runtime `awaiting_review`，并证明 Manifest 与无写 capability 的 Wiki fixture 保持不变；一次独立生成请求的真实 503 也被安全分类为 `knowledge_provider_unavailable`，随后全新 attempt 通过。通过 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build、secret scan、`.env.test` ignore 检查与 `git diff --check`；API key 未进入源码或提交。该阶段尚未接 `main.ts` worker，Runtime A/B brand、Queue cancellation/native fetch unwind 与 Windows Obsidian 实机是当时的后续门槛；G.2x 已关闭 main worker 接线、brand 与 cancellation，existing→update 真实网络组合及 Windows 实机仍待完成。
- [x] G.2x Runtime owner、exact Queue/native-fetch cancellation、production worker controller 与 `main.ts` 接线最终通过全仓 209 个 Jest suite / 3694 个测试；覆盖 Queue exact-claim unwind、跨代 claim 防回写、Review durable-write 后 hand-off、同 Runtime owner brand、controller 异常退避/限流唤醒/有限 drain、真实 EventSink 二次 source observation、旧代 close-and-drain startup 屏障与 renderer timer 归属。真实 DeepSeek production integration 以 2 次 HTTP 200 完成 analysis → generation → durable Review / `awaiting_review`，并证明 preflight 零网络、Manifest/Wiki 未写。通过 TypeScript `noEmit`、全仓 Prettier、全仓 ESLint、production build、secret scan、`.env.test` ignore 检查与 `git diff --check`；API key 未进入源码或提交。existing→update 真实网络组合及 Windows Obsidian 专用测试 Vault 实机验收仍由下方独立项目跟踪。
- [x] G.2y Electron native-module loader 修复新增 1 个 suite / 8 个测试，总回归达到全仓 210 个 Jest suite / 3702 个测试；另通过全仓 Prettier/format check、TypeScript production build、ESLint、构建产物动态 Node import 检查和 Windows Obsidian CLI 实机。专用 Vault 证明 Runtime v3 初始化、真实 DeepSeek 两阶段 HTTP 200、durable `awaiting_review`、reload 零重复请求、plugin disable exact abort、`startup_recovery` 与 re-enable attempt 2 收敛；Source/Schema 基线、Wiki 数量/字节及对应 Manifest 摘要按各阶段预期保持不变，API key 未输出或写入仓库。
- [x] G.2z-a/b/e live read-only Studio 最终通过全仓 212 个 Jest suite / 3732 个测试、全仓 ESLint、TypeScript production build、Prettier 与 `git diff --check`；真实 DeepSeek production integration 继续以两阶段调用通过。Windows 专用 Vault 冷重载后发布 `workflow_read_ready`，Activity 从 Runtime revision 53 显示 3 条 durable job，Review 从同一快照显示 2 条 exact anchored pending proposal；Activity mutation 与 Review decision 按钮全部禁用。临时外部创建目标会立即投影为 `occupied / reject_only / review_create_target_occupied`，删除后恢复 `current / blocks_allowed`；前后 Wiki 均为 1 文件 / 126 字节，Runtime SHA-256 不变。主窗口与真实 popout 均读取同一 durable revision，disable/enable 后重新恢复相同 pending Review，Obsidian `dev:errors` 全程为空。另补 Barrier observation session 身份保护，旧代异步完成不能关闭新代 live observation。
- [x] G.2z-c exact Activity/literal Reject 最终通过全仓 214 个 Jest suite / 3780 个测试、全仓 ESLint、Prettier、TypeScript production build 与 `git diff --check`；最终真实 DeepSeek Knowledge Compiler production integration 1/1 通过（其余 7 个非目标 integration case 按配置跳过）。Windows 专用 Vault 中 Pause `r95/q57 → r96/q58`、Resume `r108/q62 → r109/q63` 均只推进一次目标 Queue command；user pause 跨 disable/enable 保持且可恢复。whole-proposal Reject `r109/q63/v2 → r110/q64/v3`，精确把目标 Review 置为 rejected revision 1、job 置为 cancelled、移除 pending anchor 并追加 rejection tombstone；重载后仍 terminal。Wiki 继续为 1 文件 / 126 字节且 SHA-256 不变，Manifest、active transaction、input revisions、apply ledger 在 Reject 前后哈希不变，Obsidian `dev:errors`/error console 为空。Cancel/Retry 的 exact、stale、commit-then-throw 与 lifecycle 路径由真实 Queue adapter 自动化覆盖；实机 fixture 无 eligible job，UI 正确隐藏动作且未手工篡改 Runtime。另补 stale high-watermark Retry eligibility，避免为必然被 Queue 拒绝的旧输入显示 Retry。
- [x] G.2z-d reviewed Apply/Recovery 最终通过 6 个聚焦 Jest suite / 71 个测试及全仓 220 个 Jest suite / 3840 个测试，并通过真实 DeepSeek Knowledge Compiler production integration 1/1、TypeScript `noEmit`、`npm run format`、全仓 `format:check`、ESLint、production build 与 `git diff --check`。Windows 专用 Vault 在完整应用重载后完成 fresh create，目标 SHA-256 `e0717d27125235a5fcd50792c2d7ce2fd86954f4e53ed6256b84a9f87e706e31`，Manifest `2 → 3` 且只产生一条 exact ledger；随后以真实 DeepSeek proposal 和 **Accept block 1** 完成 update，目标/Review/Manifest SHA-256 同为 `5c829b462dd774d5471791d719c49676b1a3e516b38bc41f5f1e32260ba52210`，Manifest `3 → 4`、总 ledger 为 2。外部修改目标后，Review 立即投影 `stale / reject_only / review_before_hash_changed`，Accept 禁用且 Wiki、Manifest、ledger、journal 均无误写；恢复先前接受字节后重新可审。随后用只拦截目标 ChangeSet 的 `activeTransaction: null → prepared` Runtime transform、连续两次抛错并自动还原的受控 wrapper 制造 no-journal 窗口；wrapper 命中恰好 2 次，未篡改 Runtime，系统持久化 accepted Review、exact apply claim、`failed/applying` job 与 `recovery_required` pause，同时保持旧 Wiki hash、Manifest 4、零新增 ledger 和空 journal。Recovery UI 显示 `Decision required`，从真实 **Continue** 按钮收敛到 `r261/q144/v8/manifest5`、`completed/completed`、目标 SHA-256 `d433f6af28c4f26df7cf6c2867214a6bc79ca2b00ec7be02d967ff10771da3e8`、该 ChangeSet 一条 ledger/总计 3 条、无 claim/marker/active transaction。完整 Obsidian `app:reload` 后为 `r267/q146/v8/manifest5`，Wiki hash 与 ledger 数量不变，Review 为空、Recovery 页消失，`dev:errors` 与 error console 均为空。开发部署时发现 plugin disable/enable 会复用旧模块缓存，因此本轮以完整应用重载作为新构建与冷启动证据；DeepSeek Pro 的一次 strict invalid response 和 Flash 的 `no_changes` 都未产生 Review/Wiki 写入，最终提案链路使用已启用的 Flash profile 完成。API key 未输出或写入仓库。
- [x] H.1 scoped Query/Markdown citation jump 完成代码、自动化与 bounded Windows 交互门禁：Runtime 单-envelope projection 只保留 current Manifest + latest exact ledger + accepted Review 精确写出的当前 page version；Reader 对当前 Wiki bytes 做 SHA-256 与 Runtime revision sandwich 复证；纯内存检索只复用 Search v3 的中英文 tokenizer，不依赖全局设置、模型、mtime、路径重读或全 Vault fallback。Knowledge Studio 仅暂存 grounded excerpts，React 只回传 current opaque citation ref；Markdown line/heading occurrence/unique quote 在 source hash 复证后通过 captured App/Vault/Workspace 打开，PDF 在该 H.1 checkpoint 当时明确 disabled。换代、刷新、hint、取消或 close 会撤销 UI authority 与晚到发布；精确撤销不会误伤另一视图的新查询，Runtime/Vault 权威 hint 则会先同步撤销整个 Bundle 的旧引用再刷新。生产 Query capability 被一次性不可变捕获且限制在对应 Bundle source roots。15 个聚焦 suite / 327 tests、全仓 228 suite / 3926 tests、真实 DeepSeek production integration 1/1（其余 7 项按缺失的非目标 provider 配置跳过）全部通过；同时通过 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build、文档 Prettier、secret scan 与 `git diff --check`。独立 Windows Vault 以完整 `app:reload` 载入生产构建后发布 Query/Activity/Review；`NATIVE-ACCEPTANCE-20260804` 与 `durable provenance` 两次真实表单查询都命中 `Wiki/Acceptance/Native Knowledge Compiler.md`，首个 opaque quote citation 打开 `Sources/Acceptance/Abort Source.md` 并选择 `0:0 → 17:251`。第二次查询前后 Runtime/Wiki/Source SHA-256 分别保持 `e60f9c1f28ad821fcd105055e1802e6e87925a4bdaae446961131e3b4abbe10d`、`d433f6af28c4f26df7cf6c2867214a6bc79ca2b00ec7be02d967ff10771da3e8`、`6701bbaf1659d0ad680933e63dd3a670d4a474fc742c807bb5b50c4f26f4f373`，Runtime revision 273 / Manifest revision 5、无 active transaction，`dev:errors` 为空。该 H.1 checkpoint 当时尚未接模型综合，Save to Wiki 与 PDF page jump 也仍 disabled；两者现均已由 H.3 接通代码、自动化与 Windows 端到端验收。
- [x] H.1 lifecycle hardening 实机复验通过：Windows 测试 Vault 精确部署新构建后完整 `restart`，重新创建延迟恢复页签对应的真实 Studio view；旧 query/citation ref 在显式 refresh 后由生产稳定端口以脱敏 `KnowledgeScopedQueryError` 拒绝，新查询仍命中同一 applied Wiki 且新引用继续打开 `Sources/Acceptance/Abort Source.md`、选择 `0:0 → 17:251`。严格查询前后 Runtime/Wiki/Source SHA-256 分别保持 `b5cc159bfb21eca04aeb7985b35922fa4d7e90a18fb988c1baef8adf572dc9fa`、`d433f6af28c4f26df7cf6c2867214a6bc79ca2b00ec7be02d967ff10771da3e8`、`6701bbaf1659d0ad680933e63dd3a670d4a474fc742c807bb5b50c4f26f4f373`，Runtime revision 279 / Manifest revision 5、无 active transaction，`dev:errors` 与 error console 为空。
- [x] H.2 grounded answer 完成代码与自动化发布门禁：Core 使用 authentic request + context digest，把 applied-Wiki chunk 与 exact Markdown source excerpt 分层，模型只能返回 request-local evidence id；source fact/inference/insufficient 采用 strict JSON，无自由 summary。Production preflight 在同一 lease 内持有独立 Compiler/answer route，answer exactly-one non-streaming DeepSeek 请求且无 fallback/repair/internal retry；owner close 主动 abort 在途请求并清除 answer route。模型后完整 Wiki/source provenance 与实际使用 locator 均二次复证，任何 drift/取消/换代都不发布，React 仅渲染普通文本与 opaque citation。12 个聚焦 suite / 138 tests、全仓 231 suite / 3949 tests 全部通过；真实 DeepSeek production integration 2/2 通过（H.2 answer 1 次 HTTP 200 + Compiler analysis/generation 2 次 HTTP 200，其余 7 项按非目标配置跳过）。同时通过 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build、文档 Prettier、secret scan、`.env.test` ignore 检查与 `git diff --check`；API key 未进入源码或提交。
- [x] H.2 Windows bounded acceptance 通过：Obsidian 1.13.4 的独立 `native-acceptance` Vault 以当前 production bundle 发起真实 DeepSeek Query，依次显示 Supported、Partial 与 deterministic Insufficient evidence，并渲染 Source fact、Inference 和 Evidence still needed。首个 opaque citation 打开 `Sources/Acceptance/Abort Source.md` 并精确选择 `0:0 → 17:251`。无命中查询在受控内存 fetch 探针下保持 `calls=0`；source-backed 在途查询于 plugin reload 前为 `calls=1/aborts=0/forwarded=0`，reload 后为 `calls=1/aborts=1/forwarded=0`，旧答案未发布。稳定查询前后 Runtime/Wiki/Source SHA-256 分别保持 `c10f52d67d61ec9dfa4041593482eb81df68a2e09f0a1bd6b5c858f14a654ebd`、`d433f6af28c4f26df7cf6c2867214a6bc79ca2b00ec7be02d967ff10771da3e8`、`6701bbaf1659d0ad680933e63dd3a670d4a474fc742c807bb5b50c4f26f4f373`，Runtime revision 291、active transaction 为空。探针随后恢复并删除，最终完整 restart 后插件启用、grounded Query 重新发布、探针为 `undefined`，`dev:errors` 与 error console 均为空；冷启动按设计追加 same-content observation，故 reload 跨度不作为 Query 零写入哈希证据。
- [x] H.3 生产代码与自动化覆盖 Vault Markdown/text/PDF Chat intent chooser、byte-only PDF page parser、raw-byte-verified PDF page jump，以及 Save-to-Wiki managed capture → exact `managed_copy` Manifest registration → watcher/Queue → Compiler `query_writeback` → Review → transaction → Apply 链路。自动化同时证明普通 Query 仍只读、Save 无直接 Wiki 写权、不伪造 Queue receipt、不绕过显式 Review，且换代/刷新/持久状态变化会撤销旧操作权限；Windows create 还会在 durable acceptance 前复证父目录存在，不会隐式创建未 journal 的目录。
- [x] H.3 Save-to-Wiki Windows 端到端验收：真实 DeepSeek managed capture → `query_writeback` Review → explicit Apply → startup recovery → Manifest/Queue/ledger finalize 全链通过，Save/Review 前 Wiki 零写入、旧页零误写、最终目标 hash/ledger/Manifest identity 一致且 clean console。
- [x] H.3 Chat chooser Windows 端到端验收：两种 intent、选择前零副作用、one-use Chat-only context、durable Manifest/Queue/Activity、重复注册字节级幂等与真实 Cancel/Resume 清理全部通过。
- [x] H.3 最终自动化发布门禁：全仓 242 个 Jest suite / 4050 个单元测试通过；真实 DeepSeek integration 2 个目标测试通过、7 个非目标 provider 测试按配置跳过；TypeScript `noEmit`、`npm run format`、源码与文档 Prettier check、全仓 ESLint、production build、`git diff --check`、`.env.test` ignore 与变更集 secret scan 全部通过。真实 API key 未进入源码、日志或提交。
- [x] H.3 PDF Windows 端到端验收：全新独立 Vault 中真实 PDF `Add to Knowledge` → Manifest → watcher/Queue → DeepSeek Compiler → Review/Apply → applied provenance 全链完成；source raw hash 与 1/3/5 页 locator 一致，proposal afterHash 与最终 Wiki hash 一致。主窗口与 popout 的首/中/末页和重复跳转均使用 canonical `#page=N` 且打开前后复证 exact bytes；source drift 只允许一次失败复读而零打开，query 换代和 plugin reload 后旧引用均零读零开，新代引用继续可用。恢复 source 后 Queue 为 Running、全部 job terminal、active transaction 为空；探针移除，开发者错误与 warn/error console 为空。
- [x] Durable `no_changes` / Runtime v4 自动化发布门禁：10 个核心 suite / 386 tests、全仓 243 suites / 4106 个单元测试全部通过；真实 DeepSeek integration 2 个目标测试通过、7 个非目标 provider 测试按配置跳过。同时通过 TypeScript `noEmit`、`npm run format`、源码/文档 Prettier check、全仓 ESLint、production build、`git diff --check`、`.env.test` ignore 与变更集 long-`sk-` scan；真实 API key 未进入源码、日志或提交。
- [x] Durable `no_changes` / Runtime v4 Windows 实机：Obsidian 1.13.4 的隔离 `pdf-acceptance` Vault 先将无 proof 的 v3 当前 completion 保守迁移并重跑，Runtime `v3/r57/Manifest2/input8 → v4/r74/Manifest3/input11`；新 `all_targets_unchanged` marker 精确绑定 completed job/id/revision/attempt/source/hash/pipeline，Review、`lastSuccessful`、Apply ledger、Wiki、source 与 schema 全部不变。随后以无语义 Schema 注释产生新 fingerprint，DeepSeek endpoint 计数恰好 2，attempt 1 以 Manifest 9 原子持久新 marker，无 Review/Wiki/ledger 变化；热 reload `input27→28` 与冷 disable/enable `28→29` 均为零模型请求、job/Manifest/marker/Review/ledger/Wiki/source/schema digest 不变。最终 `v4/r179/input30`、Queue Running、无 active job/rerun/pending Review/transaction，探针已恢复并 reload 回 native fetch，脱敏 `dev:errors`/error console 无 Knowledge/uncaught/credential-shaped 命中。证据保存于 `%TEMP%\copilot-nochanges-20260810-072850`，无原始内容、请求、响应或密钥。
- [x] G.1 自动化回归覆盖完整文件排他发布、并发初始化、realpath/symlink containment、共享 envelope 无丢失并发更新、跨 runtime revision 续号、watcher-capture 乱序拒绝、全 slot corruption fail-closed、create/update CAS 竞争与 delete replay 分类。
- [x] Production output freshness 自动化发布门禁：Runtime exact Apply/no-change authority、hash-only bounded output reader、authority sandwich、cold no-change reuse、live output reverse-index、shared owners、terminal/processing/awaiting-review repair、Apply fence、成功 Apply 后 repair/rerun 与 crash-recovery successor 收敛、stale-output `no_changes` 防循环 blocker、identity/I/O fail-closed 及资源上限均有回归覆盖。Queue 聚焦 suite 的 107 个测试、Runtime/Handoff 跨重启回归及全仓 246 个 Jest suite / 4187 个单元测试全部通过；真实 DeepSeek integration 2 个目标测试通过、7 个非目标 provider 测试按配置跳过。TypeScript `noEmit`、`npm run format`、源码与文档 Prettier check、全仓 ESLint、production build、`git diff --check`、`.env.test` ignore 与不回显内容的变更集 secret scan 全部通过；真实 API key 未进入源码、日志或提交。
- [x] Production output freshness Windows 实机：隔离 `pdf-acceptance` Bundle 的 intact-output cold scan 与 plugin reload 均为 DeepSeek endpoint `0`，10 个既有 job、0 Review，reload 只把 Queue revision `130 → 131`。删除一个 401-byte accepted output 后产生 `2` 次真实 compiler 请求，source input revision `26 → 45`，进入唯一 `awaiting_review` 且没有自动写回；生产端 Reject 未增加模型请求。恢复 exact bytes 后再次为 `0` 请求，最终 Queue Running、0 active/rerun/Review/transaction，Manifest revision 仍为 `9`，Wiki/source/schema/applied provenance/freshness digest 全部回到基线，目标 SHA-256 恢复为 `c1278d61…21c6`，探针和 raw backup 已移除，`dev:errors` 与 error console 无 Knowledge/uncaught/credential 命中。最终证据保存于 `%TEMP%\copilot-output-drift-20260810-113213`；byte-change Windows 实机仍未冒充完成，其安全语义由自动化覆盖。
- [x] Folder-only import 代码与自动化接线：Knowledge Studio 显式文件夹 picker、stable/revocable production port、唯一 Bundle/source root、有界全选择预检、Windows path/case collision 拒绝、逐文件 exact-byte 排他发布、`managed_copy/folder_import` Manifest 注册、partial/resumable receipt、批次单次 generation refresh 与 popout-safe picker 均已接通；不保存外部绝对路径，不赋予 worker 自动 Review/Apply 权限。
- [x] Folder-only import 自动化发布门禁：全仓 251 个 Jest suite / 4233 个单元测试全部通过；真实 DeepSeek integration 2 个目标测试通过、7 个非目标 provider 测试按配置跳过。TypeScript `noEmit`、`npm run format`、源码与文档 Prettier check、全仓 ESLint、production build、`git diff --check`、`.env.test` ignore 与不回显内容的变更集 secret scan 全部通过；真实 API key 未进入源码、日志或提交。
- [ ] 继续收紧 folder import 的本地 namespace 竞争：当前在 temp open、write 与 hard-link 前后反复复证 source root/final parent 的 lstat+realpath，并覆盖父目录被并发换成 symlink 的清理；Node 路径 API 不提供 openat/目录句柄相对 hard-link，最终 link 调用瞬间的 junction swap 仍归入 Windows 双实例/本地对抗 race 门禁，不宣称已证明完全消除。
- [x] Folder-only import Windows production-port 实机：以最新 build 做 artifact-exact 真 unload/enable 后，通过 renderer 的真实 `File` capability 调用 production stable port。`D:\my_idea` 首次导入 5/5、628,205 bytes、0 conflict/failure，Activity `11 → 16`，新增 5 个 job 全部 completed，active/failed/recovery 均为 0；模型均证明 `no_changes`，因此正确保持 Review 0，未虚构 Accept/Apply。exact retry 为 0 imported / 5 reused / 0 bytes，Activity 不增长且没有新 Queue/model work；受控删除一个目标后重试为 1 imported / 4 reused，随后 exact restore，最终 Vault 目标为 5 文件 / 628,205 bytes、private temp 0。外部目录导入前后 count/bytes/mtime/attributes、相对 identity 与内容 aggregate hash 完全一致，safety/error 为 0；Studio 的 **Import folder**、Activity、Review 表面均正常，console、developer errors 与 cleanup warning 均为 0。
- [ ] Folder-only import 剩余 Windows 交互门禁：production stable port 已通过，但尚未人工点击物理系统 folder picker 选择目录；本次模型结果全部为 `no_changes`，所以也未出现可供显式 Review/Apply 的非空提案。后续用物理 picker 手点复验，并在有真实 Wiki 变更提案时完成显式 Review/Apply；Windows 不同字节目标的冲突不覆盖仍保留为单独实机对抗门禁，自动化覆盖不冒充该实机结果。
- [ ] 首批功能实现后，在 Windows Obsidian 测试 Vault 中完成 Golden Flow 实机验收。

## 中文使用手册（2026-08-11）

### Session Goal

为当前核心 production port 与独立 Review/Apply、Query、Save、PDF、Recovery 链路已通过有界 Windows 实机、但完整 Golden Flow 和部分物理交互仍待现场复验的 Personal Knowledge Studio，编写一套可直接在 Obsidian 打开、仅依靠文档即可操作和审阅的中文用户手册；把用户操作、长期规范、隐私和故障恢复与工程架构/验收日志分离。

### Architecture Decisions

- 用户手册放在 `docs/knowledge/zh-CN/`，采用一页入口与按任务拆分的 Markdown 页面；相对链接同时适用于 GitHub 和 Obsidian。
- `docs/personal-knowledge.md` 继续保留稳定英文入口和详细技术边界，只在顶部链接中文手册；Runtime/CAS/事务证明和历史实机数据不复制进普通用户步骤。
- 每个操作页统一包含目标、前置条件、步骤、预期结果、异常处理、数据/网络/费用和相关页面。
- 当前产品声明限定为 Windows Obsidian Desktop、单人、一个有效 Bundle、一个 sourceRoot、DeepSeek V4、显式 Review/Apply；持续文件夹同步、OCR、自动 Apply、Delete、多 Bundle、跨平台和双实例不写成支持能力。
- 手册明确 `no_changes` 是正常成功、文件夹导入是一次性 managed snapshot、Query 只使用已应用知识、`Save to Wiki` 仍需重新经过编译和人工审核。

### Task Tracking

- [x] 完成现有 docs、代码 UI 标签、Bundle/DeepSeek 配置与 Windows 验收边界盘点。
- [x] 新建中文手册首页、维护恢复、长期规范、隐私安全、故障排查和参考手册。
- [x] 新建首次使用、安装模型与 Bundle 配置页面。
- [x] 完成来源导入、Studio/Activity、Review/Apply、Query/Save-to-Wiki 日常工作流页面并做准确性复核。
- [x] 更新 `docs/index.md`、`docs/personal-knowledge.md` 与 README 的中文手册入口。
- [x] 完成全手册 Markdown 格式、相对链接、支持声明、敏感信息和差异门禁。
- [x] 在手册首页增加大白话功能介绍，说明普通 Chat 能产生什么，以及 Chat、Add to Knowledge、Query、Save to Wiki、Review/Apply 的不同产物。
- [x] 澄清 `sourceRoot` 目录与已登记 `Sources` 的区别，并解释当前单文件 `Add to Knowledge` 为什么位于 Chat。
- [ ] 由用户在 Windows Obsidian 中按手册进行现场审阅：物理点击文件夹选择器、观察 `no_changes`，并用确定产生变更的材料完成非空 Review/Apply。

### Testing Checklist

- [x] `npx prettier --check` 覆盖全部新手册及入口文件。
- [x] 所有本地 Markdown 链接解析到仓库内真实文件。
- [x] 手册中无真实 API Key、个人绝对路径、私有 fixture ID 或请求/响应正文。
- [x] 不把未验收的物理 picker、非空 folder Review、不同字节 Windows 冲突或完整 Golden Flow 冒充已完成。
- [x] `git diff --check` 通过，变更范围只包含预期 Markdown 文档。
- [x] 复核新增首页说明与 production Chat capture、folder import、Query writeback 和 Review/Apply 行为一致，并重新运行文档格式、链接与差异检查。
- [x] 将最新版 14 篇中文手册同步到 Windows 测试 Vault；源与目标逐文件哈希一致，127 个本地链接全部可解析，临时备份已清理。

## Knowledge Source Lifecycle（2026-08-11）

### Session Goal

让已注册 Knowledge Source 的删除、误删和恢复变成可解释、可操作、可审计的来源生命周期：一个来源缺失不得再让整个 Knowledge Studio 变成不可用；用户可以在不删除现有 Wiki 文件的前提下恢复精确原路径或安全停止跟踪。

### Architecture Decisions

- Obsidian/Windows Explorer 没有可靠的统一 before-delete hook；插件自己的移除入口必须事前确认，原生或外部删除则在事后立即进入持续的 Source Missing 恢复流。
- `source_missing` 和 delete/rename 派生的可恢复 blocker 只隔离受影响来源；`capture_failed`、Windows 大小写/路径冲突和未收敛的 fatal durable observation 仍 fail closed。
- 恢复必须回到原 Vault 相对路径并重新通过 exact-byte observation；`Check again` 不创建、复制、移动或覆盖文件，也不会把 Windows 回收站误写成插件可直接操作的权限。
- 已打开的 Sources 通过同一代 issue hint 自动收敛 `Ready → Missing`，精确原路径恢复且 capture settle 后再收敛 `Missing → Ready`。
- 同一 Bundle 的 recoverable missing + exact `source_observation_pending` 冷启动状态只发布 least-authority Sources-only pre-release surface；不放行 worker/model/Query/Review/Apply/Wiki capability。
- 安全移除是 Runtime v5 owned 原子操作，不直接调用通用 `SourceManifestRepository.removeSource()`；protected tombstone 保留退役证明和 revision floor，只撤销 active Manifest membership/provenance 并保留 Queue/Review/Apply ledger 历史，不自动删除来源或 Wiki bytes。
- 目标来源的 allocated/bound observation 在退役提交中原子 terminalize；真实 active Queue、pending Review/Apply、transaction、rerun 或 Recovery 工作仍必须先收敛。
- 正常 generation 换代的 `waiting_for_layout` 只是短暂过渡：先撤销旧代权限，再显示中性 `Refreshing Knowledge Studio…`；刷新页不保留 Bundle、snapshot 或操作能力。红色 `Knowledge Studio unavailable` 只用于已判定需要处理的持久配置、Runtime 或 Recovery 等终态问题。

### Task Tracking

- [x] 定位误删导致 Studio 全局不可用的 watcher → startup blocker → Barrier 根因，并实机恢复被删来源后确认 Runtime/Queue/Studio 重新 Running。
- [x] 将可恢复的 missing/delete/rename 与真正的 fatal observation/path 错误分层，保留其他来源和 Studio 的可用性。
- [x] 实现 same-path restore 的同代解封、exact-byte 重观察、live Sources 刷新与旧 citation 撤销/新代签发。
- [x] 实现 Runtime v5 protected retirement tombstone 和降级 fence；原子撤销 active Manifest membership/provenance、terminalize 目标 allocated/bound observation，保留 Queue/Review/Apply ledger 历史且不改 Wiki bytes。
- [x] 实现 exact startup-pending 的 Sources-only pre-release recovery surface；只暴露 load/check/retire，不授予 worker/model/Query/Review/Apply/Wiki authority。
- [x] 在 Knowledge Studio 显示持续 Source Missing 状态、后果说明、Check again 和 Remove from Knowledge 操作；不提供任意新路径替换入口。
- [x] 为插件自有移除流程增加强确认，并在 Obsidian file menu 中对已注册来源提示“先从 Knowledge 安全移除”；不 monkey-patch Obsidian 原生删除。
- [x] 将正常换代明确呈现为中性、无操作的 `Refreshing Knowledge Studio…`，不再在操作间隙短暂渲染红色 unavailable。
- [x] 同步中文手册、技术边界和故障恢复文档，明确现有 Wiki 不会随 Source 自动删除。

### Testing Checklist

- [x] 自动化覆盖单文件 delete/rename、文件夹祖先菜单匹配和批量 issue；Studio 只隔离影响来源，已打开 Sources 自动刷新且一次同步批次最多一个通用 Notice。
- [x] 自动化覆盖精确原路径恢复（同/不同字节）、大小写别名、重复事件和重启收敛；新路径不能被静默改绑。
- [x] 自动化覆盖 Runtime v4→v5 downgrade fence、ready/applied source 退役、目标 allocated/bound observation terminalization、相邻来源继续可用、Queue/Review/ledger 历史保留、stale token/并发改名，以及 commit-then-throw 与重放幂等。
- [x] 自动化覆盖退役后旧 Query/citation 失效、零新模型请求、零自动 Wiki 删除，以及其他来源继续查询/编译。
- [x] 自动化覆盖 file-menu 提示、取消/二次确认、Settings/Projects/unload 换代与 stale authority 失效；提示明确不能拦截键盘或 Explorer 删除。
- [x] 自动化覆盖 live 代→中性 refreshing→同 Bundle 新代，并证明旧 load/action/query 在换代时被撤销；持久配置/Runtime/Recovery 问题仍收敛到红色 unavailable。
- [x] 聚焦来源生命周期、pre-release recovery、live issue hint、Runtime v5 退役与后续 Chat Draft 回归已通过；当前最终全仓单测为 262 个 Jest suite / 4387 个测试全部通过。
- [x] 完成最终 DeepSeek integration（2 passed / 7 skipped）、TypeScript `noEmit`、`npm run format`、`format:check`、全仓 ESLint、production build、`git diff --check`、敏感信息与个人绝对路径扫描；新增内容无真实凭据或本机路径。
- [x] 在 Windows Obsidian 测试 Vault 完成受控删除 → 单一 Notice → 已打开 Sources `Ready → Missing` → `Check again` 零重建/零覆盖 → 精确恢复 → `Missing → Ready` 的有界现场验收；Queue/Review/Apply/Wiki/data.json 均保持基线，临时探针与备份已清理。
- [x] 使用新建的一次性来源完成 Windows production import → terminal no-changes → Sources `Remove` → `Confirm removal` 有界现场验收：6 个真实来源未动，active `7 → 6`、protected tombstone `0 → 1`，来源与 Wiki 字节不被退役删除，Queue/Review/input 历史精确保留；随后只清理已退役的一次性文件和空目录，Studio 保持 6 Ready / 0 Missing。
- [x] 最新 production artifact 已在 Windows 测试 Vault 真 unload/enable；通过正式 generation invalidation 路径的 bounded DOM 观察确认 `Refreshing Knowledge Studio…` 出现、红色 unavailable 为 0，随后同一 Bundle 重新 selected/ready。前后 Queue jobs/rerun、Review、Manifest、Wiki 与 data.json 精确不变，observer、部署备份与临时脚本均已清理；这项程序化实测不替代下方用户人工观感验收。
- [x] 用户已在 Windows Obsidian 实际重走相关操作，并确认操作间隙不再出现造成失败误解的红色 `Knowledge Studio unavailable`，当前中性刷新观感正常；本项采用用户现场反馈作为 UX 验收证据，不扩大为其他未测物理交互或完整 Golden Flow 证据。
- [ ] 完成物理右键菜单提示的 Windows 人工现场验收；CDP 合成输入未能可靠打开原生菜单，当前只由自动化覆盖，且插件仍不能拦截键盘或 Explorer 删除。

## Chat → Knowledge Draft（2026-08-11）

### Session Goal

把普通 Chat 的理解、解释和总结结果接入受控知识流水线：用户可以从一条完整 AI 回复创建草稿，在提交前编辑并核对内容，然后把它作为新的 Knowledge Source 登记；该动作不得直接改 Wiki，必须先进入 Activity，并且任何 Wiki 写入都不得绕过 Review 或 Apply。

### Architecture Decisions

- 普通 Chat 继续是临时理解与写作工作台；只有用户显式点击并确认的完整 AI 回复才能进入草稿流程，流式中、错误消息和用户消息不具备该能力。
- 草稿在确认前必须允许编辑标题与 Markdown 正文，并明确标记为 AI 生成、需要对照原始材料核验；当前切片不伪造书名、页码、引用或来源事实。
- durable 成功仅表示在唯一 configured sourceRoot 下排他创建精确 Markdown 文件并完成 Manifest registration；Wiki 仍只能由后续 Compiler → Review → Apply 写入。
- 草稿路径必须由确定性内容摘要和 Windows-safe 固定前缀生成，不接受 UI 提供任意路径，不覆盖既有文件；精确重放收敛为复用，不同内容不能占用同一身份。
- 打开编辑器前由 stable port 生成并冻结精确 Bundle/sourceRoot capability，窗口显示该目标；换代或配置变化后旧 capability 必须拒绝，禁止把已确认内容静默改投新的 Bundle。
- 沿用 released generation 的 stable/revocable Chat capture capability、Manifest registration、generation refresh 与 Vault drain；失效 generation、取消和不完整 durable 边界必须 fail closed 或通过精确重放收敛。
- 提交中允许用户请求停止，但不得把 abort 描述成跨文件/Manifest 的事务回滚；界面要求先检查 Sources/Activity，durable 成功仍以真实 receipt 为准，相同内容可安全重试收敛。
- 产品投资遵循低成本 BYOK 路线：普通月度模型预算假设为不超过人民币 30 元，重度使用不超过 100 元；该数值是待实测预算而非已证明成本。当前不复制 Copilot Plus 的托管模型、计费、许可证或通用 Web Agent 后端。

### Task Tracking

- [x] 完成产品经理、项目经理、用户体验与 Copilot Plus 定位评估，并确定 Build / Reuse / Don't clone 边界。
- [x] 保存中文产品评估与 Plus 对比报告，并接入中文手册首页。
- [x] 定义 Chat Knowledge Draft 的 strict port、捕获格式、来源 origin 与脱敏错误语义。
- [x] 实现 AI 回复动作、可编辑确认界面、排他文件创建、Manifest 登记和单次 generation refresh。
- [x] 更新中文首页、Chat/来源工作流、Query Save 区分、隐私费用、技术边界与参考文档。
- [x] 在 Windows Obsidian 使用一条已保存的完整非错误 AI 回复进入真实 Chat UI，并在提交前把标题与正文完全替换为一次性无隐私草稿；实测 destination Bundle/sourceRoot 锁定、未确认时禁用、Create Source → managed `chat_knowledge_draft` Source → completed Activity / `no_changes`、零 Review/Apply、Wiki 与 `data.json` 精确不变。随后通过 Sources 的正式二次确认 Remove 退役，仅在 tombstone、路径、长度与哈希全部复证后删除一次性文件；Runtime 历史保留且无临时备份残留。本项不声称当前 Chat provider 成功联网生成了新回答。

### Testing Checklist

- [x] 聚焦 UI 单测覆盖完整 AI 回复入口、用户/系统/错误/空清理结果/流式消息无入口、隐藏 reasoning 清理、空白标题、正文编辑、显式确认、不可用目的地、重复提交锁、停止请求、失败保留编辑内容与卸载 abort。
- [x] 聚焦捕获/协调器单测覆盖标题/正文/严格 UTF-8 byte 边界、确定性 Markdown 与路径、精确重放、既有路径冲突、零覆盖、create→register、commit-then-throw、refresh failure、pre-publish abort、retained drain、长目标路径和 parser ambiguity。
- [x] 聚焦 stable-port 单测覆盖旧 generation 的 Add-to-Knowledge 在途 abort、Draft caller 预先 abort、打开时 destination capability 换代拒绝，以及 Draft durable commit 后换代仍返回真实 receipt。
- [x] 本轮 Chat Draft 聚焦门禁达到 7 个 Jest suites / 65 个 tests，并通过 TypeScript `noEmit` 与 scoped ESLint；全仓门禁仍由下项跟踪。
- [x] 新增 production-shaped 跨层测试：真实 Chat Draft registration 请求换代后，下一代 observation 进入真实 worker，并以 `no_changes` 收敛为 completed Activity；Review 为空、Source ready、Manifest no-changes marker 存在，且草稿创建本身仍不直接写 Queue 或 Wiki。
- [x] 最终聚焦门禁为 8 个 Jest suites / 78 个 tests，全仓单测为 262 个 suites / 4387 个 tests；TypeScript `noEmit`、Prettier、ESLint、production build、diff-check 与敏感信息扫描均通过。

## 产品二次复评（2026-08-12）

### Session Goal

在 Chat → Knowledge Draft、Source 生命周期、中性刷新和 14 篇中文手册完成后，重新从产品经理、项目经理和用户体验官视角评估当前产品阶段；只用 2026-08-12 可核验的 Copilot 官方英文页面更新 Plus 对比，并把下一阶段投资转成可执行的 Build / Reuse / Don't clone 与四周验证决策。

### Architecture Decisions

- 当前阶段定义为 **owner-operated personal production**：对已配置、愿意审核和维护备份的当前所有者可正式使用；对其他用户仍是受约束 private beta，不宣称公开 GA。
- 二次综合建议分为 6.8/10；分别保留 UX 6.6、已配置目标用户 7.5、首次普通用户 4.7、工程安全 8.8、核心闭环 7.6、商业成熟 4.2，避免用单一总分掩盖体验与工程差距。
- Plus 只按可核验英文定价、功能文档、隐私和条款评价；明确 V4 英文首页与现有定价/文档尚未完全对齐，多 Agent、Symposium、Miyo 只作为官方宣传方向，不冒充稳定权益。
- 当前 3.3.3 仓库边界限定为开源客户端中的 Plus UI、Agent/Projects/Composer/Miyo 连接与远端调用适配；不把 Brevilabs/Miyo 服务端、计费许可或官网宣传的 V4 多 Agent/Symposium 描述成仓库已证明包含的能力，也不把 semver 3.3.3 等同于产品代际 V3。
- 原定下一阶段中的首个本地 readiness 基础切片已在下一节实现，并通过已配置主窗口的 Windows 有界验收；随后优先 Review 修正与证据、Wiki 版本回滚、统一 Source 入口和 Draft 原始材料绑定。Web、OCR、Explorer 单文件、Graph、多 Agent、多人和跨平台保持后置。

### Task Tracking

- [x] 更新并保存二次产品评估报告，分开呈现产品经理、项目经理和用户体验官判断。
- [x] 纳入 Chat Draft、Source 生命周期、中性刷新、14 篇手册、262 suites / 4387 tests 与 Windows 有界验收带来的进步。
- [x] 保留物理 folder picker、不同字节 Windows 冲突、非空 folder Review/Apply、物理右键提示和完整 Golden Flow 的真实 pending 状态。
- [x] 更新 Copilot Plus 的 $14.99/月、$139.99/年、首次购买 14 天退款、公开月额度未知，以及 V4 首页与现有定价/功能文档尚未完全对齐的信息风险。
- [x] 记录 ¥30/月常规、¥100/月重度预算假设与 ¥60/小时时间价值算例；预算仍须由真实账单和净节省验证。
- [x] 定义 20 个真实来源、4 周本地验证，包括 activation、Review disposition、证据覆盖、7/30 天复用、每个接受页面成本和零未授权写入。

### Testing Checklist

- [x] 对二次评估报告和本段 TODO 执行 scoped Prettier check。
- [x] 验证二次评估报告中的 5 个仓库内 Markdown 链接全部可解析。
- [x] 执行 scoped `git diff --check`；本次复评子任务只编辑评估报告与 TODO，未修改代码或其他文档。
- [x] 将二次复评冻结后的 14 篇中文手册同步到 Windows Vault；文件名集合与逐文件 SHA-256 完全一致，127 个本地链接、0 broken，验证后清理同步备份并在 Obsidian 聚焦复评报告；未触碰 Sources/Wiki/Runtime/data.json 或插件产物，也未调用模型。

## Knowledge 设置与就绪检查（2026-08-12）

### Session Goal

把首次使用时分散的 Project / Bundle、Knowledge 模型和普通 Chat 模型状态汇总为 Knowledge Studio 内可理解、可操作的本地检查页；明确“本地已配置”不等于“服务在线”，并让用户只通过安全导航修正配置，不再从统一的红色 unavailable 页面猜问题。

### Architecture Decisions

- 采用 workflow vertical slice：本刀交付只读诊断、三个独立状态卡和安全导航；自动创建 Project、目录、Schema、写 Bundle YAML、切模型、写 API Key及联网 Test connection 留到后续独立切片。
- Readiness 是独立 presentation authority：只消费现有 Startup Barrier 的 typed、secret-free 状态与普通 Chat 的本地模型投影；不重新运行 preflight，不持有 workflow/model/Queue/Review/Apply 权限。
- Knowledge 与 Chat 分开判断。Chat 未配置只影响新聊天及从新回答创建 Knowledge Draft，不得把已就绪的 Knowledge 工作流判为不可用。
- 所有被动检查严格零模型/零 provider 网络；`workflow_read_ready` 只能表述为本地准入完成且网络尚未测试，不能显示 Online、Connected 或 API Key valid。
- 修正入口仅允许打开现有 Copilot Settings、Chat、当前重新解析的 project.md / Schema，以及通过 `Refresh displayed status` 刷新当前展示；失去当前精确身份时 fail closed 并给固定 Notice。Setup 本身不重新运行 preflight 或 provider 检查。
- 正常 generation 换代继续使用中性的 Refreshing；Recovery / Source Recovery 继续走既有专用界面，不被 Setup 页面覆盖。

### Task Tracking

- [x] 新增 secret-free readiness projector、普通 Chat 本地模型 projector 与单调生命周期 Store。
- [x] 在 Knowledge Studio unavailable 状态渲染 `Setup & status`，并在 ready 页提供可返回的状态入口。
- [x] 接入 Startup Barrier、Settings / Projects / Chat selection 变更与插件 unload，保证当前状态投影和旧生命周期关闭。
- [x] 实现只读导航 port；不自动写配置、目录、凭据，也不调用模型。
- [x] 更新中文手册、英文技术真值和本轮架构/边界说明；明确打开既有 Settings 可能触发插件更新检查，但被动 readiness 为零模型/零 provider 请求。
- [x] 回写产品复评：三卡 readiness 已实现且有聚焦自动化证据，不再列为待开发项；精确区分已通过的 Windows 主窗口就绪路径与仍未现场制造的配置失败 / Recovery / popout 边界。

### Testing Checklist

- [x] 覆盖所有 Startup status、已知/未知诊断映射，以及 Knowledge 与 Chat 独立状态。
- [x] 覆盖 Chat 选中模型 0/1/多匹配、disabled / retired / project-disabled / credential missing / locally configured。
- [x] 覆盖 Store revision、listener isolation、dispose 和 caller-owned 数据隔离；本切片没有异步 projector 结果可回写。
- [x] 覆盖 unavailable Setup、ready→Setup→Back、Recovery 仍由原面板呈现、popout 重建与导航回调。
- [x] 断言 readiness snapshot / DOM / Notice 不含 key、原始 YAML、私有路径或 raw error；被动 composition 直接覆盖零 fetch，生产接线只调用同步纯投影且不构造 provider / model。日志边界继续由固定、无输入日志策略保证，本切片不新增 readiness 日志。
- [x] 聚焦门禁为 12 个 Jest suites / 150 tests（Chat local/composition、readiness/store/navigation/subscription/publication、Panel/Root/View 与 availability adapter）；最终全仓为 271 suites / 4503 tests。TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build、文档 16 文件 / 134 本地链接、`git diff --check` 与变更集敏感信息/个人路径扫描均通过；未运行真实模型 integration。
- [x] Windows Obsidian 主窗口有界验收已通过：final production artifact 真正 disable/enable 后，已就绪 Studio → `Setup & status` → 三卡 → `Refresh displayed status` → `Back to Studio` 两轮正常；Workspace / Knowledge 为 locally ready，缺 OpenRouter Key 的可选 Chat 单独为 credential missing 且不阻断 Studio。UI 明示本次状态检查零模型请求，Key 真伪、license entitlement、账单、额度、网络与本地 server 均未测试；刷新前后 `data.json`、Sources / Wiki / Knowledge / Projects 树保持精确不变，UI 资源请求为 0，最终 console 无 error / warn。
- [ ] Setup/readiness 剩余 Windows 现场边界：不为验收而破坏配置或 durable state，因此尚未人工制造 no Project / no Bundle / invalid Bundle、Recovery / Sources-only、自然 generation `Refreshing` 与 popout Setup；这些路径保留自动化覆盖，不冒充本轮现场结果。

## Review 修正与证据（2026-08-12）

### Session Goal

让用户在 Review 不满意时能够看清提案依据并进入安全的修正路径，而不是只能接受、拒绝后再猜应该改 Source 还是 Schema；本轮先按可独立验收的纵切收敛范围，不修改任何 AI prompt。

### Architecture Decisions

- **教程冻结**：功能、聚焦测试、全仓门禁和必要实机验收稳定前，暂不修改或同步 `docs/` 与 Windows Manuals；开发期间只维护本节 TODO。最终确认行为后一次性更新教程，避免反复改写和同步。
- 先复用现有 Review proposal、sourceRefs、locator 与 generation-owned capability，不为 UI 创建新的证据真值来源，也不绕过 Review / Apply。
- “证据并排”“用户编辑候选”“带反馈重新生成”按用户价值拆成独立纵切；只有当前纵切完整通过后再进入下一项，避免把 UI、Runtime 写协议和模型调用混成一次大改。

### Task Tracking

- [x] 完成现有 Review / provenance / source-open / Runtime command 的只读架构与 UX 审计；本刀收敛为 proposal-level 证据预览与 exact Source jump，不假称 citation 能证明每个 diff block，不包含候选编辑或反馈重生成。
- [x] 实现当前最小纵切：Review plan 只发布 64 条有界、无 path/sourceId/hash/locator 的证据摘要和 opaque ref；点击时 fresh 重证 pending proposal、Runtime snapshot token、workflow generation、Manifest/watch source identity、sourceRoot 与当前文件 bytes，再复用 Markdown/PDF exact navigator。Controller、delegator、adapter 和 production composer 均按 generation fail closed；连续跳转的不可取消 Obsidian workspace 副作用按 App owner 串行，最终可见位置归最新点击。
- [x] 同步补齐 Runtime 持久文本 64 MiB 硬上限：read / migration / mutation 均在 `JSON.parse` 前拒绝超限输入，任何超限输出不提交，避免畸形 durable proposal 造成无界解析；测试覆盖 pre-parse、exact limit、read、migration 与 mutation no-write。
- [x] 完成当前冻结树自动化门禁：R1 production wiring 相邻 14 suites / 194 tests 通过；最终全仓 277 suites / 4555 tests 通过；TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build 与 `git diff --check` 通过。未运行模型 integration，也未执行 Windows Obsidian R1 人工验收。
- [ ] Windows Obsidian 有界验收：真实非空 Review 显示 proposal evidence；Markdown / PDF exact jump、stale source、连续点击 latest-visible、Reject 零 Wiki 写与 Validate-and-Apply 原有事务边界均按冻结产物复核。
- [ ] 功能冻结后一次性解除教程冻结并更新相关手册；在此之前禁止为中间状态修改 `docs/`。

## Source Documents

- [`designdocs/PERSONAL_KNOWLEDGE_OS_PRD.md`](./designdocs/PERSONAL_KNOWLEDGE_OS_PRD.md)：产品目标、Golden Flow、需求和验收标准。
- [`designdocs/PERSONAL_KNOWLEDGE_AGENT_SOLUTION.md`](./designdocs/PERSONAL_KNOWLEDGE_AGENT_SOLUTION.md)：技术架构、数据契约、可靠性与路线图。
- [`designdocs/OPEN_SOURCE_REUSE_PLAN.md`](./designdocs/OPEN_SOURCE_REUSE_PLAN.md)：外部代码 Copy / Port / Reference 台账。
- [`designdocs/GITHUB_PERSONAL_KNOWLEDGE_LANDSCAPE.md`](./designdocs/GITHUB_PERSONAL_KNOWLEDGE_LANDSCAPE.md)：开源项目调研快照。
- [`designdocs/PERSONAL_KNOWLEDGE_EXECUTION_PLAN.md`](./designdocs/PERSONAL_KNOWLEDGE_EXECUTION_PLAN.md)：Windows-only commit 顺序、依赖与退出门槛。
- [`THIRD_PARTY_NOTICES.md`](./THIRD_PARTY_NOTICES.md)：人工复制/派生外部材料的归属台账与发布门禁。
