# Development Session TODO

## Session Goal

以 Windows 上的 Obsidian Copilot 为唯一首期平台，设计并分阶段实现一套长期个人使用、体验优先的知识操作系统；把助手、笔记和知识引擎连接为可摄入、可审核、可引用、可持续维护的完整闭环。

## Completed Tasks ✅

- [x] 阅读并拆解 SOC Agent 原始方案及其产品决策框架。
- [x] 核对当前仓库的消息、上下文、工具、搜索、记忆、项目隔离和持久化架构。
- [x] 根据用户澄清，将目标从“Copilot 项目总架构”修正为“基于 Copilot 的个人知识库方案”。
- [x] 区分当前已实现能力与 ACP、Planner、MCP 等设计中能力。
- [x] 编写个人知识库产品与技术方案文档。
- [x] 补充原方案方法的保留、改写和舍弃清单。
- [x] 检查文档与代码、现有设计文档之间的一致性。
- [x] 阅读 Karpathy LLM Wiki 原文并提炼 Raw / Wiki / Schema、Ingest / Query / Lint、index / log 模式。
- [x] 核对 Google Cloud OKF v0.1 官方公告、规范、合规规则和非目标。
- [x] 将 LLM Wiki 与 OKF 的采用方式合并进个人知识库主方案。
- [x] 调研 GitHub 上活跃的 LLM Wiki、OKF、个人知识库、Agent Memory 和时序知识图谱项目。
- [x] 对比其技术栈、知识工作流、许可证和与 Obsidian Copilot 的适配程度。
- [x] 形成可采用、延后采用和明确不采用的工程结论。
- [x] 盘点当前项目可直接复用的 Chat 文件拖入、处理进度、ApplyView、Search v3、graph boost、工具和 popout-window 能力。
- [x] 对 `llm-wiki-compiler`、OKF、`llm_wiki`、`claude-obsidian` 和 Graphiti 完成 commit 锁定、许可证与文件级代码审计。
- [x] 建立外部实现的 Copy / Port / Reference 复用分类，并记录上游需要修复的缺陷。
- [x] 编写体验优先的个人知识操作系统 PRD，定义 Knowledge Studio、Golden Flow、验收标准与本地成功指标。
- [x] 将主技术方案路线图改为端到端体验切片，并补充 manifest、pipeline fingerprint、持久队列、事务和可选时态投影。
- [x] 将首期平台范围收敛为 Windows Obsidian Desktop，移除 macOS、Linux、iOS、Android 和浏览器适配承诺。
- [x] 编写 Windows-only commit-by-commit 执行计划，并建立 Now / Next / Later 退出门槛。
- [x] 建立第三方来源声明与许可证落盘规则；五个候选当前明确标记为 audited/reference、未复制源码。
- [x] 完成 Slice 1A 纯 TypeScript 契约、strict runtime schema、语义 validator 与公共导出边界。
- [x] 完成原始字节 SHA-256、pipeline fingerprint、稳定 source identity、Windows Vault path 和输出存在性 freshness 判定。
- [x] 建立 Markdown 文本、PDF locator、OKF round-trip、Windows 路径/碰撞、文件占用和中文/emoji fixture。
- [x] 完成 Source Manifest Repository 与可注入 Storage Port；支持 stable identity、rename、freshness、扩展字段保真与 revision CAS。
- [x] 修复并发 success/failure 覆盖竞态，以单调观察时间和有界 CAS retry 保留较新状态。
- [x] 完成持久 Ingest Queue Core：严格快照、revision CAS、Bundle 隔离、claim ownership、同来源去重、durable source high-watermark 与 exactly-one latest rerun。
- [x] 完成 Queue 纯 core 的暂停/恢复/取消、安全启动状态转换、显式审核、指数退避、最大重试、provider 限流暂停、错误脱敏与同会话基础设施失败恢复；不等同于真实 startup coordinator 已接通。
- [x] 将 applying 设为事务安全边界；Commit E 日志完成前，中断或失败 apply 进入不可绕过的 recovery-required gate。
- [x] 完成 accepted ChangeSet preflight、Vault-global pre-state journal、Windows 确定性写入、单文件原子 CAS、commit marker 与纯 transaction core 的幂等 startup roll-forward；真实启动编排仍是 pending。
- [x] 完成 Queue v2 exact apply/commit marker、v1 严格迁移与提交协调器；Commit G 再严格迁移到 v3，并加入 pending Review Store anchor、accepted terminal identity 与 durable rejection tombstone。
- [x] 完成 provider-neutral 两阶段 Knowledge Compiler Core：strict unknown output、可信 evidence 映射、caller-owned target authorization、Windows exact observation、opaque target binding、runtime hash/id/status、确定性 candidate validation 与 fake-model 回归。
- [x] 从 legacy ApplyView 抽取无写入能力的精确 diff renderer，并修复普通 rerender、popout migration 与定时器的 document/window 归属。
- [x] 完成 strict durable Review Store：完整 proposal、canonical digest、queue job claim、record revision、accepted/rejected payload、CAS 竞争与幂等重放。
- [x] 完成 Review Store → Queue 的 0→1 hand-off：Queue 只有收到同 Bundle、同 exact job claim 的 durable pending record 才进入 `awaiting_review`；接受/拒绝必须匹配该 proposal digest 与 terminal revision 1。
- [x] 增加 `reconcilePendingReview`，可在 proposal 已落盘但 Queue 尚未完成 hand-off 的崩溃窗口后，把同一 attempt 恢复为 awaiting-review；v1/v2 `legacy_unverified` anchor 只能由真实 pending record 升级。
- [x] 完成 opaque multi-file review decision Core：逐文件/逐块选择只传 opaque id，Core 精确重组文本、重新 hash、重新验证；delete 在授权 read-set journal 化前 reject-only。
- [x] 完成 durable Queue review rejection：Queue v3 保存 exact job/changeSet rejection identity，拒绝不构造空 accepted ChangeSet，并原子提升 retained latest rerun。
- [x] 完成 Activity 只读投影、Review/Activity React 面板、reload-only Controller，以及 popout-safe Windows Knowledge Studio ItemView。
- [x] 加入 Windows Desktop 功能级 guard、命令/导航入口与用户文档；保留现有插件 `isDesktopOnly: false`，不扩大 Knowledge Studio 的平台承诺。
- [x] 完成 `knowledge-runtime-v1.json` 持久化基础代码：单一 strict envelope 承载 Queue、Review、Manifest、Vault-global Transaction slot 与 watcher-capture `inputRevision`，所有替换经过完整 envelope 语义校验和 revision CAS。
- [x] 完成 Windows 文件 adapter 的 create/update 代码层：首次 runtime 以完整临时文件 + flush + 排他 hard-link 发布，Wiki create 使用排他创建，update 使用 `Vault.process`，能力声明绑定 store 并在 preflight 前禁用 delete；真实 Windows Obsidian 验收与 Golden Flow 接线仍未完成。
- [x] 完成 Review Store 启动协调器 Core：pending 审核可恢复 exact Queue anchor，rejected 审核可恢复其 pending predecessor 后收敛为 durable rejection，重复启动零 Queue revision 增长；accepted 只输出 identity-only runtime classification 输入，不自动调用模型、开始 apply 或写 Wiki。协调器以有界 revision 重读取得稳定 Review snapshot，并把 observed revision 返回给更高层继续复证。
- [x] 完成 G.2b durable Manifest plan/intent：Compiler 冻结完整 primary-source page projection、ownership/authorization、Manifest revision/digest 与 source hash/pipeline/input revision；Review v2 只能从 immutable plan 投影 accepted filter/rewrite，transaction journal v3 通过 plan digest 持久回链最终 intent。
- [x] 完成 exact atomic Manifest/apply ledger adapter：首次 prepared journal 在 shared envelope 内预留 Manifest read-set 与单调 source revision，页面 committed 后 Manifest success 与 ledger 同 transform 发布；exact replay 不改字节，同 transaction id 不同 identity fail closed。
- [x] 收紧 Runtime v2 authority：普通 Manifest storage 不能创建/修改 `lastSuccessful` 或 reserved commit metadata，active transaction 锁住通用 Manifest CAS；shared page update 原子传播所有 co-owner hash，unsafe runtime-v1 状态保留原字节并 typed fail closed。
- [x] 完成 Runtime v2 启动交叉校验：Manifest success 与 reserved metadata 必须双向存在并指向该 source 最新 ledger；source/hash/pipeline/input/intent/ChangeSet/time、Manifest revision/digest、连续 ledger 链与 Windows shared-page co-owner projection 任一撕裂均在启动时 fail closed。
- [x] 在 prepared journal 发布前拒绝复用已入 ledger 的 transaction id，并把 durable transaction replacement 限定为 immutable payload 下的合法 prepared → applying → committed/recovery 状态迁移。
- [x] 完成 Queue v4 与全写入周期原子授权复证：accepted apply claim 持久化最终 Manifest intent digest；直接 apply 在任何 target observation 前先复证完整 Queue/Review/allocator/Manifest read-set/source-order authority，prepared 发布、prepared → applying、启动读取 unfinished journal、每次共享 envelope mutation 与最终 Manifest+ledger callback 都重复或保留该证明；旧 v3 或 stale Manifest 在任何新 Wiki 文件访问前 fail closed。
- [x] 完成 no-journal 显式恢复 Core：Queue v5 保存 exact abandonment tombstone，Runtime 从同一 atomic envelope 分类 accepted-not-started/active/blocked/finalizing/committed/abandoned，并以 opaque recovery id 在 continue 前重证完整 Manifest authority，或在无任何 journal/commit/ledger 写入证据时原子 abandon；崩溃重放与 prepared-publication 竞争均收敛且不产生 split-brain。
- [x] 完成 layout-ready startup gate Core：严格按 Queue startup recovery → active transaction/commit marker 收敛 → Review hand-off 收敛 → Runtime 单-envelope accepted 批量分类执行；首次 sticky file conflict 只接受同 transaction 的 blocked 重读，Review revision 变化有界重跑。Gate 保留 Runtime/Queue revision 高水位、精确校验 accepted action↔classification 集合，并从最终 atomic snapshot 观察 Vault-global transaction；返回的 `observed_clear` 仍只是乐观展示结果，不会自动 resume、begin accepted apply、continue、abandon 或调用模型。
- [x] 完成 shared-envelope conditional startup release Core：`observed_clear` 只生成 Runtime/Review/Queue revision 乐观令牌，Runtime 在同一次原子 transform 中重证 Vault-global transaction、跨 Bundle 写恢复证据、当前 marker/claim/applying 状态与全部 accepted terminal classification，成功时只把 exact `startup_recovery` Queue 改为 running 并提升 Queue/Runtime revision。普通 `resume()`、`pause → resume` 与两步 generic Queue CAS 都不能清除或洗白 `startup_recovery`/`recovery_required`/`commit_pending_ack` 单调恢复门；同门写入必须保留当前 claim/job/marker，accepted-not-started anchor 在 running/startup 状态均受保护，跨门写入必须等于 accepted begin、startup recovery、current committed journal resolution 或 marker finalization 的精确投影，不能拿历史 ledger 替换当前恢复证据。`applyAbandonments` 对通用 Queue writer 永远只读，只能由显式 Runtime abandon 原子追加；source high-watermark 不可删除、回退或在同 revision 改写，任何推进（含首写）都不得超过共享 allocator，同时保留合法 A→B→A watcher 收敛。commit-then-throw 不猜测成功，旧令牌失效后必须重跑 Gate。`accepted_not_started` 可由显式 exact acceptance 在 running 或保持 startup pause 的情况下建立 apply claim，崩溃则升级为 no-journal recovery。
- [x] 完成 Runtime v3 durable source-observation hand-off Core：watcher 先以全局永久唯一且重试稳定的 `captureId` 幂等分配 opaque token/revision，再以 first-write-wins 绑定 exact source hash/pipeline；Queue high-watermark 只有通过 adapter-private token capability 匹配 bound record 才能推进，并在同一 shared-envelope transform 中把观察标为 consumed。更高但未绑定的 allocation 不会淘汰较低 bound read；更高 bound read 会原子 supersede 较低未终结观察。窄 `SourceObservationHandoff` facade 统一 bind/enqueue/settle，可收敛 bind/Queue commit-then-throw；历史 consumed capture 重放直接返回自身 durable receipt，不调用 Queue 或借用最新 job。Runtime 可按 Bundle 枚举 pending allocated/bound work 供重启恢复，任一 pending allocated/bound observation 都阻止 startup release。v2 迁移只建立 legacy fence/checkpoint，不伪造历史 token；每次 parse 还拒绝 HWM 回退、伪 supersession 和其他 API 不可达 journal。
- [x] 完成 G.2h 插件级 fail-closed 启动前置接线：Project frontmatter 无损保存 untrusted Bundle 值，layout-ready 后从已完成初始化的 Projects 严格解析全部 Bundle，并按 Windows 大小写不敏感边界做跨 Bundle 冲突校验；普通 Projects 独立于 Knowledge Runtime 启动。稳定 Delegating Studio port、动态 Session Store 与启动 Barrier 在缺失/非法/多 Bundle 未选择时不制造占位 Bundle，唯一真实 Bundle 可驱动已恢复 View 切换；有效配置仍停在 workflow-adapters-unavailable，绝不调用 Gate/Release/Queue/model/watcher。ProjectRegister 初始化已 single-flight、成功幂等、失败可重试，cleanup 永久失效；插件卸载在首个 `await` 前同步关闭 Barrier/Session/Port/Project 监听。
- [x] 完成 G.2i Projects lifecycle ownership：ProjectManager、ProjectFileManager、ProjectContextCache、FileCache、ProjectLoadTracker、VaultDataManager 与 project-mode FileParserManager 显式捕获 owning App/Vault，插件热重载或跨 Vault 时同步退役旧 Manager/订阅/监听/内存 cache；Project state 使用 opaque owner，pending write 使用 exact lease，所有初始化/重载/目录扫描共享 prepared-scan CAS，目录代次同时约束 Vault events 与 CRUD continuation，旧 scan、旧操作、旧 cleanup 与同路径 ABA 均不能提交到新 snapshot。新插件先退役旧 ProjectManager 再建立 Project state owner，卸载则在首个 await 前解除 Manager/Project/Vault-data owner，避免状态清空触发旧 autosave或旧 cleanup 拆除新监听；Runtime 动态模块加载受启动代次约束，log flush 绑定显式 Vault。并发 project switch 只有最新请求可发布 selection/UI 结果，ChainManager settings subscription 随 owner 释放。
- [x] 完成 G.2j 独立 Vault source watcher adapter：严格 opaque watch plan、exact App/Vault/adapter owner、同步 capture authority/id、durable allocate-before-read、captured-path exact binary SHA-256、handoff-only commit、安装期 handoff capability 快照、注入端口 data-only success payload 复证、allocate/commit frozen request 有界重试、captured-path read 有界重试、跨 lifecycle/watch-plan continuation fail-close 与同路径多 Bundle 隔离。当前 lifecycle 内观察到的 rename/delete/folder 破坏事件会同步使受影响 source 的后续阶段失权并 quarantine；G.2q 已把启动缺失、大小写漂移与 Windows-key collision 也升级为同代 authoritative blocker。重启 full crawl 只复证当前文件状态，不持久保存停机期间 destructive-event receipt。replace/close 不能取消已经发起的 adapter operation（包括 commit），但其返回后不能进入下一 read/commit 阶段或发布旧通知。配套 exact-byte reader 在未来 executor 的 parser/model 前复证 Queue job hash，并返回 parser 必须消费的同一 bytes。只读 loader/byte parser 已由 G.2l 补齐；production Release/worker composition 仍保持 unavailable。
- [x] 完成 G.2k opaque watch-plan authority builder：从 strict Bundle/Manifest detached snapshot、exact schema bytes 与 caller-projected pipeline behavior profile 复证 source/write boundary，按通用 Windows suffix 无歧义选择 parser。对传入快照，per-source fingerprint 绑定 exact Bundle-config digest、compiler version/configuration、selected parser identity/configuration、schema raw-byte hash、projected model behavior、output language、OKF 与 citation contract；plan 另保留 Manifest revision/digest、submitted-profile digest 与总 digest。Core 只防御性拒绝常见 credential-like 字段，production adapter 仍必须执行显式 allowlist。watcher 构造/替换已删除 raw source array 入口，只接受 module-private token + WeakMap state 认证的 frozen plan；同物理 source 可服务不同 Bundle，空 Manifest Bundle 也要求可调用的 durable handoff。G.2l 已补上当前 durable snapshot 的只读装配与 parser 前后复证；插件启动组装保持 unavailable。
- [x] 完成 G.2l 只读 production-facing workflow-plan Core 与 byte-only parser boundary：通过注入端口从 Manifest facade、exact App/Vault reader、Projects/settings generation 和显式 model allowlist 双采集全部 Bundle；两次 authority digest 不同即整体失败。G.2q-b/c 已把这些端口组装并接入 plugin-owned observation session；parser/model/worker 执行仍未组成可用插件 workflow。Profile Source 精确匹配 `name|provider`，禁止 fallback，只保留版本化 projection-only 行为和 endpoint/routing 哈希摘要，API key、完整 settings、未知模型字段及 credential-like compiler/parser 配置均不进入 profile。Watch plan 为每个 source 持久绑定 config-free parser profile digest，空 Manifest 也复证完整 parser registry。opaque execution binding 在 parser 前后重复复证 Manifest revision/digest 与 schema raw bytes，以 fatal UTF-8、BOM/CRLF 不归一和 byte round-trip 桥接 Compiler schema；首个 UTF-8 text parser 只消费 `readExpected` 返回的同一 bytes，限制字节/字符并在边界检查点检测净 mutation。Loader/Profile/Execution state 均由 WeakMap 隔离且 class/prototype/instance frozen。Queue worker、模型、Review 或 Wiki write 仍未接入；parse job 尚未由不可伪造的 durable Queue/observation capability 认证，不能视为 compiler-ready executor。
- [x] 完成 G.2m 模型执行前双重 capability Core：Queue 为 exact controller/attempt/startedAt 与 Queue-owned `AbortSignal` 签发 process-local revocable claim，并把 claim 绑定到 exact QueueStorage；Runtime 从同一 atomic envelope 证明 unique exact processing job、consumed observation 与 Manifest，legacy checkpoint/high-watermark 不能冒充 observation。opaque execution owner 排他绑定单一 Runtime 与单一 workflow loader generation，相同数据或复用同一个 owner 也不能跨 Runtime/plan 拼接。authorized preparation 在任何 source read 前匹配 Runtime↔workflow Manifest，并以 Runtime → Manifest/schema/profile → Runtime sandwich 在 parser/model 前后重复复证，普通 job DTO、`unbound_read_only` marker、spread/prototype forgery 均不能升级权限。每份 preparation 只允许创建一个 adapter；Compiler 为每次 `compile()` 签发独立 opaque session，并为 exact analysis/generation request 签发一次性 call authority；adapter 在 analysis pin session，generation 必须来自同一 session，跨 Compiler、跨 compile 与重放均 fail closed。Knowledge 私有 route 用 WeakMap 隐藏 credential/transport closure并完整匹配 provider/model/projected configuration digest，job-bound adapter 只用 exact Queue signal、每阶段只发起一次 outer route call、调用前后复证，并只接收有界 JSON text 后在私有边界解码冻结。profile 强制声明 non-streaming/no-fallback；具体 transport 尚未实现，后续仍须独立审计其内部无 streaming/fallback/retry。fresh adapter 在首次调用前仍是可信组装 capability；production composer 必须立即交给预期 Compiler，若未来会暴露该对象则须先增加 composer-issued pre-bind owner。本阶段仍不接 `main.ts`、后台 worker、真实 DeepSeek/其他 provider HTTP、prompt encoder、Review 或 Wiki write，也未新增/修改 AI prompt 内容。
- [x] 完成 G.2n 确定性 Knowledge Prompt Encoder 与 DeepSeek 私有 Transport：用户已明确授权本独立提交新增 Knowledge 编译 prompt。两阶段均固定为 system + canonical INPUT_JSON 两条消息；schema 只作为受 system 权限约束的 Wiki 组织/内容规则，evidence/context/currentContent/outputLanguage 等不可信字符串只进入 JSON 数据区。prompt 合约含 strict 输出 shape、最小 JSON 示例、字符/UTF-8 预算及覆盖实际两阶段消息的 identity；prompt identity 与 DeepSeek route identity 均进入 pipeline profile/fingerprint，concrete route 再绑定完整 profile digest。DeepSeek 首期只允许官方 `https://api.deepseek.com/chat/completions`、当前 `deepseek-v4-flash` / `deepseek-v4-pro`，用注入的 native-fetch-compatible port 透传 exact Queue signal，强制 `stream:false`、JSON Object、exact model/max_tokens、单次 POST、无 fallback/内部 retry/repair。响应从首字节流式累计，8 MiB 前置 cap 后 fatal UTF-8 + strict provider envelope；仅 `stop`、单 choice、exact model、assistant non-empty content 与一致且不超限的 usage 可进入既有 adapter 的第二道 32 MB/strict Core schema。credential 只留在 closure/header；custom endpoint、旧模型、未知/已废弃配置、redirect/SSE/length/tool call/429/5xx 全 fail closed。模块已导出但未接 `main.ts`、settings/key resolver、worker、Review 或 Wiki write，Studio 继续 unavailable、默认零网络。
- [x] 完成 G.2o DeepSeek V4 迁移、生产零网络预检与脱敏失败策略：direct DeepSeek catalog 只保留 reviewed V4 Flash/Pro 为可选 identity，旧 Chat/Reasoner 保留为 disabled retired marker 且 default/Quick Ask/Command/Project 不得静默 fallback；thinking 与 sampling 配置由 provider-specific policy 在请求前互斥校验。插件在 Runtime/Projects/Bundle strict validation 后，用同代 Projects、已 hydrate settings、固定 compiler/parser/prompt/route profile 和主 renderer native fetch 逐一构造并立即丢弃 private route，仅保留脱敏的同步 preflight 结果、绝不 invoke；settings/Projects 变化或卸载同步 close 当前结果 generation。Transport → Adapter → Compiler → Queue 的错误只经 module-private brand 和固定 code policy 投影，真实 429 才能进入 durable rate-limit pause，500/503/network 由 Queue 有界重试，鉴权/余额/request/response/未知 HTTP 终止，transport 无内部 retry。有效预检仍停在 `workflow_adapters_unavailable`，没有启动 Gate、Queue、watcher、model、Review 或 Wiki write。
- [x] 完成 G.2p plugin-owned preflight/recovery lifecycle 与 recovery-only 生产接线：preflight candidate 构造期和同步 `preflight()` 期的 Settings/Projects 重入均以 generation 复证关闭；exact admission 冻结 strict Bundle owners，替换、取消、配置/Runtime identity 变化与卸载同步关闭当前 preflight 和一次性 recovery capability。成功预检后以真实 Runtime Queue/Review/Transaction、Vault file store 和 startup Gate 做 owner-first、逐 Bundle恢复，在首个 attention/blocked 停止；Gate 在 Queue recovery、apply reconciliation、Review reconciliation 和 accepted snapshot 之间复证 current generation，旧代不能在早期 await 后新进入 transaction/file access，已进入的单一 durable phase 仍完成 crash-safe 收敛。Barrier 分别发布 clear/attention/blocked/unavailable，所有外部 code/kind 经固定 allowlist 脱敏。该路径只可收敛 durable Queue/Review bookkeeping 并 roll forward 已持久化、已授权 journal；conditional Release 现在由后续 G.2r observation session 触发，仍没有 worker、watcher、model、新 transaction preparation、no-journal 自动决策或新 Wiki 生成。
- [x] 完成 G.2q-a listener-first observation startup Core：watcher 可先同步注册 listener、再恢复观察并显式 full crawl，重复 `start()` 不二次 crawl，pre-crawl plan replacement 不隐式扫描。这里的 crawl 只覆盖 opaque plan 中已经由 Manifest 注册的 sources，不从 `sourceRoots` 自动发现新文件。启动缺失、exact path 大小写漂移、Windows-key collision、capture failure 与 rename/delete 均成为当前 plan generation 的 authoritative quarantine blocker；同代 recreate 不能自行解封。reconciler 只对当前 plan/pipeline 下、经 `readExpected` 复证相同 raw hash 的 bound observation 做 exact token/hash/pipeline 重放；allocated 与 hash/path drift 不猜测旧字节，仅在更高 revision crawl capture 成功时被 supersede，残留记录在 final reconciliation 继续阻塞。协调器固定 `listener → pre-recovery → crawl → idle → final recovery → idle → blocker proof`，并在 caller generation 取消时同步关闭 watcher/reconciler；真实 Runtime 集成证明成功的 revision 3 crawl 可把旧 allocated + drifted bound 收敛为唯一 pending Queue job，整个 Core 没有 Release、`runNext`、model、Review 或 Wiki port。Runtime conditional Release 同时原子阻断 allocated/bound residual work。该 Core 已由同代 startup observation session 激活，Studio 继续 unavailable。
- [x] 完成 G.2q-b 同代 production observation 内部组装：production preflight 用同一次 Projects/settings/resources snapshot 构造并复用 exact `ProjectKnowledgePipelineProfileSource`，以 module-authenticated、secret-free workflow lease 持有 frozen owners、owned parser capabilities 与 profile resolve authority；Settings/Projects/preflight replacement 或 close 会先清除 capability 引用，再同步通知 observation generation 撤销。`KnowledgeProductionObservationComposer` 在同一 execution owner 下组装真实 Runtime Manifest repository、exact App/Vault source/schema reader、double-collected workflow plan、Runtime observation allocator/binder 与 startup-held Queue；Queue 只把 `enqueue` capability 交给 handoff，private executor 对任何意外 claim/run fail closed。初次收敛后保留 live listener，并以同一 caller signal 支持串行 `reprove()` quiesce/reconcile/blocker proof 和同步 `assertHealthy()`；任何 stale、并发、阻塞或撤销都会关闭会话。该 composer 不执行 conditional Release、`runNext`、model、Review decision、transaction preparation 或 Wiki mutation，Studio 继续 unavailable。
- [x] 完成 G.2q-c plugin-owned observation session 接入：startup Barrier 在同代 recovery `observed_clear` 后才启动 observation port，并保留 live watcher；新的 startup generation、取消、Settings/Projects/Runtime replacement 与 unload 会同步关闭 observation。Barrier 只接受完整 convergence result，observation blocker/diagnostic 不被伪装成 release receipt；Queue 在 fresh Gate/release 前保持 held，未接 `runNext`、worker、model、Review 或 Wiki mutation。
- [x] 完成 G.2r observation → fresh Gate → conditional Release 接线：初次 observation convergence 后先以同一 signal 做 reproof/health assertion，再按配置 Bundle 顺序运行 fresh Gate，并通过 Runtime shared-envelope release 原子重证 revision、恢复证据、accepted terminal state 与 source-observation watermark；仅全部 Bundle 成功/已 running 才返回 released。stale、blocked、observation drift、generation replacement 或 unload 均 fail closed，Queue 不会被 generic resume 绕过；仍未接 worker、Compiler、model、Review decision 或 Wiki mutation。

## Pending Tasks 📋

### Slice 1A：契约与来源归属 ✅

- [x] 建立 `THIRD_PARTY_NOTICES.md` 和第三方许可证目录规则；实际 Copy 时再同步加入许可证原文与文件映射。
- [x] 建立 Golden Flow 的 Markdown、PDF locator、OKF round-trip、CRLF、盘符/反斜杠、大小写碰撞、保留设备名、文件占用和中文路径 fixture。
- [x] 实现纯 TypeScript `KnowledgeBundleConfig`、`SourceManifestEntry`、`SourceLocator`、`ClaimCitation`、`KnowledgeIngestJob` 和 discriminated `KnowledgeFileChange`。
- [x] 实现 source byte hash、pipeline fingerprint、稳定 source identity、Windows path contract 和输出存在性校验。

### Slice 1B：持久队列与可恢复写入

- [x] 实现可注入的 `QueueStorage`、`IngestExecutor`、`EventSink`、`RetryPolicy` 和队列状态机。
- [x] 支持任务去重、处理中 latest rerun、暂停、取消、指数退避、重试、stale claim 防护和 Queue 纯 core 的安全启动状态恢复。
- [x] 实现 Windows/Vault `QueueStorage` adapter 的 revision compare-and-replace 代码层，以及跨 runtime 重建、并发安全的 per-source watcher-capture `inputRevision` 分配器；真实 Windows `DataAdapter.process` 行为仍由独立实机验收项负责。
- [x] 实现专用 durable observation hand-off、Runtime v3 observation journal、token-capability exact Queue 消费、pending work 重启枚举与窄生产 facade；不再以 scalar allocator 上界授权任意 Queue payload，也不把历史 capture 重放错误关联到较新 Queue job。
- [x] 实现独立 `ObsidianVaultSourceWatcher` production adapter：只接受 opaque `KnowledgeSourceWatchPlan`，绑定 exact App/Vault/adapter，先同步捕获 path/authority/captureId，再 durable allocate，随后用 owning Vault `DataAdapter.readBinary` 读取 exact bytes、计算 raw SHA-256 并仅经 `SourceObservationHandoff` commit；安装时复证并快照 handoff 方法，reader/allocation/settlement success 只接受 detached data payload。allocate 重试复用同一 frozen request；read 重试固定 captured path，首次通过本地复证的 bytes/hash 才成为 commit payload；commit 重试复用同一 frozen token/hash/pipeline request。旧 generation 的当前 adapter call 返回后不能启动下一 read/commit 或发布旧通知，但已发起的 call 不能被取消。同路径多 Bundle 独立观察；仅当前 lifecycle 实际观察到的 rename/delete/folder 事件会同步 quarantine 对应 source 并阻断同路径 ABA。`ObsidianExactSourceArtifactReader.readExpected` 为真实 executor 提供 parser/model 前的 job hash 复证与同一 bytes hand-off。该 adapter 已由同代 startup observation session 激活，但不会运行 worker 或开放 Studio。
- [x] 实现严格、不可旁路的 watch-plan Core builder：重新解析 detached Bundle/Manifest snapshot 并验证二者关系，以 exact schema snapshot 和 caller-projected parser/model/compiler/output/OKF/citation behavior profile 计算 fingerprint；Bundle config、compiler config 与 selected parser config 均进入身份。module-private token、WeakMap state 与 frozen class/prototype/instance 关闭 direct construction、container reflection 和 monkeypatch 旁路。Core 对常见 credential-like key 的拒绝只是纵深防御，production adapter 仍必须执行显式 allowlist。builder 只保留冻结后的 source/Bundle authority，不保留 schema bytes 或配置值。
- [x] 实现只读 production-facing snapshot loader Core、exact schema bridge、显式 allowlist profile source 与 Knowledge 专用 UTF-8 byte parser，定义并测试 Manifest facade、exact reader、Projects/settings generation 的可注入边界；G.2q-b/c 已把它们接入同代 startup observation session，但尚未接 compiler worker。不传完整 provider settings，也不沿用会二次按路径读取或走闭源缓存的 legacy `FileParserManager`。
- [x] 实现 Knowledge 专用 provider execution binding、model adapter、确定性 prompt encoder、首个 DeepSeek private transport、生产 profile/credential/native-fetch 零网络 preflight 与脱敏 failure projection：module-private Queue/Runtime/observation capability 重新绑定 source identity，不信任 `unbound_read_only` marker 或同型 job DTO；私有 route/credential closure 匹配 endpoint/routing/model configuration，DeepSeek concrete route 再绑定完整 pipeline profile。禁止回读 settings、复用 ChatModelManager/LangChain fallback、`safeFetch/requestUrl` 或依赖可变 provider defaults。workflow plan 在 source/parser/model 边界稳定复读 Manifest、schema 与 Projects/settings profile，Runtime proof 来自同一 atomic envelope；exact execution owner 与 per-compile opaque session 阻断跨 Runtime/plan/compiler 阶段拼接。真实 worker 仍须在 fresh adapter 可被其他代码观察前把它直接交给预期 Compiler，并覆盖 App/Vault/parser/Queue generation 的完整生命周期。
- [x] 完成 listener-before-crawl、allocated/bound observation recovery、重启后当前状态 revalidation 与权威 full crawl 的独立 Core；没有 durable destructive-event receipt，结果只声明 observation converged，不携带 Queue release authority；现已由插件同代 observation session 持有生命周期。
- [x] 以同代 secret-free workflow lease 和 observation-only production composer 组装 Manifest/schema/profile loader、startup-held enqueue-only Queue 与 G.2q observation Core；支持同步撤销、初次收敛后的 repeatable reproof 与 synchronous health assertion，并已由 `main.ts` 的 recovery-clear 生命周期接入。
- [ ] 在已完成 fresh Gate/conditional Release 的 observation session 后补齐完整 artifact/projection validator 与 worker；真实 executor 必须把 `readExpected` 返回的同一 bytes 直接交给 byte-only parser，并在 fresh model adapter 可被其他代码观察前完成预期 Compiler 组装。若 production 架构不能保证该私有交接，先实现 composer-issued pre-bind owner。完成前 Studio 继续 unavailable。
- [x] 更新并迁移插件 DeepSeek model catalog 与 Knowledge-effective 配置：只接受 `deepseek-v4-flash` / `deepseek-v4-pro`，旧 `deepseek-chat` / `deepseek-reasoner` 不 alias、不 fallback；thinking profile 使用可审计的零温度占位且 wire 不发送 `temperature/top_p`，Minimal 明确关闭 thinking 后才使用采样。direct DeepSeek 不支持的 Frequency Penalty 与 Knowledge 未映射的 `numCtx`、Responses API、prompt caching、provider routing/custom endpoint 均在请求前拒绝。
- [ ] 在接入真实 durable adapter 并长期使用前定义 terminal job 归档/压缩策略，避免运行队列无限增长。
- [ ] 终态归档必须原子协调 Queue jobs、pending review anchors、accepted/rejected identity、Review Store terminal records、source high-watermark 与 observation terminal receipts；若删除历史，必须保留 capture replay、allocator floor 和旧输入不可复活所需的等价 tombstone，不能留下悬空记录。
- [x] 实现 before-hash CAS、完整 pre-state journal、确定性写入顺序、commit marker 和 transaction 纯 core 启动 roll-forward。
- [x] 在纯 core 协调器层验证任一断点后都不会出现“任务/manifest 已成功但 Wiki 页面未完成”的状态。
- [x] 实现 Windows/Vault `TransactionStorage` 的 Vault-global revision/token CAS slot 代码层，并以永久 envelope slot 的 `null` 清除避免删除状态文件。
- [x] 实现 `KnowledgeFileStore` 的排他 create 与 `Vault.process` update compare-and-swap 代码层；after-state replay 可识别 `already_after`，第三状态 fail closed。
- [ ] 实现可证明安全的 compare-and-delete；当前生产 capability 固定 `delete: false`，精确 before 确实需要删除时必须拒绝，不能用 read-then-delete 冒充。
- [ ] 在真实 Windows Obsidian test Vault 验证 `DataAdapter.process`/`Vault.process` 的 callback 串行化、双实例竞争、返回值、插件重载、外部编辑与崩溃行为；完成前不宣称 power-loss durability 或 production-ready CAS。
- [x] 实现 exact `transactionId/revision/changeSetDigest/intent/journal/receipt` 幂等的 `ApplyCommitManifestPort` adapter/ledger；同 identity 重放是 byte-preserving 单一逻辑成功，同 key 不同 digest fail closed。
- [x] 将完整 post-compile `generatedPages`、ownership/authorization、Manifest revision/digest 与 per-source hash/pipeline/`inputRevision` 作为 durable plan/intent 写入 Review/transaction journal，并在 Runtime prepared reservation 与最终 commit 重复复证。
- [ ] 将 schema、非 target link 与 source artifact 的 read-set 写入 journal，并在首次写入与恢复前复证；Manifest target authorization（revision、ownership、co-owner、sole-source、last-generated hash）子集已完成。完成其余 dependency 与 safe compare-delete 前真实 UI/apply 继续不启用 compiler delete。
- [x] 在 Manifest+ledger atomic callback 内重证 exact Queue apply claim、review identity 与 allocator/source high-watermark；coordinator 的早期 Queue verify 只作快速诊断，最终授权与 Manifest/ledger 同一原子 transform，exact ledger replay 继续最先返回。
- [ ] 为 outer runtime revision 预留 transaction progress、Manifest/ledger、Queue marker、journal ack/release 所需容量，避免接近 `Number.MAX_SAFE_INTEGER` 时文件 CAS 后才耗尽 envelope revision。
- [ ] 定义并实现 `no_changes` 的 durable source-success/Manifest revision 语义；当前无文件 ChangeSet 不产生 Manifest commit plan，不能静默当作已成功摄入。
- [ ] 在真实写盘前决定 CAS 后、progress 前崩溃的 content ABA 策略：接受 content-addressed at-least-once，或增加 mutation-intent marker 并在精确 before 状态 fail closed。
- [ ] 为 transaction `recovery_required` 增加显式重新校验、继续、回滚或放弃动作及 Knowledge Studio 恢复 UI。
- [x] 为“accepted apply claim 已落盘、transaction journal 尚未创建即崩溃”增加 no-journal 显式恢复 Core：先从同一 runtime snapshot 证明 exact Review/Queue/allocator 身份以及 journal、commit marker、source-input ledger 均不存在，再允许 continue 或 abandon；不得自动推断未写盘。Manifest 漂移会阻止 continue，但不阻止经原子证明的 abandon。
- [ ] 严格解析并脱敏 file/projection adapter 的运行时成功返回值，非法 adapter payload 统一映射为受控 infrastructure error。

### Slice 1C：Golden Flow 产品闭环

- [x] 为 Knowledge Studio 加入 Windows Desktop 功能级 platform guard、非支持平台提示，并记录 `manifest.json` 保持非 desktop-only 的兼容决定与用户文档。
- [ ] 首个真实 Windows adapter 接入时完成独立测试 Vault 验收；功能 guard 不能替代原子 Windows/Vault adapter。
- [ ] 在 Chat 文件拖入中增加 `Use in this chat` / `Add to Knowledge` 分流。
- [x] 完成纯 Core 单来源两阶段 compile；生成阶段只能返回已批准的 opaque target id，不能提交 path、operation、hash、source refs、validation 或 status。
- [ ] 把已隔离实现的 parser artifact、DeepSeek JSON-Object adapter、target authorization/resolver 与 projected OKF/link validator 组装进真实 worker；adapter 不得解析 fenced JSON、静默修复模型输出或在 transport 内重试。
- [x] 抽取 ApplyView 的纯 diff 展示，并以独立 Review Core/UI 支持多文件 create/update、来源、校验和逐文件/逐块审核；delete 可见但 reject-only。
- [x] 建立最小 Knowledge Studio / Activity 表面，显示解析、分析、关联、生成、校验、审核、apply、finalizing 和 recovery 阶段；真实 adapter 未接通前明确 fail closed。
- [x] 将 Review startup reconciliation、active transaction recovery 与 no-journal atomic classification 组装进纯 TypeScript layout-ready startup gate；Gate 只收敛已有 journal/commit 证据，不执行 no-journal 决策或 Queue resume。
- [x] 为 clear startup observation 实现独立 conditional release Core；observation 变化保持原字节并要求重跑 Gate；非 startup pause 与 recovery evidence 作为显式 blocker 原样保留，须先由对应用户/限流/恢复流程改变状态，再取得 fresh Gate observation，不能降级为普通 Queue resume。
- [x] 接入生产 Project Bundle 配置源、插件级 stable delegating Studio port、动态 Bundle session 与只证明 Runtime/Projects/config 的 layout-ready 前置 Barrier；无唯一真实 Bundle 时 Controller 不启动，旧 View 不锁定 sentinel，所有状态继续 `adapter_unavailable`。
- [ ] 在已接入并可 conditional release 的 production observation session 后补齐新 apply 所需的 artifact/projection validator、worker 隔离与恢复 UI；不能把保留的 user/rate-limit pause、empty/running Queue、observation-converged 或一次 `assertHealthy()` 当作“已完成 worker readiness”的持久证明，不得自动 continue 或把旧 `observed_clear` 当作 claim authority。
- [ ] 将新 Wiki 接入 Search v3，支持 grounded answer、citation jump 和从回答保存回 Wiki。
- [ ] 完成 `Markdown/PDF → queue → compile → review → write → grounded query → unchanged skip` 的首个 vertical slice。

### Parallel Reliability Track

- [ ] 在最终消息装配点执行 L1-L5 总 token 预算。
- [ ] 使用确定性 fallback artifact ID。
- [ ] 修复聊天重载后的 context envelope 与共享 MemoryManager 恢复一致性；旧 `handleProjectSwitch()` 已发出的 clear/save 目前不能由外层 ProjectManager generation guard 撤销。
- [ ] 为同一生命周期的 project switch 增加 request-scoped context/load authority；当前旧请求不能发布 selection/UI，但已经进入 `loadProjectContext()` 的缓存、解析与 `ProjectLoadTracker` 子操作不会被后续 switch 撤销，可能继续消耗资源并把旧项目进度写入共享 loading atom。
- [ ] 把非 Project 模式的 `PDFParser`/`PDFCache` 从动态全局 `app.vault` 改为显式 Vault owner；当前 project-mode `Docs4LLMParser` 已隔离，但普通 PDF 解析响应仍可能在跨 Vault 切换后写入错误的全局 PDF cache。
- [ ] 把 `VectorStoreManager` 从永久 global-app singleton 改为 exact App/Vault lifecycle owner，并在 unload/rebind 时停止旧索引 continuation、移除旧 Vault listener；当前 G.2i 只隔离 Projects/Bundle discovery，不宣称语义索引已跨 Vault 隔离。
- [ ] 为已进入 provider 初始化的旧 ChainManager 增加最终模型提交 authority；当前 dispose 会解除订阅并停止 await 后续 housekeeping，但无法取消已经交给共享 ChatModelManager 的 provider 构造。

## Architecture Decisions

- 新方案以个人知识资产可拥有、可追溯、可迁移为产品边界。
- 以 `MessageRepository`、`ChatManager`、L1-L5 `PromptContextEnvelope` 和 `ToolRegistry` 为当前架构基线。
- LangChain 路径按当前已实现能力描述；ACP、显式 Planner 和动态 MCP 接入按演进目标描述。
- 迁移 SOC 方案的控制流、证据边界、权限、候选式学习和薄入口原则，不迁移告警、租户、Kafka、PostgreSQL、复核队列等领域结构。
- 将 Karpathy 的 LLM-maintained Wiki 作为知识复利层，将 OKF 作为该层的可选标准化文件契约；二者都不替代 Copilot Runtime。
- 不引入第二套桌面应用、Python sidecar 或图数据库作为 MVP 前提；优先在现有 TypeScript Runtime 内实现编译式 Wiki 契约。
- 优先借鉴 `llm-wiki-compiler` 的两阶段编译、manifest、ChangeSet、lint/eval 和 OKF 互操作，但在依赖和代码审计前不直接接入其 Node 24 CLI Runtime。
- Chat 继续作为即时助手入口，新增全页 Knowledge Studio 承载 Sources、Wiki、Review、Activity、Graph 和 Health。
- 体验优先不等于直接写盘：信任机制通过阶段进度、citation、multi-file diff、事务和撤销融入主流程。
- 当前 Search v3 是基础检索底座；只增加 Wiki 渐进发现和一跳图扩展，不引入 LanceDB 或第二套基础搜索。
- Source hash 必须与 pipeline fingerprint 共同决定幂等，避免 schema、parser 或模型变化后错误跳过。
- `llm_wiki` 主要提供 Knowledge Studio、队列、来源卡片、Review Inbox 和图谱 UX；不引入 Tauri/Rust Runtime。
- `claude-obsidian` 主要提供 hot/index/domain/page、Manifest 与 Ingest/Query/Lint 工作流；不复制 Bash 锁和自动 Git hook。
- Graphiti 当前只贡献 provenance 和双时态契约；只有真实历史/多跳需求达到门槛后才作为可重建外部投影接入。
- 外部源码按 Copy / Port / Reference 管理；首次复制时同步提交 attribution、许可证、原 commit/path 和修改说明。
- 当前唯一产品与验收平台是 Windows Obsidian Desktop；不为 macOS、Linux、iOS、Android 或浏览器增加兼容工作。
- 允许在 adapter 边缘使用有明确体验收益的 Windows/Node/Electron 能力，但纯知识契约保持 TypeScript 与 I/O 无关。
- 不因平台收敛直接嵌入 Node 24 CLI；所有依赖仍须兼容 Obsidian 实际捆绑的 Windows Runtime。
- Manifest 不保存 processing 状态；持久来源状态与当前 Queue Job 在 UI 层按 `sourceId` 联结。
- `sourceContentHash` 使用原始文件字节；citation quote 只统一换行符；ChangeSet before/after hash 保留精确文本字节语义。
- Windows Vault path 在领域边界选择“验证并拒绝”，不静默修复盘符、UNC、反斜杠、保留设备名或碰撞目标。
- production profile source 只能向 Pipeline fingerprint 传入显式 allowlisted model behavior 与受检静态 JSON 配置；Core 另防御性拒绝常见 credential-like 字段，但不把黑名单当成 allowlist 的替代品。Knowledge model adapter 只消费该 projection 和匹配的私有 route capability；首个 DeepSeek route factory 把 credential/native-fetch port 限定在私有 closure，零网络 preflight 构造验证后立即丢弃 route，只保留脱敏结果；未来 worker composer 也只能传 exact selected values，禁止再次把完整 settings 暴露给 Core。
- Queue 的 `inputRevision` 必须由 source adapter 按 source 严格单调分配、跨重启保存，并在相同内容观察时同样推进；不得使用内存计数器或文件 mtime 代替。
- source adapter 必须在 watcher handler 开始、任何异步读取/解析之前生成全局永久唯一且跨重试稳定的 `captureId`（生产 adapter 使用随机 UUID/等价 opaque id）并分配 opaque observation token/`inputRevision`，再以 first-write-wins hand-off 绑定随后实际读到的 bytes hash 与 pipeline fingerprint。Queue adapter 只能凭该 token 原子消费 exact bound record；相同 capture 重放不增 revision且已 consumed 时不再调用 Queue，不同 payload 冲突，较新未绑定 allocation 不会丢弃仍可用的较低 bound read，较新 bound read 才会淘汰较低未终结观察。独立 Obsidian watcher、opaque plan Core、双采集 snapshot loader、UTF-8 byte parser、durable Queue/Runtime job binding、重启 observation reconciler/coordinator、确定性 prompt、DeepSeek private transport 与零网络 credential/native-fetch preflight 已实现隔离身份边界；worker 对 credential/native-fetch 的一次性执行 lifecycle、compiler worker 和其余 plugin workflow 接线仍待完成。
- model execution authority 固定为 Queue local claim + Runtime atomic proof + workflow stable reproof 三层：Queue claim 绑定 exact QueueStorage 并在退出 executor 前同步 revoke；opaque owner 排他注册 exact Runtime/workflow loader；Runtime proof 必须同时命中 exact processing attempt、consumed observation 与 Manifest；workflow 在 source read/parser/model 前后复证 retained Manifest/schema/profile。任何层都不接受同型 DTO 替代，模型 adapter 也不接受 caller 替换 `AbortSignal`。每份 authorized preparation 只能绑定一个 adapter；每次 compile 使用独立 opaque session，analysis/generation 必须同源；private route 没有 public invoke，provider output 必须先经过 bounded data-only snapshot，再通过 post-call authority proof 才能发布。
- Compiler source identity 与 ChangeSet/review identity 同时包含 `inputRevision`；因此来源 A→B→A 不会复用第一次 A 的审核实例。该来源观察 ABA 已关闭，不等同于仍待决定的文件写入 content ABA。
- Queue 在 claim 外持久每个 source 的 `sourceHighWatermarks`（revision + source hash + pipeline fingerprint）；等价的更新观察不能改写已认领输入，但必须推进 high-watermark 以拒绝后到的旧内容。
- `QueueStorage.write` 的 expected revision 比较与完整 snapshot 替换必须是 adapter 内的同一原子操作。
- Queue snapshot 使用 strict、显式版本化 schema；新增持久字段必须配套版本升级和迁移，不得依靠静默宽容读取。
- Queue 执行语义是 at-least-once：持久 claim 保证同一时刻至多一个 active job，attempt ownership 拒绝迟到结果，但终态写入前崩溃仍可能重放模型调用。
- EventSink 只提供非阻塞 post-commit 通知，持久 Queue snapshot 才是状态真相。
- ChangeSet journal v3 使用 Vault-global 单活动槽；完整 Bundle、accepted ChangeSet、精确 before/after、最终 Manifest intent/plan digest，以及 owning source id/hash/pipeline/input revision/job attempt/start 一并持久化；claim source 必须存在于 ChangeSet source refs。
- 单文件写入只能通过 adapter 原子 compare-and-swap；全局 journal 提供可恢复语义，但 Obsidian Vault 不具备真正跨文件原子性。
- 自动恢复只做幂等 roll-forward；divergent state 进入 sticky `recovery_required`，不自动 rollback 或覆盖用户编辑；committed marker 对后续用户修改保持权威。
- Queue v5 保留 exact `applyClaim`、pending review anchor、review rejection tombstone、no-journal apply abandonment tombstone 与 `commit_pending_ack` marker；审核 hand-off 固定为 durable pending record revision 0 → exact accepted/rejected record revision 1。accepted apply claim 同时绑定 proposal digest、accepted digest、最终 Manifest intent digest、review revision/time 与完整 job claim；启动恢复改为 failed 后仍原样保留。v1/v2 无法证明的 review/apply state，以及 v3 已开始但未保存 intent digest 的审核 apply，都以 `legacy_unverified` 明确 fail closed；v4 严格迁移时只初始化空 abandonment 历史，不猜测 cancelled job 的旧决策。
- no-journal recovery id 只由跨完整生命周期仍会保留的 accepted Review identity 派生，不把最终成功后已不再保存的 `startedAt` 放入 id；action 时仍必须从当前 exact Queue claim 重新取得并核验 `startedAt`。continue 先由 Runtime 重证 Queue/Review/high-watermark/allocator/Manifest/source-order，再由 prepared-journal atomic publication 重证；abandon 则在同一 envelope transform 中要求任何 phase 的 active journal、Queue commit marker 与同 source/input ledger 均不存在，保留 immutable accepted Review，并写 cancelled job + exact tombstone。
- startup gate 必须持有严格的当前 Bundle 配置，因为 prepared/applying journal 的已有证据恢复可能真实观察并 CAS Wiki；Gate 先恢复 Queue，再收敛 active transaction/commit marker、Review pending/rejected hand-off，最后由 Runtime 在同一 envelope 中按 exact Review revision 批量返回 Queue、Vault-global transaction 与 accepted classifications。Gate 用统一 opaque recovery id 精确证明 accepted actions/classifications 一一对应，并拒绝 Runtime revision 回退、Queue revision 回退或同 revision 不同内容；`observed_clear` 不直接释放 startup pause。独立 release coordinator 只传递 Runtime/Review/Queue revision，Runtime 在同一 envelope mutation 中重证全部写恢复和 accepted terminal evidence后才释放；generic Queue facade 将 `startup_recovery`、`recovery_required` 与 `commit_pending_ack` 作为单调门，同门 mutation 必须保留 current claim/job/marker，running/startup 的 accepted-not-started pending anchor 也不能被 generic CAS 擦除；跨门 mutation 必须等于 exact accepted-begin、startup-recovery、current-active-committed resolution 或固定 metadata 的 marker-finalization 投影，任何一步都不能借历史 ledger 或伪造 abandonment 替换证据或降级为 running/user/rate-limit。Runtime 专用 abandon 是 append-only tombstone 的唯一写 authority；watcher high-watermark 不可删除、回退、同 revision 改写或超过 allocator，合法较新观察仍可更新 processing/awaiting job 的 rerun 状态与单调 `updatedAt`。没有 durable release receipt 时，commit-then-throw 后旧令牌只会失效，调用者必须重跑 Gate，不能用 `revision + 1` 猜测成功。
- Review 启动协调器只拥有 Review/Queue port：按 `recordedAt + changeSetId` 稳定扫描，pending/rejected 做 exact、可重放的 Queue 收敛；accepted 只返回供更高层 journal/ledger/no-journal 分类的 identity，绝不在启动时调用 `beginReviewApply`。每轮 mutation 后重读 Review revision，持续变化超过有界次数即 fail closed；返回 revision 仍须在最终 startup gate 复证。它尚未接入 plugin startup，不能据此开放 Studio。
- applying executor 必须返回 `completed + exact commitReceipt`；`IngestQueue.runNext` 只对外转换为 `commit_ready`，Queue 在 Manifest 之前仍保持 processing/applying。审核接受转换为新的 durable applying claim，不能直接完成 job。
- 页面提交后的协议固定为只读 `queue claim verify`，再持久执行 `atomic authority reproof+manifest+ledger → queue marker → journal ack → queue release`；最终 callback 从同一 runtime envelope 复证 Queue claim、accepted Review payload/intent 及 `allocator ≥ source high-watermark ≥ apply claim`，再按 exact transaction/revision/ChangeSet/intent/journal/receipt digest 幂等发布成功。较新的同内容观察可并存；较新的不同内容必须由 retained rerun 或启动恢复后 promoted successor 精确证明。
- 每个 retained rerun 必须精确等于该 source 当前 high-watermark；终结旧 apply 时若已有同 source active successor，rerun 继续归 successor 所有，不能被重复提升为第二个 active job。
- `commit_pending_ack` Queue marker 必须在每次 runtime parse 时与同一 transaction 的 apply ledger 精确匹配 source/hash/pipeline/input/ChangeSet/digest/revision/time；active journal 已清除但 ledger 缺失或 marker 撕裂时启动直接 fail closed。
- 直接 `ChangeSetTransaction.apply` 在 target observation 前必须通过 runtime authority port 复证 Queue/Review/high-watermark/allocator 与 Manifest read-set/source commit ordering；prepared 发布在同一 shared-envelope transform 内重复证明以关闭预检竞争。prepared → applying、启动读取 prepared/applying journal 以及活动事务期间的每次 envelope mutation 都保留同一完整 reservation；Queue v3 缺少最终 intent digest或 Manifest 已漂移时，不会先观察或修改 Wiki 文件再到最终 ledger 才失败。
- 清除 Queue marker 后仍保留 `startup_recovery` 暂停；必须重跑 Gate 并通过 conditional release，普通 Resume 不能清除该 gate。
- Compiler stage 1 只能从 caller-owned target catalog 读取/修改已知页；catalog 外路径只拥有 create-only 权限，resolver 必须使用 Windows 大小写不敏感 existence probe，既有内容不得送入生成模型。
- Compiler delete 只允许 Manifest 标记为 generated、由当前 primary source 独占且当前 bytes 仍匹配 last-generated hash 的目标；该授权已进入 Review plan/journal intent，并在 Runtime reservation/commit 复证，但安全 compare-delete 与其余 semantic dependency 未完成，产品仍保持 reject-only。
- Stage 2 只接收 runtime 绑定后的 create/update targets，并按 exact target-set digest 返回 `targetId + write/unchanged`；delete 原文、自由路径、operation、hash、validation 和 status 永不进入模型输出契约。
- 模型 citation 只能选择本次实际提供的 evidence id；source/artifact identity、locator 与 quote hash 来自 parser-owned registry，普通 claim 必须至少有一条 material-valid `supports`，`context/contradicts` 不构成 grounding。
- Candidate validator 是构造 proposed ChangeSet 的必需端口；三项 validation flag 均须由确定性 runtime 返回 true，Compiler 永不构造 accepted/applied 状态，apply-time 仍独立复验。
- legacy ApplyView 的 `Vault.create/modify` 路径继续只服务既有单文件工具；Knowledge Review 只复用纯 diff renderer，绝不把该路径当作 ChangeSet transaction writer。
- Review UI 只能提交 proposal/snapshot identity 与 opaque change/block ids；选择后的 content/hash/validation/accepted status 与完整 ChangeSet digest 由 Core 重建，同时保留 proposal-owned change identity/order，UI 无 Vault/App/storage 依赖。
- Durable Review Store v2 独立于 Queue 保存完整 proposal、Manifest commit plan 和终态 payload/intent；Queue 不能只靠 `awaiting_review.changeSetId` 支撑重启恢复。executor 必须先保存 pending record，再把精确 receipt 交给 Queue；启动编排器在重新 claim 前必须扫描并调用 `reconcilePendingReview`。
- `awaiting_review` 不能走通用 `cancel()`；拒绝必须先写入 durable Review Store，再由 `rejectReview()` 原子移除 pending anchor、写 rejection tombstone 并提升 retained rerun。
- 同一 accepted receipt 在 active applying claim 内重放不增加 revision/event；冲突 receipt fail closed。apply 已完成并清除 claim 后的晚到接受不重新 apply，而由未来 Review/Manifest 协调器判断 already-applied。
- Compiler delete 的 Manifest ownership、source provenance 与 last-generated hash read-set 已持久复证；在原子 compare-delete、schema/link/source-artifact dependency 与 Windows 实机验收完成前仍始终以 `reject_only` 呈现。
- Activity 的唯一事实源是 strict durable Queue snapshot；EventSink 只是 reload hint，`applyCommit`/`commit_pending_ack` 清除前 completed job 必须显示为 finalizing。
- Queue v3 引入、v4 保留的 `reviewRejections` 为拒绝动作提供跨崩溃幂等和 identity conflict 防护；拒绝先持久化 Review Store，再转换 Queue job，不制造空 accepted ChangeSet。
- Knowledge Studio 只在 Windows Obsidian Desktop 注册。整个 Copilot 插件仍保留原有平台范围，因此 `manifest.json` 不改为 desktop-only；这不构成对其他平台 Knowledge Studio 的支持承诺。
- 当前 Windows 启动初始化插件私有 `knowledge-runtime-v1.json`（文件名稳定、outer schema v3），再由 layout-ready Barrier 等待普通 Projects readiness、严格验证全部 project-owned Bundle、执行 generation-owned 零网络 preflight，并通过稳定 port + 动态 session 发布 unavailable 状态。成功预检后由 recovery-only startup Gate 先收敛已有 durable bookkeeping 和已授权 journal；只有 `observed_clear` 才会启动同代 observation reconciler/coordinator，注册 Vault listeners 并把合法 source observation 放入 held Queue。初次 observation 收敛后，session 会 reprove observation、运行 fresh Gate 并尝试 Runtime conditional release；只有所有配置 Bundle 通过才改变 startup Queue control。它仍不运行 worker、compiler/model、no-journal decision 或 query coordinator，不会展示伪造任务或生成新 Wiki 内容。
- Projects 生命周期以显式 App/Vault、opaque state owner 与 exact pending-write lease 为边界；新插件在任何 state reset 前同步退役旧 ProjectManager，旧 scan/CRUD/cleanup 不能发布到新 owner，同一生命周期 project switch 只允许最新请求发布 selection/UI。已经发给 Obsidian adapter 的单次 I/O 无法取消，因此 continuation 停止后由新 lifecycle rescan 收敛；同生命周期的旧 project context/load 子操作尚待 request-scoped authority，可重建的 context/file cache 若要做到同 Vault 已发出 I/O 的绝对隔离，仍需 epoch 路径或 adapter-level CAS。
- `knowledge-runtime-v1.json` 是首期个人规模的共享 envelope：跨子系统 CAS 简单，但每次 mutation 都全量 parse/validate/stringify/rewrite，存在 O(envelope size) 写放大和共享腐坏故障域。真实长期使用前必须设 size/latency threshold、协调 terminal archive/compaction，并在超过门槛时分片；它不是产品必须永久保留的核心格式。
- Runtime outer schema v3 继续只为可证明 idle、无历史 Manifest success 的 v1 envelope 执行原有受限迁移；v2→v3 是结构性迁移，可保留 active/recovery/ledger，但必须证明 Queue high-watermark 不超过 scalar allocator。迁移把既有 allocator revision 设为 `managedAfterRevision` fence，并只保存当时 exact Queue watermark 为 legacy checkpoint；不会为历史 revision 伪造 observation token，下一次 capture 从 fence + 1 开始。
- Runtime v3 每次 parse 都交叉验证 Manifest、reserved commit extension、apply ledger，以及 observation journal/Queue watermark；历史 ledger 可保留，但 extension 必须指向该 Bundle/source 的最新 proof，当前 revision 与连续 digest 链不能倒退或断裂，多来源同 Windows path 必须保持 exact shared ownership/hash。
- `SourceManifestStorage` 的通用路径只允许注册/重命名/失败观察和非保留 metadata；`lastSuccessful` 与 runtime reserved commit extension 只能由 atomic Manifest+ledger adapter 修改。shared page 的实际 owner 集由 exact Manifest digest 冻结，commit 时要求 owner authority 一致并同步推进所有 owner hash。
- 首次 runtime 文件以同目录完整临时文件、handle flush 和排他 hard-link 发布，避免并发启动暴露空/半 JSON；目录级掉电持久性与 NTFS/OneDrive/junction 行为仍属于 Windows 实机验收，不作超出证据的承诺。

## Testing Checklist

- [x] 核对所有“当前能力”均能在代码或现有设计文档中找到依据。
- [x] 核对所有规划能力均明确标记为目标或阶段项。
- [x] 运行 Markdown 格式检查。
- [x] 检查 Git diff，确保未修改无关文件。
- [x] 检查新增 PRD、主方案与复用台账之间的链接和决策一致性。
- [x] 检查 Markdown heading/fence/link 结构并运行 `git diff --check`；仓库没有安装 `node_modules`，未调用 Prettier。
- [x] 确认没有修改 DeerFlow/SOC 文件或把 `.env.test` 纳入版本控制。
- [x] Knowledge foundation 通过 TypeScript `noEmit`、目标 ESLint、本次变更文件 Prettier check 与 16 个 Jest suite / 444 个测试。
- [x] Commit E 集成后全仓单元回归通过：131 个 Jest suite / 2551 个测试；现有测试中的预期 console 警告不影响结果。
- [x] Commit F 通过 5 个定向 Jest suite / 79 个测试、TypeScript `noEmit`、全仓 ESLint 与本次变更文件 Prettier check；集成后全仓 135 个 Jest suite / 2623 个测试全部通过。
- [x] Commit G 通过 TypeScript `noEmit`、全仓 ESLint、15 个定向 Jest suite / 190 个测试，以及全仓 148 个 Jest suite / 2750 个测试；预期的既有 keychain/解密 console 输出不影响结果。
- [x] Commit G 新增/修改文件均经 Prettier 格式化；全仓 `npm run format:check` 只被本次未修改的既有 `src/LLMProviders/chatModelManager.ts` 格式基线阻塞，继续不夹带修改。
- [x] G.1 runtime adapter foundation 通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier check，以及全仓 151 个 Jest suite / 2794 个测试；既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2 Review startup coordinator 的 12 个定向测试通过：pending/rejected 收敛、重启幂等、accepted 零写入分类、Review revision 前进/持续 churn、并发 terminal decision 导致旧 Queue 操作失败后的 revision retry、稳定多记录排序、双 runtime pending/rejected 竞争、commit-then-throw 后置状态证明及 source identity 冲突；TypeScript `noEmit` 与变更文件 ESLint 通过。
- [x] G.2a 集成后全仓回归通过：152 个 Jest suite / 2806 个测试、TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier check 与 `git diff --check`；既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2b durable Manifest plan/intent 与 atomic ledger 通过 13 个定向 Jest suite / 273 个测试；最终全仓回归 154 个 Jest suite / 2871 个测试全部通过，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2c Queue v4 与 atomic apply-authority reproof 本轮通过 9 个定向 Jest suite / 234 个测试，以及全仓 154 个 Jest suite / 2894 个测试；覆盖 target observation 前 authority preflight、prepared/applying/startup 全 Manifest reservation、unfinished-journal 写前授权、Queue marker↔ledger、latest-rerun/source-ABA 与 existing-successor 回归，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2d no-journal recovery 与 Queue v5 本轮通过 Runtime 71 个测试、Queue/Core 聚焦 101 个测试及真实 compile→review→startup recovery→explicit continue→Wiki/Manifest/ledger/Queue ack 集成闭环；最终全仓 155 个 Jest suite / 2921 个测试全部通过，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。覆盖 accepted-not-started/active/blocked/finalizing/committed/abandoned 分类、stale Manifest、commit-then-throw、prepared publication 竞争、三代 rerun 保留与 exact abandonment replay；既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2e layout-ready startup gate 通过 Gate/Runtime/no-journal/真实 pipeline 聚焦 4 个 suite / 108 个测试，以及全仓 156 个 Jest suite / 2947 个测试；覆盖固定恢复顺序、同 transaction sticky conflict 归一、Runtime/Queue revision 单调性、同 revision Queue 内容一致性、accepted action↔classification 精确集合、atomic Vault-global transaction、no-journal/accepted-not-started attention、active/finalizing/Queue-only blocker、Review revision churn、真实 accepted-before-apply 字节零写入与无自动 action，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2f atomic conditional startup release 通过 Release/Runtime 聚焦 2 个 suite / 106 个测试、Queue/真实 pipeline 聚焦 2 个 suite / 55 个测试，以及全仓 157 个 Jest suite / 2980 个测试；覆盖 exact release、absent/running byte-preserving no-op、Runtime/Review/Queue stale token、user/rate-limit pause保真、generic resume/pause/CAS bypass、running/startup 下显式 exact accepted start 与 anchor 防擦除、三类恢复门单调强化、同门 claim/job/marker 保留、历史 ledger/伪造 abandonment 替换、fixed finalization metadata 与两步 CAS 降级防护、startup apply 期间真实 allocator-backed watcher divergent rerun、high-watermark 回退拒绝、较新同内容收敛与未分配首写拒绝、active transaction、claim/failed apply/commit marker、accepted-not-started blocker、abandoned/committed terminal、跨 Bundle recovery quarantine、双实例竞争、commit-then-throw 后强制重跑 Gate 与真实 stale→re-gate→release 闭环，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2g Runtime v3 durable source-observation hand-off 通过 Runtime/Queue/真实 pipeline 聚焦 3 个 suite / 167 个测试，以及全仓 157 个 Jest suite / 2997 个测试；覆盖全局 capture identity、并发幂等 allocation、first-write-wins 并发 binding、adapter-private Queue token capability、higher-unbound/older-bound 两种顺序、bind 与 Queue commit-then-throw、A→B→retry A terminal replay、无新 clock/entropy 的精确重放、重启 pending work 枚举与消费、startup blocker、v2 migration fence/checkpoint 和不可达 journal corruption matrix，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2h Project Bundle config 与插件级 fail-closed 前置接线通过 12 个定向 Jest suite / 97 个测试，以及全仓 164 个 Jest suite / 3055 个测试；覆盖 normal/hidden frontmatter 原值保存、普通编辑保留与显式删除、strict aggregate config、跨 Bundle Windows 边界冲突、ProjectRegister single-flight/cleanup、layout/runtime 到达顺序、stable delegate generation cancellation、动态 exact Bundle session、旧 View 切换及 unload fail-close，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2i Projects/插件 lifecycle ownership 通过 16 个聚焦 Jest suite / 119 个测试，以及全仓 175 个 Jest suite / 3124 个测试；覆盖 same-App hot reload、跨 App/Vault rebind、opaque state owner/lease、prepared-scan CAS、初始化/重载/目录扫描竞争、目录 B→C、旧目录 event/CRUD 反向恢复、migration/frontmatter continuation、ProjectLoadTracker、ProjectManager selection latest-wins、ChainManager subscription teardown、Runtime 动态加载、VaultDataManager listener/metadata owner 与 log flush/clear/open 的显式 Vault 归属，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2j Vault source watcher adapter 通过 watcher 聚焦 1 个 suite / 21 个测试、Runtime/Queue/真实提交管线相关 4 个 suite / 188 个测试，以及全仓 176 个 Jest suite / 3145 个测试；覆盖 exact App/Vault/adapter owner、listener-before-scan、allocate-before-read、BOM/CRLF/Unicode exact bytes、captured path、retry payload freeze、reader/allocation/settlement success 复证、capture-id factory 同步重入、partial EventRef 回滚、close/plan/source generation 交错、rename/delete/folder quarantine、old-path ABA、无关 source 在途任务保留、同路径多 Bundle 与 Runtime v3/Queue v5 乱序收敛，并通过 TypeScript `noEmit`、全仓 ESLint、变更文件 Prettier 与 `git diff --check`。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2k opaque watch-plan builder 最终通过 fingerprint/relationship/builder/watcher 聚焦 4 个 suite / 145 个测试，以及全仓 177 个 Jest suite / 3217 个测试；覆盖 strict Bundle↔Manifest source/generated-root boundary、Manifest reorder/revision identity、exact BOM/CRLF/中文 schema bytes、真实 `Uint8Array` brand/覆写 `slice()` 与跨 Bundle schema conflict、Bundle/Compiler/parser/model 关键行为 fingerprint、通用 parser routing、常见 credential-like key/cyclic/non-JSON/accessor/`__proto__` 配置、单次数组长度快照与稀疏数组拒绝、Windows collision、跨 Bundle 同物理 source、空 Bundle malformed handoff、wrong-token direct construction、WeakMap state 不可反射、class/prototype monkeypatch 拒绝、同路径多 Bundle/notification replacement 重入、handoff capability 快照、injected success DTO getter/TOCTOU、启动时缺失 source 后续捕获，以及 replacement 后已开始 commit 可完成但禁止旧通知。并通过 TypeScript `noEmit`、全仓 ESLint、production build、变更文件 Prettier 与 `git diff --check`；全仓 `format:check` 仍只被本轮未修改的既有 `src/LLMProviders/chatModelManager.ts` 基线阻塞。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2l 只读 workflow-plan/profile/byte-parser boundary 通过 6 个聚焦 suite / 152 个测试，以及全仓 181 个 Jest suite / 3298 个测试；覆盖 exact model/no fallback、unknown/API-key getter 零读取、无关 model 重排/新增稳定性、secret/config guard、endpoint/routing 哈希摘要、原输入 mutation 与 WeakMap reflection、double collect/change detection、10,001 model/parser registry 资源上限、空 Manifest parser registry、wrong-token execution/loader forgery、Manifest/schema/generation parser 前后 stale、exact reader-owned bytes、parser resolve/reject mutation 与 success/invalid `Proxy` 校验期 mutation、malformed DTO/accessor、fatal UTF-8、BOM/CRLF/中文/emoji byte round-trip、schema byte/parsed character/parser byte 上限、取消与 deep freeze。并通过 TypeScript `noEmit`、全仓 ESLint、production build、变更文件 Prettier 与 `git diff --check`；全仓 `format:check` 仍只被本轮未修改的既有 `src/LLMProviders/chatModelManager.ts` 格式基线阻塞。首次沙箱内 Jest 因 localhost `EPERM` 失败，按相同参数在允许本地测试通道的环境重跑并全绿；既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2m Queue/Runtime/workflow/model execution capability boundary 通过 10 个聚焦 Jest suite / 345 个测试，以及全仓 185 个 Jest suite / 3361 个测试；覆盖 exact QueueStorage claim、同步 revoke、strict executor-result descriptor snapshot、unique Queue tuple/consumed observation/Manifest atomic proof、同 owner 跨 Runtime/loader generation 拒绝、Runtime↔workflow proof sandwich、preparation one-shot、Manifest/schema/profile drift、单 preparation 单 adapter、Compiler one-shot call authority、per-compile opaque session、跨 Compiler/跨 compile/重放拒绝、private route/profile digest、固定 wire/decoded-object 上限及真实 profile projection。并通过 TypeScript `noEmit`、全仓 ESLint、production build、变更文件 Prettier 与 `git diff --check`；全仓 `format:check` 仍只被本轮未修改的既有 `src/LLMProviders/chatModelManager.ts` 格式基线阻塞。未运行尚不存在的真实 provider transport 集成测试，既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2n Prompt/DeepSeek route 最终通过 4 个聚焦 Jest suite / 99 个测试，以及全仓 187 个 Jest suite / 3393 个测试；覆盖 canonical two-message prompt、受限 Schema policy 与 prompt-injection 数据隔离、descriptor-only Proxy snapshot、actual prompt/provider-route identity 进入 pipeline fingerprint、完整 profile digest、旧模型/custom endpoint/unsupported setting/credential fail-close、thinking/sampling 参数互斥、exact Queue signal、HTTP 返回后及 pending read 期间非阻塞 cancel、两阶段单次 POST、JSON Object/no streaming/fallback/retry、keep-alive whitespace、429 body 零读取、network/header cause 脱敏、redirect/URL/SSE/length/model/choice/usage/tool/function-call rejection、fatal UTF-8、声明/实际响应 byte cap 与 one-byte tiny-chunk 有界 buffer。并通过 TypeScript `noEmit`、全仓 ESLint、production build、变更文件 Prettier 与 `git diff --check`；全仓 `format:check` 仍只被本轮未修改的既有 `src/LLMProviders/chatModelManager.ts` 格式基线阻塞。真实 DeepSeek 网络 smoke、Windows Obsidian native-fetch/CORS/proxy/abort/reload 验收与 production credential/worker 接线仍未运行，既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2o DeepSeek V4 catalog、错误投影与零网络 production preflight 最终通过 15 个聚焦 Jest suite / 313 个测试，以及全仓 194 个 Jest suite / 3465 个测试；覆盖 V4 migration/dedup/idempotence、retired/unknown direct model 的 user-selection/default/Project/Command/Quick Ask fail-close、High→high 与 XHigh→max、Low/Medium 拒绝、thinking/sampling/UI/Project 动态归一、fallback 最终模型与非法 base temperature 区分、production profile/parser identity、exact credential precedence/accessor rejection、eager zero-fetch preflight/close、Profile/Transport/Adapter/Compiler private brand 防伪、Compiler 与 Queue executor error 的 exact attempt signal 防重放，以及 400/401/402/422/429/500/503/未知 HTTP → durable fail/retry/rate-limit pause 矩阵和 network/invalid response 脱敏。通过 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build 与 `git diff --check`；未运行真实 DeepSeek 网络 smoke、`main.ts` 插件级生命周期自动化或 Windows Obsidian native-fetch/CORS/proxy/abort/reload 实机验收，production worker/Gate/Queue/Review/Wiki 仍未接线，`.env.test` 与真实 API key 未进入提交。既有 keychain/解密预期 console 输出不影响结果。
- [x] G.2p generation-owned preflight 与 recovery-only production Gate 最终通过核心 composer/port 2 个聚焦 Jest suite / 24 个测试，以及全仓 197 个 Jest suite / 3510 个测试；覆盖构造/preflight 同步重入、exact frozen admission、Settings/Projects/Runtime/unload 失效、one-shot 顺序/并发防重放、Vault-global owner-first、各 durable phase 间 generation 复证、已进入 apply phase 的 crash-safe 完成、attention/blocked/unavailable 脱敏、真实 authorized prepared journal → Wiki/Manifest/ledger/Queue/journal 全收敛、外部文件冲突零覆盖并持久化 `recovery_required`，以及无 Release/worker/model/new transaction/no-journal 自动 action。通过完整 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build 与 `git diff --check`；未运行真实 DeepSeek 网络、Windows native exclusive-create/NTFS/OneDrive/crash 实机验收，组合测试的 Runtime file 为内存实现且 exclusive creator 为可观察 mock。既有 keychain/解密预期 console 输出不影响结果，`.env.test` 与真实 API key 未进入提交。
- [x] G.2q-a listener-first observation startup Core 最终通过 watcher/Runtime/reconciler/coordinator 4 个聚焦 Jest suite / 187 个测试，以及全仓 199 个 Jest suite / 3555 个测试；覆盖 listener-before-crawl、startup missing/case drift/Windows collision/capture failure/destructive-event quarantine、allocated/bound exact restart reconciliation、成功 crawl supersession、residual blocker、real Runtime/Queue convergence、allocated Release blocker、plan replacement/getFiles 同步重入、非可治愈 pre-crawl failure 零 crawl、diagnostic 同步退役、generation assertion 失效关闭，以及 error-code allowlist/WeakSet brand/freeze 防伪。通过完整 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build 与 `git diff --check`；未接 `main.ts`、conditional Release、worker/model/Review/Wiki，未运行真实 DeepSeek 网络或 Windows Obsidian 实机验收。既有 keychain/解密预期 console 输出不影响结果，`.env.test` 与真实 API key 未进入提交。
- [x] G.2q-b 同代 workflow lease 与 production observation composer 最终通过 4 个聚焦 Jest suite / 68 个测试，以及全仓 200 个 Jest suite / 3581 个测试；覆盖 authentic shared ProfileSource 对本次输入 snapshot 的独立等价复证及 subclass override 拒绝、detached owner 与 owned parser capability、同步 invalidation、真实 Runtime Manifest/schema/source 双采集、listener-first crawl、startup-held enqueue-only Queue、启动后 modify → revision 2 收敛、重复 quiesce/reconcile/blocker reproof、同步 health、外层 generation 在最终 blocker inspection 中失效、rename 与 lease replacement 关闭。通过完整 TypeScript `noEmit`、`npm run format`、全仓 `format:check`、全仓 ESLint、production build、文档 Prettier 与 `git diff --check`；没有接 `main.ts`、conditional Release、`runNext`、worker/model/Review/Wiki，也没有运行真实 DeepSeek 网络或 Windows Obsidian 实机验收。既有 keychain/解密预期 console 输出不影响结果，`.env.test` 与真实 API key 未进入提交。
- [x] G.2q-c plugin-owned observation session 接入通过 4 个聚焦 Jest suite / 72 个测试，以及全仓 200 个 Jest suite / 3583 个测试；覆盖 recovery clear 后才启动 observation、完整 convergence result 校验、observation blocker/diagnostic fail-closed、live watcher 保留、startup generation/cancel/Settings/Projects/Runtime replacement/unload 关闭，以及 enqueue-only Queue 不升级为 worker。通过 TypeScript `noEmit`、全仓 ESLint、全仓 `format:check`、production build、文档 Prettier 与 `git diff --check`；未运行真实 DeepSeek 网络或 Windows Obsidian 实机验收，conditional Release、worker/model/Review/Wiki 仍未接线。既有 keychain/解密预期 console 输出不影响结果，`.env.test` 与真实 API key 未进入提交。
- [x] G.2r observation → fresh Gate → conditional Release 接线通过 2 个聚焦 Jest suite / 37 个测试（含 Barrier/recovery composer）；覆盖 convergence 后同 signal reproof、fresh per-Bundle Gate、Runtime 原子 release、release blocked/observation drift/generation close fail-closed 与 released Queue 不开放 worker。通过 TypeScript `noEmit`、全仓 ESLint、文档 Prettier 与 `git diff --check`；production build/full regression 在本轮最终提交前重跑，未运行真实 DeepSeek 网络或 Windows Obsidian 实机验收。
- [x] G.1 自动化回归覆盖完整文件排他发布、并发初始化、realpath/symlink containment、共享 envelope 无丢失并发更新、跨 runtime revision 续号、watcher-capture 乱序拒绝、全 slot corruption fail-closed、create/update CAS 竞争与 delete replay 分类。
- [ ] 首批功能实现后，在 Windows Obsidian 测试 Vault 中完成 Golden Flow 实机验收。

## Source Documents

- [`designdocs/PERSONAL_KNOWLEDGE_OS_PRD.md`](./designdocs/PERSONAL_KNOWLEDGE_OS_PRD.md)：产品目标、Golden Flow、需求和验收标准。
- [`designdocs/PERSONAL_KNOWLEDGE_AGENT_SOLUTION.md`](./designdocs/PERSONAL_KNOWLEDGE_AGENT_SOLUTION.md)：技术架构、数据契约、可靠性与路线图。
- [`designdocs/OPEN_SOURCE_REUSE_PLAN.md`](./designdocs/OPEN_SOURCE_REUSE_PLAN.md)：外部代码 Copy / Port / Reference 台账。
- [`designdocs/GITHUB_PERSONAL_KNOWLEDGE_LANDSCAPE.md`](./designdocs/GITHUB_PERSONAL_KNOWLEDGE_LANDSCAPE.md)：开源项目调研快照。
- [`designdocs/PERSONAL_KNOWLEDGE_EXECUTION_PLAN.md`](./designdocs/PERSONAL_KNOWLEDGE_EXECUTION_PLAN.md)：Windows-only commit 顺序、依赖与退出门槛。
- [`THIRD_PARTY_NOTICES.md`](./THIRD_PARTY_NOTICES.md)：人工复制/派生外部材料的归属台账与发布门禁。
